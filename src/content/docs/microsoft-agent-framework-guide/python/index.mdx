---
title: "Microsoft Agent Framework (Python)"
description: "Unified Python agent SDK — core stable at 1.11.0 (July 2026). 40 volumes of source-verified class deep dives covering 400+ classes."
framework: microsoft-agent-framework
language: python
sidebar:
  label: "Overview"
  order: 0
---

import { Card, CardGrid, LinkCard, Aside } from '@astrojs/starlight/components';

**Unified agent SDK that merges Semantic Kernel and AutoGen** into one Pythonic library. Build single agents, multi-agent orchestration, cross-framework collaboration (A2A), and declarative workflows against the same primitives.

<CardGrid>
  <Card title="Install" icon="rocket">
    ```bash
    pip install agent-framework
    ```
  </Card>
  <Card title="Version" icon="information">
    Core **1.11.0** stable · `agent-framework-foundry` and `agent-framework-openai` bundled via `agent-framework==1.11.0` · July 2026 · Python 3.10 – 3.13 · **40 deep-dive volumes**
  </Card>
  <Card title="Best for" icon="puzzle">
    Azure-native agents, multi-agent orchestration via `WorkflowBuilder`, cross-framework A2A, declarative YAML agents.
  </Card>
</CardGrid>

<Aside type="caution" title="Earlier drafts had fabricated APIs">
  Pages under this guide previously documented `agent_framework.graphs.AgentGraph`, `agent_framework.a2a.A2AProtocolAdapter`, `AzureAIAgentClient`, `ChatAgent`, `ai_function`, and other names that **do not exist**. The real primary agent class in Python is `Agent`; the Azure chat client is `FoundryChatClient` from `agent_framework.foundry`. Pages were rewritten in April 2026 after introspecting `agent-framework-core==1.1.0` and re-verified in May 2026 against `1.5.0`. See [Errata in the comprehensive guide](/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_comprehensive_guide/) for the running list.
</Aside>

<Aside type="tip" title="azure-ai-agents integration add-on — class reference (6 volumes)">
  The `azure-ai-agents` package is an optional Microsoft add-on for the Azure AI Agents service. It is **not** a replacement for `agent-framework` — use it alongside the framework when you need direct access to Azure AI Agents service resources. Source-verified class deep dives covering **80+ classes/patterns** across 6 volumes are available in the Integration Reference section below.
</Aside>

<Aside type="tip" title="Where to start">
  New? Follow **Zero → Hero** below in order (~3–5 hours). Already shipping? Use **Jump to a topic** to go straight to what you need.
</Aside>

## Zero → Hero

A guided reading order across the comprehensive guide, 2025 features, advanced and production docs. Each checkpoint is a section — read linearly or dive in wherever suits you.

<CardGrid>
  <LinkCard title="1 · Setup & core fundamentals" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_comprehensive_guide/#core-fundamentals" description="Install, architecture, unified SDK, authentication · 20 min" />
  <LinkCard title="2 · Your first agent" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_comprehensive_guide/#simple-agents" description="Agent basics, lifecycle, task execution · 20 min" />
  <LinkCard title="3 · Model providers" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_model_providers/" description="OpenAI, Azure OpenAI, Foundry, Anthropic, Ollama, Bedrock · 15 min" />
  <LinkCard title="4 · Tools" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_tools/" description="@tool, FunctionTool, approval gates, schemas, runtime context · 15 min" />
  <LinkCard title="5 · MCP integration" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_mcp/" description="MCPStdioTool, MCPStreamableHTTPTool, MCPWebsocketTool · 20 min" />
  <LinkCard title="6 · Skills" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_skills/" description="Progressive-disclosure domain knowledge, scripts, resources · 15 min" />
  <LinkCard title="7 · Middleware" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_middleware/" description="Agent / chat / function middleware, redaction, retries · 20 min" />
  <LinkCard title="8 · Multi-agent orchestration" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_orchestration/" description="Sequential, Concurrent, Handoff, GroupChat, Magentic · 30 min" />
  <LinkCard title="9 · Human in the loop" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_hitl/" description="request_info, response_handler, orchestration HITL, tool approval · 20 min" />
  <LinkCard title="10 · Checkpointing" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_checkpointing/" description="Persist and resume workflows across restarts · 15 min" />
  <LinkCard title="11 · Compaction" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_compaction/" description="Sliding window, summarization, token-budget strategies · 15 min" />
  <LinkCard title="11b · Sessions & history" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sessions/" description="AgentSession, FileHistoryProvider, custom storage backends · 15 min" />
  <LinkCard title="12 · Observability & telemetry" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_observability/" description="OpenTelemetry spans, metrics, logs, Azure Monitor · 20 min" />
  <LinkCard title="13 · Evaluation" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_evaluation/" description="evaluate_agent, LocalEvaluator, @evaluator, ground-truth checks · 20 min" />
  <LinkCard title="14 · Structured output" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_comprehensive_guide/#structured-output" description="Pydantic schemas, validation, type-safe responses · 15 min" />
  <LinkCard title="15 · Graph workflows" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_2025_features/#graph-based-workflows---python" description="Directed graphs, streaming, time-travel · 25 min" />
  <LinkCard title="15b · Functional workflows" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_functional_workflows/" description="@workflow, @step, RunContext, HITL, checkpointing · 20 min" />
  <LinkCard title="16 · Declarative agents" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_2025_features/#declarative-agents-with-python" description="YAML/JSON config, Pydantic validation · 15 min" />
  <LinkCard title="17 · A2A protocol" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_2025_features/#agent2agent-a2a-protocol---python" description="Interop with OpenAI SDK, Claude SDK, LangGraph, Google ADK · 20 min" />
  <LinkCard title="18 · Production" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_production_guide/" description="Azure Container Apps, scaling, security, DR · 30 min" />
  <LinkCard title="19 · Class deep dives (Vol. 1)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives/" description="Agent, RawAgent, FunctionTool, WorkflowBuilder, RunContext, InlineSkill, MCPStdioTool — source-verified at 1.6.0 · 30 min" />
  <LinkCard title="20 · Class deep dives (Vol. 2)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v2/" description="FileHistoryProvider, AgentMiddleware, ChatMiddleware, FunctionMiddleware, CompactionProvider, ToolResultCompactionStrategy, TokenBudgetComposedStrategy, FileCheckpointStorage, LocalEvaluator, WorkflowRunResult — source-verified at 1.6.0 · 35 min" />
  <LinkCard title="21 · Class deep dives (Vol. 3)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v3/" description="BackgroundAgentsProvider, MemoryContextProvider + MemoryFileStore, TodoProvider + TodoFileStore, AgentModeProvider, SummarizationStrategy, ContextWindowCompactionStrategy, SlidingWindowStrategy, SelectiveToolCallCompactionStrategy, WorkflowViz, MCPStreamableHTTPTool + MCPWebsocketTool — source-verified at 1.7.0 · 40 min" />
  <LinkCard title="22 · Class deep dives (Vol. 4)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v4/" description="Message + Content, ChatOptions + ChatResponse + ChatResponseUpdate + UsageDetails, ResponseStream, AgentContext, FunctionalWorkflow + StepWrapper, WorkflowEvent taxonomy, SkillsSource composition layer, EvalItem + EvalResults + EvalScoreResult, TokenizerProtocol + CharacterEstimatorTokenizer, ConversationSplit + ConversationSplitter — source-verified at 1.7.0 · 45 min" />
  <LinkCard title="23 · Class deep dives (Vol. 5)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v5/" description="Executor + @handler + @executor, AgentExecutor + AgentExecutorRequest + AgentExecutorResponse, FanOutEdgeGroup + FanInEdgeGroup + SwitchCaseEdgeGroup, Runner + WorkflowMessage, SessionContext, AgentSession + register_state_type, BaseChatClient + SupportsChatGetResponse, SecretString + load_settings, WorkflowCheckpoint + CheckpointStorage, Exception hierarchy — source-verified at 1.7.0 · 50 min" />
  <LinkCard title="24 · Class deep dives (Vol. 6)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v6/" description="ExperimentalFeature + @experimental, WorkflowRunState + WorkflowErrorDetails, WorkflowExecutor + sub-workflow messages, AgentResponse + AgentResponseUpdate + ContinuationToken, BaseEmbeddingClient + embedding family, FunctionInvocationConfiguration, ClassSkill + SkillFrontmatter + FileSkillsSource + SkillsProvider, Annotation + TextSpanRegion, provider capability protocols, MiddlewareType + layer classes — source-verified at 1.7.0 · 55 min" />
  <LinkCard title="29 · Class deep dives (Vol. 7)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v7/" description="ContextProvider authoring, BackgroundTaskInfo + BackgroundTaskStatus, GroupChatBuilder + TerminationCondition, HandoffBuilder + HandoffConfiguration, MagenticBuilder + StandardMagenticManager + MagenticProgressLedger, SequentialBuilder + ConcurrentBuilder, AgentFactory + WorkflowFactory (declarative YAML), SecureAgentConfig + ContentLabel (prompt injection defence), FunctionalWorkflowAgent, ObservabilitySettings + configure_otel_providers — source-verified at 1.7.0 · 60 min" />
  <LinkCard title="30 · Class deep dives (Vol. 8)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v8/" description="AgentFileStore + FileSystemAgentFileStore + InMemoryAgentFileStore (file workspace), FileAccessProvider + FileSearchResult + FileSearchMatch (5-tool CRUD harness), MCPSkill + MCPSkillResource + MCPSkillsSource (SEP-2640 discovery), ToolMode + validate_tool_mode (tool-choice control), AgentEvalConverter + ExpectedToolCall + CheckResult + RubricScore (eval helpers), ChatContext (full chat middleware context), WorkflowAgent + WorkflowContext (workflow-as-agent), TruncationStrategy (oldest-first compaction), HistoryProvider + InMemoryHistoryProvider (history ABC), DelegatingSkillsSource + InMemorySkillsSource + FunctionInvocationContext (skills + progressive tools) — source-verified at 1.8.0 · 60 min" />
  <LinkCard title="31 · Class deep dives (Vol. 9)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v9/" description="OllamaChatClient + OllamaChatOptions (local LLM), PurviewPolicyMiddleware + PurviewChatPolicyMiddleware + PurviewSettings (compliance), DurableAIAgent + DurableAIAgentClient + DurableAIAgentWorker (durable actors), GitHubCopilotAgent + GitHubCopilotOptions (Copilot routing), HyperlightExecuteCodeTool + AllowedDomain + FileMount (Wasm sandboxing), HyperlightCodeActProvider (CodeAct context provider), Mem0ContextProvider (cloud semantic memory), RedisContextProvider + RedisHistoryProvider (Redis persistence), StandardMagenticManager + MagenticContext + MagenticProgressLedger (Magentic-One internals), FileSkillsSource + FilteringSkillsSource + AggregatingSkillsSource (skill discovery) — source-verified at 1.8.0 · 65 min" />
  <LinkCard title="32 · Class deep dives (Vol. 10)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v10/" description="Workflow + InProcRunnerContext (execution engine API), FunctionExecutor (plain-function graph nodes), FunctionInvocationLayer (custom chat-client mixin), MemoryStore + MemoryIndexEntry + MemoryTopicRecord (memory harness ABC), TodoStore + TodoItem + TodoFileStore + TodoSessionStore (todo harness), DeduplicatingSkillsSource, SkillsProvider (from_paths + multi-source), MCPTaskOptions (SEP-2663 long-running task lifecycle), InMemoryCheckpointStorage (test checkpointing), EvalScoreResult + CompactionStrategy + BaseAgent — source-verified at 1.8.1 · 70 min" />
  <LinkCard title="33 · Class deep dives (Vol. 11)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v11/" description="AgentTelemetryLayer + ChatTelemetryLayer + EmbeddingTelemetryLayer (OTel mixin layer — token/duration histograms, AGENT_PROVIDER_NAME, sensitive telemetry toggle), Edge + EdgeGroup + SingleEdgeGroup + InternalEdgeGroup (workflow graph primitives — conditional routing, serialisation round-trip, custom subclasses), Case + Default (runtime switch-case wrappers), EdgeRunner hierarchy (SingleEdgeRunner, FanOutEdgeRunner, FanInEdgeRunner, SwitchCaseEdgeRunner — delivery status, buffering semantics, concurrent dispatch), ExecutionContext (sub-workflow tracking — HITL pending_requests, collected_responses), WorkflowGraphValidator (7 pre-build checks: duplication, type compatibility, connectivity, self-loops, dead-ends, output validation), MCPTool + MCPSpecificApproval (base for all 3 MCP transports — full constructor reference, per-tool parsing, additional_tool_argument_names), SerializationMixin + SerializationProtocol (DTO round-trip — DEFAULT_EXCLUDE, INJECTABLE, dependency injection patterns), Evaluator + EvalItemResult + EvalNotPassedError (evaluation backend Protocol — custom evaluator impl, CI gate), PerServiceCallHistoryPersistingMiddleware (per-service-call history persistence — service_stores_history flag, sentinel conversation ID) — source-verified at 1.8.1 · 75 min" />
  <LinkCard title="34 · Class deep dives (Vol. 12)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v12/" description="Skill + SkillResource + SkillScript ABCs (contract methods, custom backends), FileSkill (SKILL.md content cache, scripts XML block injection, case-insensitive lookup), InlineSkillResource + InlineSkillScript (static vs callable resources, in-process execution, auto-generated parameters_schema), FileSkillScript + SkillScriptRunner (file-backed scripts, fixed CLI array schema, async runner protocol), SupportsAgentRun (structural protocol — custom agent without inheritance, isinstance guard), RunnerContext (full execution context protocol — messaging, events, checkpointing, streaming, HITL), SwitchCaseEdgeGroupCase + SwitchCaseEdgeGroupDefault (serialisable branch descriptors — condition_name, to_dict/from_dict, missing-callable placeholder), ValidationTypeEnum + WorkflowValidationError hierarchy (6 validation categories, EdgeDuplicationError, TypeCompatibilityError, GraphConnectivityError, match-based handler), A2AAgent + A2AAgentSession + A2AExecutor (URL/AgentCard/client init, dual streaming clients, auth interceptors, multi-turn sessions, A2A server exposure), WorkflowRunnerException + WorkflowCheckpointException + MiddlewareException (leaf exception classes, supervisor pattern) — source-verified at 1.8.1 · 80 min" />
  <LinkCard title="35 · Class deep dives (Vol. 13)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v13/" description="OpenAIChatClient+OpenAIChatOptions+OpenAISettings+RawOpenAIChatClient (Responses API — 4-layer MRO, STORES_BY_DEFAULT, x-ms-served-model header, reasoning model options), OpenAIChatCompletionClient+OpenAIChatCompletionOptions (Chat Completions API — logprobs, prediction, stateless vs stateful table), OpenAIEmbeddingClient+OpenAIEmbeddingOptions+OpenAIContinuationToken+OpenAIContentFilterException (embeddings+RAG, background polling, Azure content filter handling), AnthropicClient+AnthropicChatOptions+RawAnthropicClient (Anthropic Messages API — extended thinking, top_k, service_tier, beta flags), ClaudeAgent+ClaudeAgentOptions+RawClaudeAgent (Claude Code CLI agent — tool allow/blocklist, MCP servers, permission_mode, sandbox, workflow participation), AnthropicFoundryClient+AnthropicBedrockClient+AnthropicVertexClient (multi-cloud Claude — Azure Foundry AAD, Bedrock IAM, Vertex ADC, failover pattern), GroupChatOrchestrator+AgentBasedGroupChatOrchestrator+GroupChatState+AgentOrchestrationOutput (group-chat internals — round-robin and LLM selection, frozen state snapshot), HandoffAgentExecutor+HandoffAgentUserRequest+HandoffSentEvent+OrchestrationState (handoff internals — autonomous mode, HITL, event observability, checkpoint round-trip), MagenticOrchestrator+MagenticAgentExecutor+MagenticOrchestratorEvent+MagenticPlanReviewRequest+MagenticPlanReviewResponse (Magentic deeper — outer/inner loop, reset signal, plan signoff HITL, progress ledger serialisation), HttpRequestHandler+DefaultHttpRequestHandler+MCPToolHandler+DefaultMCPToolHandler+ExternalInputRequest+ToolApprovalRequest (declarative handlers — SSRF guard, MCP discovery, tool approval gate, agent HITL) — source-verified at 1.8.1 · 85 min" />
  <LinkCard title="36 · Class deep dives (Vol. 14)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v14/" description="State (superstep caching — set/get/commit/discard/export_state/import_state, delete sentinel, checkpoint integration), OutputDesignation (output routing — is_terminal/is_intermediate/classify, compatibility mode vs explicit output_from, intermediate_output_from), MessageType enum + WorkflowMessage (STANDARD vs RESPONSE types, plural trace_contexts for fan-in OTel linking, to_dict/from_dict round-trip), DictConvertible + encode_value (dict/JSON round-trip mixin — from_dict, clone deep/shallow, to_json/from_json, recursive encoding), MiddlewareWrapper + BaseMiddlewarePipeline (callable→middleware adapter, has_middlewares gate, auto-wrapping in _register_middleware_with_wrapper), AgentMiddlewarePipeline + ChatMiddlewarePipeline + FunctionMiddlewarePipeline (concrete pipeline execute() — streaming support, matches() cache key, MiddlewareTermination short-circuit), MiddlewareDict + categorize_middleware (TypedDict bag — mixed-type list sorting, callable type inference, multiple source merge), FunctionRequestResult TypedDict (action return/continue/stop, errors_in_a_row, result_message, function_call_count — tool-loop integration), OtelAttr + MessageListTimestampFilter (GenAI semantic convention enum — all 35 attribute names; logging filter for 1µs timestamp offsets in structured logs), PolicyEnforcementFunctionMiddleware + ConfidentialityLabel + ContentVariableStore + VariableReferenceContent + LabeledMessage + InspectVariableInput (prompt injection IFC defence — variable indirection store, confidentiality PUBLIC/PRIVATE/USER_IDENTITY, block vs approval-on-violation modes, SecureAgentConfig one-liner setup) — source-verified at 1.8.1 · 90 min" />
  <LinkCard title="37 · Class deep dives (Vol. 15)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v15/" description="AGUIChatClient+AGUIEventConverter+AGUIHttpService (AG-UI streaming stateful UI protocol — per-session converter, SSE delivery, stateless mode), AgentFrameworkAgent+AgentFrameworkWorkflow+state_update+add_agent_framework_fastapi_endpoint (FastAPI/Teams hosting — pending-approval registry, streaming SSE, CORS), ThreadItemConverter+simple_to_agent_input+stream_agent_response (ChatKit thread-item streaming — multimodal mapping, delta merging), DevServer+serve+register_cleanup (local OpenAI-compatible debug server — multi-agent, CORS security fix, graceful teardown), GAIA+GAIATelemetryConfig+TaskRunner+TaskResult+Prediction+Evaluation (GAIA benchmark lab — telemetry config, async runner, result aggregation), CopilotStudioAgent (Copilot Studio bridge — COPILOTSTUDIOAGENT__ env prefix, conversation turn streaming), AzureAISearchContextProvider (semantic/agentic retrieval — AZURE_SEARCH_ env prefix, top_k/semantic_configuration_name), CosmosHistoryProvider (Cosmos DB history — AZURE_COSMOS_ env prefix, store_context_messages, store_context_from), DurableAIAgentClient+DurableAIAgentWorker+DurableAIAgentOrchestrationContext+AgentCallbackContext (durable task hosting — get_agent().run(), yield Task, callback protocol), AgentFunctionApp (Azure Functions serverless — enable_http_endpoints, enable_mcp_tool_trigger, health check) — source-verified at 1.8.1 · 90 min" />
  <LinkCard title="38 · Class deep dives (Vol. 16)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v16/" description="FoundryAgent+FoundryAgentOptions+RawFoundryAgent+RawFoundryAgentChatClient (hosted Azure AI Foundry agents — PromptAgent/HostedAgent modes, isolation_key per-session, allow_preview, client_type override), FoundryLocalClient+FoundryLocalChatOptions (on-device LLM via Foundry Local — bootstrap, prepare_model, device NPU/GPU/CPU, structured output), FoundryMemoryProvider (Foundry Memory Store semantic memory — memory_store_name, scope, update_delay, before/after_run lifecycle), FoundryEvals+GeneratedEvaluatorRef+evaluate_foundry_target (Foundry cloud eval harness — 19 built-in evaluator name constants, smart defaults, tool_call_accuracy auto-add), BedrockChatClient+BedrockChatOptions+BedrockGuardrailConfig+BedrockSettings (Bedrock Converse API — BEDROCK_REGION/BEDROCK_CHAT_MODEL env vars, guardrail sync/async streaming, unsupported-options table), BedrockEmbeddingClient+BedrockEmbeddingOptions+BedrockEmbeddingSettings (Bedrock Titan embeddings — BEDROCK_EMBEDDING_MODEL, normalize flag, MemoryContextProvider wiring), MagenticManagerBase (custom Magentic manager authoring — 4 abstract methods: plan/replan/create_progress_ledger/prepare_final_answer, checkpoint save/restore hooks), BaseGroupChatOrchestrator+GroupChatRequestSentEvent+GroupChatResponseReceivedEvent (group chat base class — max_rounds coercion, 4 @handler overloads, event dataclasses for observability), AgentRequestInfoResponse+CacheProvider (HITL approval model — approve()/from_messages()/from_strings() factories; CacheProvider Protocol for Purview integration), Purview exception hierarchy+acquire_token (PurviewAuthenticationError/PaymentRequiredError/RateLimitError/RequestError/ServiceError hierarchy; MSAL silent-then-interactive token flow) — source-verified at 1.8.1 · 95 min" />
  <LinkCard title="39 · Class deep dives (Vol. 17)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v17/" description="ToolApprovalMiddleware+ToolApprovalRule+ToolApprovalState (interactive tool approval gates — standing rules, queue drain, argument matching), AgentLoopMiddleware+JudgeVerdict+todos_remaining+background_tasks_running (LLM-judged self-improvement loops — fresh_context snapshot, with_judge, return_final_only), SamplingApprovalCallback+MCP sampling security (confused-deputy mitigation — deny-by-default, token cap, async callback), to_prompt_agent (Foundry PromptAgent publisher — RawFoundryChatClient required, model resolution, local MCP raises ValueError), FoundryEmbeddingClient+FoundryEmbeddingOptions+FoundryEmbeddingSettings+RawFoundryEmbeddingClient (multimodal text+image embeddings — mixed batch split/reassemble, image_model precedence), ContentUnderstandingContextProvider+AnalysisSection+DocumentStatus (Azure Content Understanding — auto-detect analyzer, deferred analysis, auto-registered list_documents tool), FileSearchConfig+FileSearchBackend+OpenAIFileSearchBackend+FoundryFileSearchBackend (vector store upload/delete backends — create_and_poll, custom backend), AgentFrameworkTracer (Agent Lightning OTel bridge — OBSERVABILITY_SETTINGS singleton, init/teardown lifecycle), TaskRunner+patch_env_set_state+unpatch_env_set_state (Tau2 airline benchmark harness — three-part conversation assembly, reinit() chaining, evaluate reward), 8 new FoundryChatClient hosted tool factories (get_mcp_tool, get_azure_ai_search_tool, get_sharepoint_tool, get_fabric_tool, get_memory_search_tool, get_computer_use_tool, get_browser_automation_tool, get_a2a_tool) — first volume source-verified at 1.9.0 · 100 min" />
  <LinkCard title="40 · Class deep dives (Vol. 18)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v18/" description="Skill+SkillFrontmatter+SkillScriptRunner (Agent Skills spec — name/description/compatibility validation, file-script runner protocol), InlineSkill (@skill.resource/@skill.script decorators — XML content synthesis, content caching), InMemorySkillsSource+AggregatingSkillsSource+FilteringSkillsSource+DeduplicatingSkillsSource (skills pipeline — fan-in, predicate filter, first-one-wins dedup), AgentFileStore+InMemoryAgentFileStore+FileSearchMatch+FileSearchResult (file storage ABC + in-memory backend — exclusive-create, async lock, __slots__ DTOs, custom Azure Blob example), FileAccessProvider (6-tool file workspace — save/read/delete/list/search, require_delete_approval gate, shared across sessions), BackgroundAgentsProvider+BackgroundTaskInfo+BackgroundTaskStatus (background sub-agent delegation — asyncio.Task fan-out, continue_task session resume, LOST status on process restart), MemoryStore+MemoryTopicRecord+MemoryIndexEntry (durable memory backing store ABC — Markdown serialization, slug normalization, pointer line rendering, custom dict-backed store example), WorkflowGraphValidator+EdgeDuplicationError+GraphConnectivityError+TypeCompatibilityError (7-check pre-build validation — DFS reachability, self-loop warnings, FanIn list-wrapping type check), MagenticBuilder+MagenticManagerBase+MagenticProgressLedger+MagenticProgressLedgerItem (Magentic One internals — 5-item progress ledger, abstract plan/replan/create_progress_ledger/prepare_final_answer, custom manager example), LocalEvaluator+EvalItem+EvalScoreResult+ConversationSplit+ExpectedToolCall (local evaluation framework — per_turn_items, LAST_TURN/FULL splits, @evaluator custom checks, evaluate_agent end-to-end) — source-verified at 1.9.0 · 95 min" />
  <LinkCard title="41 · Class deep dives (Vol. 19)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v19/" description="ConcurrentBuilder (fan-out/fan-in — with_aggregator callback/Executor, output_from, intermediate_output_from, with_request_info, checkpoint), SequentialBuilder (shared-conversation chains — chain_only_agent_responses, output_from, with_request_info per-agent, checkpoint), HandoffBuilder+HandoffConfiguration+HandoffSentEvent (decentralised handoff — add_handoff routing graph, with_start_agent, with_autonomous_mode per-agent turn limits, output_from), HandoffAgentUserRequest (HITL factories — create_response str/Message, terminate), OrchestrationState (unified checkpoint dataclass — conversation+round_index+metadata+task, to_dict/from_dict, pattern-specific metadata extension), AgentModeProvider+get_agent_mode+set_agent_mode (plan/execute mode harness — mode_descriptions Mapping, {available_modes}/{current_mode} template, default_mode resolution, external-change notification message), TodoItem+TodoInput+TodoCompleteInput (todo DTO primitives — SerializationMixin round-trip, stripping/validation rules, __eq__/__repr__), TodoStore+TodoSessionStore+TodoFileStore (backing store ABC — session state storage, file-backed JSON with path-traversal guard, Windows reserved name encoding, custom store example), TodoProvider (5-tool harness — todos_add/complete/remove/get_remaining/get_all, per-session asyncio.Lock via WeakKeyDictionary, custom store injection, combined with AgentModeProvider), MagenticResetSignal+StandardMagenticManager (stall-recovery sentinel + fully-customisable LLM manager — 7 prompt overrides, max_stall/reset/round_count, progress_ledger_retry_count, observe REPLANNED events) — source-verified at 1.9.0 · 100 min" />
  <LinkCard title="42 · Class deep dives (Vol. 20)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v20/" description="SupportsCodeInterpreterTool+SupportsWebSearchTool+SupportsImageGenerationTool+SupportsMCPTool+SupportsFileSearchTool+SupportsShellTool (6 @runtime_checkable hosted-tool Protocols — provider-agnostic isinstance guard + factory call), SupportsGetEmbeddings (typed embedding duck-typing Protocol — get_embeddings signature, Generic contravariant input), ReleaseCandidateFeature+FeatureStageWarning+ExperimentalWarning (feature staging system — deduplication via _WARNED_FEATURES, single-line formatter, CI error filter), EmbeddingGenerationOptions+Embedding+GeneratedEmbeddings (embedding DTO layer — TypedDict options, Generic vector Embedding, list[Embedding] subclass with usage), WorkflowEventSource (EXECUTOR vs FRAMEWORK origin str-Enum), SubWorkflowRequestMessage+SubWorkflowResponseMessage (hierarchical workflow HITL bridge — create_response type validation, executor_id multi-instance routing), RequestInfoMixin+response_handler decorator (executor-level HITL wiring — introspection vs explicit type modes, forward references, duplicate-handler guard), WorkflowAgent.RequestInfoFunctionArgs (HITL tool-call serialization dataclass — to_dict/from_dict, REQUEST_INFO_FUNCTION_NAME sentinel), EdgeGroupDeliveryStatus (OTel edge-delivery status enum — 6 values: DELIVERED/BUFFERED/DROPPED_TYPE_MISMATCH/DROPPED_CONDITION_FALSE/DROPPED_TARGET_MISMATCH/EXCEPTION), IntegrityLabel+LabelTrackingFunctionMiddleware (IFC security experimental — TRUSTED/UNTRUSTED labels, 3-tier propagation priority table, variable indirection auto-hide, get_security_tools, reset_context_label), MiddlewareTermination+WorkflowConvergenceException (control-flow short-circuit with result payload + runner max-iteration exceeded) — source-verified at 1.9.0 · 105 min" />
  <LinkCard title="43 · Class deep dives (Vol. 21)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v21/" description="WorkflowContext[OutT,W_OutT] (per-executor execution context — send_message, yield_output, request_info, state R/W, source_executor_ids, is_streaming), FanInEdgeGroup+FanOutEdgeGroup (converging/broadcasting edge groups — min-source/target validation, selection_func dynamic routing), SwitchCaseEdgeGroup+SwitchCaseEdgeGroupCase+SwitchCaseEdgeGroupDefault (switch/case routing — sequential predicate evaluation, exactly-one-default invariant), TokenBudgetComposedStrategy (multi-strategy composition — early_stop, annotate_message_groups pipeline, strict-budget double-fallback), SelectiveToolCallCompactionStrategy+ToolResultCompactionStrategy (tool history reduction — exclude vs inline summary, keep_last_tool_call_groups), SlidingWindowStrategy+SummarizationStrategy (sliding window + LLM-backed summarization — preserve_system, target_count+threshold, bidirectional trace metadata), StepWrapper+FunctionalWorkflow+RunContext (functional workflow internals — caching by call_index, executor_bypassed on cache hit, checkpoint per step), MCPWebsocketTool+MCPStreamableHTTPTool (MCP network transports — WebSocket wss://, streamable HTTP/SSE with header_provider ContextVar injection), MCPTaskOptions (SEP-2663 long-running task lifecycle — default_ttl, max_task_wait, cancel_remote_task_on_local_cancellation), AgentResponseUpdate+ChatResponseUpdate+ContinuationToken (streaming chunk types — .text property, continuation_token resumption, author_name, finish_reason) — source-verified at 1.9.0 · 110 min" />
  <LinkCard title="44 · Class deep dives (Vol. 22)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v22/" description="AgentFrameworkException hierarchy (4-branch tree: Agent/ChatClient/Integration/Content + Tool/Workflow/Settings families — inner_exception, log_level, sticky-disable pattern, UserInputRequiredException.contents), WorkflowInterrupted+get_run_context() (BaseException HITL signal that bypasses except Exception — ContextVar retrieval from any @step without RunContext param), enable_instrumentation()/disable_instrumentation()/enable_sensitive_telemetry() (sticky disable, force-override, OBSERVABILITY_SETTINGS singleton), get_tracer()/get_meter()/create_mcp_client_span()/create_workflow_span() (OTel helper functions for custom spans and metrics), WorkflowState (PowerFx-backed variable store — Workflow.Inputs/Outputs, Local, System, Agent namespaces, eval() expression engine, set_agent_result()), HttpRequestInfo+HttpRequestResult+HttpRequestHandler+DefaultHttpRequestHandler (declarative HTTP action layer — 3 construction modes, SSRF warning, timeout_ms/connection_name, multi-value header preservation), ExternalInputRequest+ExternalInputResponse+AgentExternalInputRequest+AgentExternalInputResponse (declarative HITL pause/resume — request_id correlation, Yield/Resume, typed value for confirmations, function_results injection), MCPToolInvocation+MCPToolResult+MCPToolApprovalRequest+DefaultMCPToolHandler (declarative MCP dispatch — header_names security, error vs raise contract, allowlist pattern), combine_labels()+check_confidentiality_allowed()+store_untrusted_content() (label algebra — UNTRUSTED wins, PUBLIC/PRIVATE/USER_IDENTITY hierarchy, auto-variable-ID, VariableReferenceContent), quarantined_llm()+inspect_variable()+get_security_tools()+SECURITY_TOOL_INSTRUCTIONS (isolated LLM calls over stored variables, audit-log inspection, one-call tool wiring, inline instruction constant) — source-verified at 1.9.0 · 115 min" />
  <LinkCard title="45 · Class deep dives (Vol. 23)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v23/" description="DeclarativeActionExecutor+DeclarativeWorkflowState+DeclarativeEnvConfig+discover_env_references() (PowerFx graph-node base layer — Env allowlist, dot-path get/set, eval() expression engine, PowerFx-safe type coercion), ConditionGroupEvaluatorExecutor+IfConditionEvaluatorExecutor+ConditionResult (declarative branching — first-match semantics, ELSE_BRANCH_INDEX=-1, branch_index edge routing), ForeachInitExecutor+ForeachNextExecutor+BreakLoopExecutor+ContinueLoopExecutor+LoopIterationResult+LoopControl (declarative loop nodes — LOOP_STATE_KEY in State, current_item/current_index, break/continue signals), SetValueExecutor+SetVariableExecutor+CreateConversationExecutor+SetMultipleVariablesExecutor+SendActivityExecutor+ParseValueExecutor (declarative variable and messaging graph nodes — _get_variable_path helper, UUID conversation init), AgentManifest+AgentDefinition+PromptAgent+EnvironmentVariable+agent_schema_dispatch() (declarative schema types — template dispatch, parameters PropertySchema, resources list, PowerFx-evaluated string fields), Property+ArrayProperty+ObjectProperty+PropertySchema (schema type dispatch — from_dict type→kind rename, to_json_schema() required-array conversion), Connection+ReferenceConnection+RemoteConnection+ApiKeyConnection+AnonymousConnection (connection kind dispatch — apiKey log_value=False, PowerFx-safe endpoint eval), McpTool+McpServerApprovalMode hierarchy (MCP tool approval gating — string shortcut always/never, specify with allowlist/denylist), Template+Format+Parser+Model+ModelOptions (declarative model config — additionalProperties **kwargs merge, Format.strict, Parser.options), InvokeAzureAgentExecutor+AgentResult+ExternalLoopState+AgentExternalInputRequest+AgentExternalInputResponse+_extract_json_from_response() (Azure AI Foundry agent invocation with HITL external-loop — Yield/Resume, last-JSON extraction from streaming responses) — source-verified at agent-framework-declarative 1.0.0rc2 · 120 min" />
  <LinkCard title="46 · Class deep dives (Vol. 24)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v24/" description="is_type_compatible+serialize_type+deserialize_type+normalize_type_to_list+resolve_type_annotation+try_coerce_to_type+is_instance_of+is_chat_agent (workflow edge type-validation engine — 7-case compatibility algorithm, Union splitting, forward-reference eval, dict→Pydantic coercion), encode_checkpoint_value+decode_checkpoint_value (checkpoint serde — JSON-native pass-through, recursive collection encoding, pickle+base64 for custom types, allowed_types security allowlist), Runner+create_edge_runner (Pregel superstep runner — run_until_convergence loop, 50 ms event polling, convergence detection, EdgeGroup→EdgeRunner factory dispatch), ShouldContinueCallable+ShouldContinueResult+FeedbackCallable+NextMessageCallable+JUDGE constants (AgentLoopMiddleware callable types — keyword injection table, DEFAULT_JUDGE_INSTRUCTIONS+CRITERIA_PLACEHOLDER template, FeedbackCallable progress log), ToolApprovalScope+ToolApprovalRuleCallback+ALWAYS_APPROVE constants+create_always_approve helpers (approval protocol internals — Literal['tool','tool_with_arguments'] scope, DEFAULT_TOOL_APPROVAL_SOURCE_ID session key, pre-approval callback), GroupChatRequestMessage+ORCH_MSG_KIND constants+clean_conversation_for_handoff+create_completion_message (orchestration protocol utilities — request envelope, kind classification strings, text-only handoff cleaning, standardised completion), MagenticOrchestratorEvent+MagenticOrchestratorEventType+MagenticAgentExecutor (Magentic event observability — PLAN_CREATED/REPLANNED/PROGRESS_LEDGER_UPDATED enum, reset-aware executor with handle_magentic_reset), evaluate_traces+RawFoundryEmbeddingClient (Foundry trace-based evaluation — 3 input modes, poll_interval/timeout; multimodal text+image batch split/reassemble), RawOpenAIChatCompletionClient (raw Chat Completions — layer ordering contract: FunctionInvocationLayer→ChatMiddlewareLayer→ChatTelemetryLayer, INJECTABLE async_client), normalize_messages_input+ensure_author+latest_user_message (workflow message utilities — heterogeneous AgentRunInputs normalisation, author fallback, reverse-scan user message) — source-verified at 1.9.0 · 125 min" />
  <LinkCard title="47 · Class deep dives (Vol. 25)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v25/" description="FunctionTool+OpenApiTool+WebSearchTool+FileSearchTool+CodeInterpreterTool+Binding; AgentFactory+DeclarativeLoaderError+ProviderLookupError+ProviderTypeMapping; WorkflowFactory+DeclarativeWorkflowBuilder; QuestionExecutor+RequestExternalInputExecutor; HttpRequestActionExecutor; InvokeMcpToolActionExecutor+MCPToolApprovalRequest; BaseToolExecutor+InvokeFunctionToolExecutor+ToolApprovalRequest+ToolApprovalResponse+ToolInvocationResult; JoinExecutor+EndWorkflowExecutor+CancelDialogExecutor; ActionComplete+ActionTrigger+DeclarativeStateData+ConversationData; ClearAllVariablesExecutor+EditTableExecutor+ResetVariableExecutor+SetTextVariableExecutor — source-verified at declarative 1.0.0rc2 / agent-framework 1.9.0 · 130 min" />
  <LinkCard title="48 · Class deep dives (Vol. 26)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v26/" description="DurableAIAgentWorker+DurableAIAgentClient, DurableAIAgent+DurableAgentExecutor, DurableAIAgentOrchestrationContext, AgentEntity+AgentEntityStateProviderMixin, AgentCallbackContext+AgentResponseCallbackProtocol, RunRequest, AgentSessionId+DurableAgentSession, DurableAgentState+DurableAgentStateData, entry+usage hierarchy, content hierarchy+DurableStateFields+ContentTypes — 10 class groups source-verified at agent-framework-durabletask 1.0.0b260521 · 135 min" />
  <LinkCard title="49 · Class deep dives (Vol. 27)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v27/" description="ContentLabel + combine_labels + check_confidentiality_allowed (IFC label algebra — string coercion, priority table, context ≤ allowed check), store_untrusted_content + get_security_tools + security tool constructors (FIDES tool wiring — approval_mode=never_require, audit log), enable/disable_instrumentation + enable_sensitive_telemetry (sticky OTel lifecycle — _user_disabled flag, force=True override), create_resource + create_metric_views + histogram boundary constants (OTel provider setup — 14-bucket TOKEN_USAGE + OPERATION_DURATION boundaries), get_tracer + get_meter + INNER_ACCUMULATED_USAGE context vars (span helpers + inner-span dedup), create_mcp_client_span + set_mcp_span_error (MCP telemetry — {method} {target} span naming), group_messages + annotate_message_groups (compaction grouping — 4 kinds, suffix-only re-annotation), apply_compaction + project_included_messages + included_messages + annotate_token_counts (compaction pipeline internals), normalize_messages + detect_media_type_from_base64 + merge_chat_options + prepend_instructions_to_messages (message utilities — magic-bytes MIME detection, \\n concat), normalize_tools + validate_chat_options + map_chat_to_agent_update + add_usage_details (validation + conversion — async validate, raw_representation, int-only usage) — source-verified at 1.9.0 · 140 min" />
  <LinkCard title="50 · Class deep dives (Vol. 28)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v28/" description="RawClaudeAgent+ClaudeAgent+ClaudeAgentOptions+ClaudeAgentSettings (Claude Code CLI integration — AGENT_PROVIDER_NAME='anthropic.claude', owns-client lifecycle, PermissionMode literals, env-prefix resolution), AgentFrameworkAgent+AgentConfig (AG-UI wrapper for any SupportsAgentRun — state_schema Pydantic dispatch, predict_state_config, require_confirmation gate, snapshot_store), AGUIThreadSnapshot+AGUIThreadSnapshotStore+InMemoryAGUIThreadSnapshotStore (replayable thread state — messages+state+interrupt triad, max_snapshots LRU eviction, scope+thread_id composite key), PredictiveStateHandler+PredictStateConfig (streaming tool-call state prediction — config dict, streaming_tool_args accumulator, last_emitted_state dedup guard), AGUIRequest+AGUIChatOptions+AgentState+RunMetadata (AG-UI protocol types — AliasChoices camelCase aliases, client-side tools forwarding, thread_id continuity), A2AAgentSession+A2AContinuationToken (A2A session management — context_id/task_id/task_state slot keys, service_session_id mapping, TaskState continuation detection), ThreadItemConverter (ChatKit thread bridge — attachment_data_fetcher async callback, to_agent_input() normalisation), AgentApprovalExecutor+AgentRequestInfoExecutor+AgentRequestInfoResponse (approval gate — allow_direct_output terminal mode, @handler/@response_handler pair, approve() shortcut), AgentFrameworkWorkflow (workflow-to-AG-UI bridge — workflow vs workflow_factory mutex, (scope,thread_id) cache key), FlowState+AGUIHttpService+run_workflow_stream (run-level streaming state, SSE parsing, thread/run-id resolution) — source-verified at 1.9.0 / agent-framework-claude 1.0.0b260609 / agent-framework-ag-ui 1.0.0rc5 · 145 min" />
  <LinkCard title="51 · Class deep dives (Vol. 29)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v29/" description="SecureMCPToolProxy+apply_mcp_security_labels (FIDES MCP security proxy — wrap-or-URL init, auto-label on connect, annotation_overrides, mark_write_tools_as_sinks, refresh_labels()), BedrockGuardrailConfig+BedrockChatOptions.guardrailConfig (Bedrock content safety — guardrailIdentifier+guardrailVersion+trace+streamProcessingMode), GroupChatRequestMessage+GroupChatRequestSentEvent+GroupChatResponseReceivedEvent (observable group-chat events — additional_instruction envelope, round_index+participant_name), MagenticOrchestratorEvent+MagenticOrchestratorEventType+MagenticProgressLedgerItem (PLAN_CREATED/REPLANNED/PROGRESS_LEDGER_UPDATED — Message|MagenticProgressLedger content union, reason+answer str|bool), ReleaseCandidateFeature+ExperimentalWarning+FeatureStageWarning (feature staging — RC empty in 1.10.0, ExperimentalWarning→FeatureStageWarning→FutureWarning), ToolExecutionException+_MCPTaskAbandoned+_MCPDeadlineExpired (MCP error taxonomy — ToolException→ToolExecutionException→_MCPTaskAbandoned chain, internal _MCPDeadlineExpired sentinel), AgentMiddlewareLayer+ChatMiddlewareLayer (MRO mixin layers — run()/get_response() intercept, pipeline bypass when empty, ResponseStream.from_awaitable streaming), DiscoveryResponse+EntityInfo+AgentFrameworkRequest+OpenAIError (DevUI protocol — 18-field EntityInfo, model-as-entity_id routing, OpenAIError.create() factory), GroupChatState+AgentOrchestrationOutput+create_completion_message+clean_conversation_for_handoff (orchestration utilities — frozen dataclass, strict Pydantic output, text-only handoff cleaning), RawGitHubCopilotAgent+GitHubCopilotSettings+GitHubCopilotOptions (GitHub Copilot CLI agent — GITHUB_COPILOT_* env prefix, on_pre_tool_use/on_permission_request hooks, BYOK provider config) — source-verified at 1.10.0 / agent-framework-ag-ui 1.0.0rc7 · 150 min" />
  <LinkCard title="52 · Class deep dives (Vol. 30)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v30/" description="WorkflowViz (to_digraph/to_mermaid/export — include_internal_executors, DOT+Mermaid+SVG/PNG/PDF outputs, Jupyter rendering via save_svg+IPython.display), FunctionalWorkflow+FunctionalWorkflowAgent (@workflow decorator — streaming run() overloads, checkpoint_storage= per-run override, HITL resume via responses=+checkpoint_id=, agent-compatible adapter), StepWrapper+RunContext (step caching by call_index — executor_bypassed on cache hit, RunContext.request_info() HITL suspension, get_state/set_state KV, add_event custom events), CompactionProvider+ContextWindowCompactionStrategy (two-phase compaction — before_strategy/after_strategy, DEFAULT_TOOL_EVICTION_THRESHOLD=0.5/DEFAULT_TRUNCATION_THRESHOLD=0.8, keep_last_tool_call_groups), SlidingWindowStrategy+SelectiveToolCallCompactionStrategy+CharacterEstimatorTokenizer (targeted compaction — keep_last_groups+preserve_system, keep_last_tool_call_groups=0 removes all, 4-char/token heuristic), AGUIChatClient+AGUIEventConverter (AG-UI chat integration — multi-layer MRO FunctionInvocationLayer+ChatMiddlewareLayer+ChatTelemetryLayer, thread_id continuity, convert_event 10+ event-type dispatch), ClassSkill+AggregatingSkillsSource+FilteringSkillsSource+DelegatingSkillsSource (skills composition — @ClassSkill.resource/@ClassSkill.script decorators, fan-in+predicate+custom decorator pattern), BackgroundAgentsProvider+BackgroundTaskInfo+BackgroundTaskStatus (background task delegation — 6 tools to LLM, continue_task resume, RUNNING/COMPLETED/FAILED/LOST enum), TodoItem+TodoFileStore+TodoInput+TodoCompleteInput (structured todo tracking — TodoFileStore JSON persistence, SessionContext source_id, TodoProvider toolset), AgentEvalConverter+ConversationSplitter+EvalResults+EvalItem+CheckResult (evaluation pipeline — convert_message typed content, ConversationSplitter callable protocol + ConversationSplit.LAST_TURN built-in strategy, EvalResults sub_results per-agent breakdown, EvalItem query/response properties) — source-verified at agent-framework 1.10.0 · 155 min" />
  <LinkCard title="53 · Class deep dives (Vol. 31)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v31/" description="FileMemoryProvider; create_harness_agent; AgentLoopMiddleware advanced patterns; ToolApprovalMiddleware+ToolApprovalRule+ToolApprovalState; FileSkillsSource depth+filter predicates; FoundryAgent; AgentExecutorResponse.with_text(); todos_remaining+background_tasks_running; custom ContextProvider+SessionContext.metadata; InMemoryCheckpointStorage — source-verified at agent-framework 1.10.0 · 155 min" />
  <LinkCard title="54 · Class deep dives (Vol. 32)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v32/" description="evaluate_agent+evaluate_workflow+LocalEvaluator+EvalItem; EvalResults+EvalItemResult+EvalScoreResult+ConversationSplit+EvalNotPassedError; keyword_check+tool_called_check+tool_calls_present+tool_call_args_match+@evaluator; WorkflowEvent complete factory catalog (12 methods+AGENT_FORWARDED_EVENT_TYPES); WorkflowErrorDetails+WorkflowRunState+WorkflowEventSource; compaction annotation constants+included_messages+included_token_count; background_tasks_running_message+todos_remaining_message; ToolApprovalRuleCallback+create_always_approve helpers; FileStoreEntry+DEFAULT_FILE_ACCESS constants; BackgroundAgentsProvider+BackgroundTaskInfo deep patterns — source-verified at agent-framework 1.10.0 · 155 min" />
  <LinkCard title="55 · Class deep dives (Vol. 33)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v33/" description="WorkflowBuilder 1.10.0 new API (add_chain/add_multi_selection_edge_group/intermediate_output_from); Workflow+WorkflowRunResult full result API; FunctionTool advanced (max_invocations/declaration-only/result_parser/input_model); Agent.run() structured output+streaming+per-call overrides; MCPStdioTool advanced (additional_tool_argument_names/MCPSpecificApproval); SecretString+load_settings (env prefix/.env/required_fields tuple); FunctionInvocationConfiguration tool-loop control; FileHistoryProvider+custom HistoryProvider; AgentMiddleware+@agent_middleware+MiddlewareTermination; SkillsProvider+custom SkillsSource+DeduplicatingSkillsSource — source-verified at agent-framework 1.10.0 · 160 min" />
  <LinkCard title="56 · Class deep dives (Vol. 34)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v34/" description="OpenAIChatCompletionOptions+Prediction+PredictionTextContent+RawOpenAIChatCompletionClient+OpenAIChatCompletionClient (Chat Completions API stateless layer); ReasoningOptions+StreamOptions+OpenAIContinuationToken (reasoning effort+streaming controls); OpenAIContentFilterException+ContentFilterResult+ContentFilterResultSeverity+ContentFilterCodes (Azure content filter error hierarchy); WorkflowOrchestrationContext (host-agnostic workflow orchestration Protocol); DurableWorkflowClient (external durable workflow client — start/stream/HITL); WorkflowRegistrationPlan+plan_workflow_registration (durable task registration helpers); TaskType+TaskMetadata+PendingHITLRequest+ExecutorResult (durable orchestrator internals); DurableTaskWorkflowContext (durabletask-native orchestration context); ConversationStore+InMemoryConversationStore+CheckpointConversationManager (DevUI conversation store abstraction); AgentTask+PreCompletedTask+AzureFunctionsAgentExecutor (Azure Functions agent orchestration) — source-verified at agent-framework 1.10.0 · 165 min" />
  <LinkCard title="57 · Class deep dives (Vol. 35)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v35/" description="RawAgent (low-latency agent without middleware/telemetry — Generic TOptions IDE autocomplete, streaming overload); ChatContext (chat middleware pipeline context — messages/options/metadata/result mutation, PII redaction via stream_transform_hooks, caching short-circuit); SwitchCaseEdgeGroup+SwitchCaseEdgeGroupCase+SwitchCaseEdgeGroupDefault (conditional workflow fan-out — ordered case evaluation, exactly-one-default validation, to_dict serialization); WorkflowContext (per-executor context — send_message/yield_output, OTel PRODUCER spans, fan-in source_executor_ids); AgentLoopMiddleware+JudgeVerdict (iterative agent loop — should_continue predicate, fresh_context snapshots, with_judge LLM judge factory, DEFAULT_MAX_ITERATIONS=10/DEFAULT_JUDGE_MAX_ITERATIONS=5); MemoryStore+MemoryFileStore (abstract + file-backed durable memory — list/get/write/delete topics, rebuild_index, owner path-traversal guard, export_provider_state/import_provider_state); MemoryContextProvider (memory-backed history — recent_turns, selection_limit, consolidation_interval, per-event-loop async locks); AgentModeProvider (plan/execute mode harness — mode_set/mode_get tools, {available_modes}/{current_mode} template, external set_agent_mode notification); LocalEvaluator+EvalItem+EvalResults (local eval pipeline — variadic EvalCheck callables, asyncio.gather parallel, per-evaluator result_counts breakdown) — source-verified at agent-framework 1.10.0 · 170 min" />
  <LinkCard title="58 · Class deep dives (Vol. 36)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v36/" description="FileMemoryProvider (7-tool file-scoped agent memory — scope= for cross-session user memory, asyncio.Lock index guard); MessageInjectionMiddleware (async message injection from tool code — drains session-state queue into next model call); ToolApprovalMiddleware+ToolApprovalRule+ToolApprovalState (standing approval rules, queued requests, auto-approval callbacks); BackgroundAgentsProvider+BackgroundTaskInfo+BackgroundTaskStatus (asyncio.Task fan-out, continue_task resume, LOST on process restart); FunctionalWorkflow+FunctionalWorkflowAgent+RunContext+WorkflowInterrupted (@step caches by call_index, HITL via ctx.request_info() raises WorkflowInterrupted on first pass and returns response on replay); CachingSkillsSource+DeduplicatingSkillsSource+FilteringSkillsSource+AggregatingSkillsSource (composable skills pipeline decorators); ObservabilitySettings+AgentTelemetryLayer+EdgeGroupDeliveryStatus (sticky-disable OTel, VS Code extension port, sensitive telemetry opt-in); SecureAgentConfig+ContentLabel+LabeledMessage+IntegrityLabel+ConfidentialityLabel (IFC prompt-injection defence — variable indirection, block vs approval-on-violation modes); TodoProvider+TodoSessionStore+TodoFileStore+TodoItem (5-tool session-scoped todo harness, WeakKeyDictionary per-session locks, atomic JSON writes); WorkflowGraphValidator+EdgeDuplicationError+GraphConnectivityError+TypeCompatibilityError (7-check pre-build validation, DFS reachability, FanIn list-wrapping type check) — first volume source-verified at agent-framework 1.11.0 · 175 min" />
  <LinkCard title="59 · Class deep dives (Vol. 37)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v37/" description="SummarizationStrategy+TokenBudgetComposedStrategy+ToolResultCompactionStrategy+TruncationStrategy (four advanced compaction strategies — LLM-driven summarisation with security note, hard token-budget composition with ordered pipeline+early_stop, tool-result collapsing to [Tool results:…] summaries, oldest-first message/token truncation); EvalItemResult+EvalScoreResult+EvalNotPassedError+ExpectedToolCall+RubricScore (evaluation result types — per-item pass/fail/error status+is_passed/is_failed/is_error properties, per-dimension rubric breakdown, tool-call name+args assertion); FileSearchMatch+FileSearchResult+FileStoreEntry+InMemoryAgentFileStore+FileSystemAgentFileStore (file-access DTOs and store backends — 1-based line_number match, file/directory entry type guard, dict-backed in-memory store with exclusive-create lock, disk-backed store with lazy root creation and symlink rejection); MemoryContextProvider+MemoryIndexEntry+MemoryTopicRecord+MemoryStore+MemoryFileStore (durable topic memory harness — MEMORY.md pointer index, per-topic slug+deduped bullets, file-backed multi-owner store, recent_turns injection alongside durable memory); ContentVariableStore+InspectVariableInput+VariableReferenceContent (FIDES variable-indirection layer — var_{16hex} IDs, store/retrieve/exists/clear lifecycle, VariableReferenceContent prevents untrusted data entering LLM context); LabelTrackingFunctionMiddleware+PolicyEnforcementFunctionMiddleware (security label propagation — 3-tier label priority: embedded labels > source_integrity > input join, PolicyEnforcementFunctionMiddleware allow_untrusted_tools set, block vs approval_on_violation, audit_log); ChatTelemetryLayer+EmbeddingTelemetryLayer+OtelAttr+MessageListTimestampFilter (OTel telemetry mix-ins — OtelAttr span-attribute/span-name/metric constants (GenAI semantic-convention), token usage+duration histograms, embedding span tracing, 1µs log-timestamp stagger filter); WorkflowEvent+WorkflowErrorDetails+WorkflowEventSource+WorkflowRunState (workflow event system — 12 factory methods, from_exception() traceback capture, FRAMEWORK vs EXECUTOR origin, 7-state run lifecycle); SubWorkflowRequestMessage+SubWorkflowResponseMessage+ExecutionContext (sub-workflow HITL messaging — create_response() type-validates before wrapping, ExecutionContext pending_requests/collected_responses/expected_response_count); InMemorySkillsSource+SkillsSourceContext (in-memory skill serving — pre-built Skill list, frozen SkillsSourceContext agent+session context passed to every source/decorator) — source-verified at agent-framework 1.11.0 · 180 min" />
  <LinkCard title="60 · Class deep dives (Vol. 38)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v38/" description="AnthropicVertexClient+RawAnthropicVertexClient (Claude on Google Vertex AI — region/project_id/credentials wiring, middleware-free raw variant, OTEL_PROVIDER_NAME='google.vertex.ai'); GAIA+Task+Prediction+Evaluation+GAIATelemetryConfig (GAIA benchmark harness — custom TaskRunner+Evaluator protocols, concurrent task execution with semaphore, OTel file tracing, HuggingFace dataset download); AgentFrameworkException hierarchy (18-class exception taxonomy — inner_exception chaining, log_level suppression, UserInputRequiredException.contents propagation, WorkflowCheckpointException, MiddlewareException, IntegrationException tree); ExperimentalFeature+ReleaseCandidateFeature+FeatureStageWarning (feature-stage enums — 12 EXPERIMENTAL IDs including HARNESS/FIDES/FUNCTIONAL_WORKFLOWS, RC empty in 1.11.0, warnings.filterwarnings suppression, getattr guard); AgentSession (lightweight session state — session_id UUID auto-gen, service_session_id ServiceSessionId, state dict SerializationProtocol round-trip, to_dict/from_dict persistence); AgentResponse+AgentResponseUpdate (agent run result model — messages list, usage_details, finish_reason, value structured Pydantic output, from_updates streaming assembly, to_json/from_json serialisation); ChatContext (chat middleware pipeline context — messages/options/metadata mutation, result override, stream_transform_hooks PII redaction, caching short-circuit); ResponseStream (async stream wrapper — finalizer, transform_hooks per-update, result_hooks post-finalize, cleanup_hooks, get_final_response() on-demand, fake_stream_source pattern for testing); BaseEmbeddingClient+GeneratedEmbeddings+EmbeddingGenerationOptions (custom embedding backend — abstract get_embeddings, Embedding vector wrapper, usage dict, OTEL_PROVIDER_NAME hook, sentence-transformers adapter example); BaseChatClient (custom LLM provider — _inner_get_response streaming/non-streaming, get_response typed overloads, to_dict config serialisation, OpenAI-compatible self-hosted endpoint example) — source-verified at agent-framework 1.11.0 · 185 min" />
  <LinkCard title="61 · Class deep dives (Vol. 39)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v39/" description="CheckpointStorage+FileCheckpointStorage+WorkflowCheckpoint (checkpoint Protocol — 6 methods, hybrid JSON+pickle+base64 format, allowed_checkpoint_types security allowlist, previous_checkpoint_id chaining, graph_signature_hash compatibility guard); InProcRunnerContext (in-process workflow runner — lazy asyncio.Queue loop binding, send/drain messages, event streaming, add_request_info_event HITL correlation, set_runtime_checkpoint_storage per-run override); FunctionInvocationLayer (tool-execution loop MRO mixin — function_middleware pipeline, max_iterations/max_function_calls budget, per-call middleware injection, compaction_strategy/tokenizer overloads); AgentContext (agent middleware context object — agent/messages/session/tools/options/stream/metadata/result fields, stream_transform_hooks PII redaction, client_kwargs/function_invocation_kwargs passthrough); CopilotStudioAgent (Microsoft Copilot Studio bridge — COPILOTSTUDIOAGENT__ env prefix, PowerPlatformCloud/AgentType params, MSAL silent-then-interactive token flow, context_providers/middleware wiring); PurviewPolicyMiddleware+PurviewChatPolicyMiddleware+PurviewSettings (Microsoft Purview compliance — UPLOAD_TEXT pre-check, blocked_prompt_message/blocked_response_message, ignore_exceptions/ignore_payment_required, CacheProvider TTL, MiddlewareTermination short-circuit); DevServer (local OpenAI-compatible debug server — entities_dir scan, developer vs user mode, auth_enabled Bearer token, auto-generated loopback token, CORS allowlist, Host-header allowlist DNS rebinding guard); InMemoryHistoryProvider+FileHistoryProvider (history provider pair — skip_excluded compaction integration, store_context_messages/store_context_from scoping, 64-stripe threading.Lock file writes, Windows reserved name encoding, path-traversal guard); PerServiceCallHistoryPersistingMiddleware (per-model-call history persistence — service_stores_history flag, _prepare_service_call_context load path, _persist_service_call_response after each model call, sentinel conversation ID for local function loop); FunctionInvocationConfiguration+FunctionRequestResult+UserInputRequiredException (tool loop control TypedDict — max_iterations LLM roundtrips vs max_function_calls individual executions, terminate_on_unknown_calls, additional_tools hidden tools, FunctionRequestResult action/result_message/errors_in_a_row, UserInputRequiredException.contents propagation) — source-verified at agent-framework 1.11.0 · 190 min" />
  <LinkCard title="62 · Class deep dives (Vol. 40)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v40/" description="MagenticOrchestrator (two-level outer/inner loop — stall detection, require_plan_signoff HITL gate, reset_and_replan, MagenticOrchestratorEvent emission); MagenticContext+MagenticResetSignal (mutable orchestrator state dataclass — task/round_count/stall_count/reset_count, DictConvertible round-trip, clone(deep=True) before manager calls); MagenticPlanReviewRequest+MagenticPlanReviewResponse (HITL plan approval — approve()/revise() helpers, feedback message injection, iterative replanning loop); StandardMagenticManager+MagenticProgressLedger (LLM-driven orchestration — 8 prompt overrides, max_stall_count=3/max_reset_count/max_round_count/progress_ledger_retry_count, 5-field progress ledger is_request_satisfied/is_in_loop/is_progress_being_made/next_speaker/instruction_or_question); GroupChatOrchestrator (selection-function-driven group chat — selection_func protocol, max_rounds, termination_condition, round-robin and keyword-stop examples); ContentVariableStore+VariableReferenceContent (FIDES prompt-injection guard — store/retrieve/exists/list_variables API, var_{16hex} IDs, VariableReferenceContent prevents raw content entering LLM context); LabelTrackingFunctionMiddleware (3-tier integrity propagation — embedded > source_integrity declaration > input join, auto_hide_untrusted, get_context_label, get_variable_store); PolicyEnforcementFunctionMiddleware (pre-execution policy gate — allow_untrusted_tools whitelist, block_on_violation, audit_log, approval_on_violation HITL); FileMemoryProvider+FileStoreEntry (session-scoped file memory — 7 injected tools: write/read/delete/ls/grep/replace/replace_lines, scope= user grouping across sessions); AgentModeProvider (plan/execute mode switching — mode_descriptions, default_mode, mode_set/mode_get tools, get_agent_mode/set_agent_mode helpers, {available_modes}/{current_mode} template); GAIA+GAIATelemetryConfig+Task+TaskResult+Evaluator (GAIA benchmark harness — hf_token, file/OTLP tracing, custom Evaluator Protocol, run(task_runner, level, max_n, parallel) — TaskRunner adapter required, per-task TaskResult with runtime_seconds/error) — source-verified at agent-framework 1.11.0 · 195 min" />
  <LinkCard title="25 · azure-ai-agents integration — class reference Vol. 3" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v3/" description="Add-on: AsyncFunctionTool, AzureFunctionTool, AzureAISearchTool, VectorStore, ThreadRun, RunStep, ResponseFormatJsonSchema, TruncationObject, MessageAttachment, AsyncAgentEventHandler — source-verified · 45 min" />
  <LinkCard title="26 · azure-ai-agents integration — class reference Vol. 4" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v4/" description="Add-on: enable_auto_function_calls, create_thread_and_process_run, FunctionTool dynamic registration, CodeInterpreterTool file upload, FileSearchTool+VectorStore lifecycle, AzureAISearchTool query modes, BingGroundingTool params, ConnectedAgentTool multi-agent, custom AgentEventHandler, AsyncToolSet concurrent execution — source-verified · 50 min" />
  <LinkCard title="27 · azure-ai-agents integration — class reference Vol. 5" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v5/" description="Add-on: Agent model, AgentThread, ToolOutput, VectorStoreFileBatch, VectorStoreFile, FileInfo, MessageDeltaChunk (.text property), RunStepDeltaChunk (code-interpreter streaming), SubmitToolOutputsAction (full dispatch loop), AgentRunStream + all 8 AgentEventHandler hooks — source-verified · 55 min" />
  <LinkCard title="28 · azure-ai-agents integration — class reference Vol. 6" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v6/" description="Add-on: OpenAPI auth hierarchy (anonymous/managed-identity/connection), BingGroundingSearchConfiguration, RunCompletionUsage + RunStepCompletionUsage, VectorStoreDataSource + VectorStoreConfigurations, expiry policy + static/auto chunking, AgentsNamedToolChoice, file-search step results + FileSearchRankingOptions, streaming event taxonomy (AgentStreamEvent + 4 sub-enums), RequiredFunctionToolCall manual dispatch, multimodal ThreadMessageOptions (image file + URL) — source-verified · 60 min" />
</CardGrid>

## Jump to a topic

<CardGrid>
  <LinkCard title="Model providers (OpenAI, Azure, Foundry, Anthropic, Ollama, Bedrock)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_model_providers/" />
  <LinkCard title="Multi-agent orchestration (5 builders)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_orchestration/" />
  <LinkCard title="Middleware — agent / chat / function" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_middleware/" />
  <LinkCard title="Tools — @tool & FunctionTool" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_tools/" />
  <LinkCard title="MCP tools (stdio, streamable HTTP, websocket)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_mcp/" />
  <LinkCard title="Skills — progressive-disclosure knowledge" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_skills/" />
  <LinkCard title="Human in the loop — request_info & approval" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_hitl/" />
  <LinkCard title="Checkpointing — pause/resume workflows" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_checkpointing/" />
  <LinkCard title="Compaction — stay inside the context window" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_compaction/" />
  <LinkCard title="Sessions & history — AgentSession, FileHistoryProvider" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sessions/" />
  <LinkCard title="Observability & telemetry (OpenTelemetry)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_observability/" />
  <LinkCard title="Evaluation — evaluate_agent, LocalEvaluator" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_evaluation/" />
  <LinkCard title="Agent lifecycle" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_comprehensive_guide/#simple-agents" />
  <LinkCard title="Pydantic structured output" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_comprehensive_guide/#structured-output" />
  <LinkCard title="Graph workflows" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_2025_features/#graph-based-workflows---python" />
  <LinkCard title="Functional workflows (@workflow / @step)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_functional_workflows/" />
  <LinkCard title="A2A cross-framework" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_2025_features/#agent2agent-a2a-protocol---python" />
  <LinkCard title="Declarative YAML agents" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_2025_features/#declarative-agents-with-python" />
  <LinkCard title="Content safety" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_2025_features/#content-safety---python" />
  <LinkCard title="Async best practices" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_2025_features/#async-best-practices" />
  <LinkCard title="Class deep dives Vol. 1 (10 classes, source-verified)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives/" />
  <LinkCard title="Class deep dives Vol. 2 (10 more classes, source-verified)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v2/" />
  <LinkCard title="Class deep dives Vol. 3 (10 harness + compaction + viz + MCP classes, 1.7.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v3/" />
  <LinkCard title="Class deep dives Vol. 4 (Message/Content, chat layer, ResponseStream, AgentContext, FunctionalWorkflow, WorkflowEvent, SkillsSource, EvalItem, Tokenizer, ConversationSplit)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v4/" />
  <LinkCard title="Class deep dives Vol. 5 (Executor + @handler, AgentExecutor, edge groups, Runner, SessionContext, AgentSession, BaseChatClient, SecretString, WorkflowCheckpoint, Exceptions)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v5/" />
  <LinkCard title="Class deep dives Vol. 6 (ExperimentalFeature, WorkflowRunState, WorkflowExecutor, AgentResponse, BaseEmbeddingClient, FunctionInvocationConfiguration, ClassSkill, Annotation, capability protocols, middleware layers)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v6/" />
  <LinkCard title="Class deep dives Vol. 7 (ContextProvider, BackgroundTaskInfo, GroupChatBuilder, HandoffBuilder, MagenticBuilder, SequentialBuilder, ConcurrentBuilder, AgentFactory, WorkflowFactory, SecureAgentConfig, FunctionalWorkflowAgent, ObservabilitySettings)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v7/" />
  <LinkCard title="Class deep dives Vol. 8 — 1.8.0 (AgentFileStore, FileAccessProvider, MCPSkill, ToolMode, AgentEvalConverter, ChatContext, WorkflowAgent, TruncationStrategy, HistoryProvider, DelegatingSkillsSource + FunctionInvocationContext)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v8/" />
  <LinkCard title="Class deep dives Vol. 9 — 1.8.0 (OllamaChatClient, PurviewPolicyMiddleware, DurableAIAgent+Worker+Client, GitHubCopilotAgent, HyperlightExecuteCodeTool, HyperlightCodeActProvider, Mem0ContextProvider, RedisContextProvider+HistoryProvider, StandardMagenticManager+MagenticContext, FileSkillsSource+FilteringSkillsSource)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v9/" />
  <LinkCard title="Class deep dives Vol. 10 — 1.8.1 (Workflow + InProcRunnerContext, FunctionExecutor, FunctionInvocationLayer, MemoryStore + MemoryIndexEntry + MemoryTopicRecord, TodoStore + TodoFileStore + TodoSessionStore, DeduplicatingSkillsSource, SkillsProvider, MCPTaskOptions, InMemoryCheckpointStorage, EvalScoreResult + CompactionStrategy + BaseAgent)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v10/" />
  <LinkCard title="Class deep dives Vol. 11 — 1.8.1 (AgentTelemetryLayer, Edge+EdgeGroup+SingleEdgeGroup+InternalEdgeGroup, Case+Default, EdgeRunner hierarchy, ExecutionContext, WorkflowGraphValidator, MCPTool+MCPSpecificApproval, SerializationMixin+SerializationProtocol, Evaluator+EvalItemResult+EvalNotPassedError, PerServiceCallHistoryPersistingMiddleware)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v11/" />
  <LinkCard title="Class deep dives Vol. 12 — 1.8.1 (Skill+SkillResource+SkillScript ABCs, FileSkill, InlineSkillResource+InlineSkillScript, FileSkillScript+SkillScriptRunner, SupportsAgentRun, RunnerContext, SwitchCaseEdgeGroupCase+SwitchCaseEdgeGroupDefault, ValidationTypeEnum+WorkflowValidationError hierarchy, A2AAgent+A2AAgentSession+A2AExecutor, WorkflowRunnerException+WorkflowCheckpointException+MiddlewareException)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v12/" />
  <LinkCard title="Class deep dives Vol. 13 — 1.8.1 (OpenAIChatClient+OpenAIChatOptions+OpenAISettings, OpenAIChatCompletionClient, OpenAIEmbeddingClient+ContentFilterResultSeverity, AnthropicClient+AnthropicChatOptions, ClaudeAgent+ClaudeAgentOptions, AnthropicFoundryClient+AnthropicBedrockClient+AnthropicVertexClient, GroupChatOrchestrator+AgentBasedGroupChatOrchestrator, HandoffAgentExecutor+OrchestrationState, MagenticOrchestrator+MagenticPlanReviewRequest, declarative HTTP/MCP/approval handlers)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v13/" />
  <LinkCard title="Class deep dives Vol. 14 — 1.8.1 (State superstep cache, OutputDesignation output routing, MessageType+WorkflowMessage fan-in OTel, DictConvertible mixin, MiddlewareWrapper+BaseMiddlewarePipeline, pipeline classes, MiddlewareDict+categorize_middleware, FunctionRequestResult, OtelAttr+MessageListTimestampFilter, prompt-injection IFC defence security module)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v14/" />
  <LinkCard title="Class deep dives Vol. 15 — 1.8.1 (AGUIChatClient+AGUIEventConverter+AGUIHttpService, AgentFrameworkAgent+AgentFrameworkWorkflow+state_update, ThreadItemConverter+simple_to_agent_input+stream_agent_response, DevServer+serve+register_cleanup, GAIA+GAIATelemetryConfig+TaskRunner, CopilotStudioAgent, AzureAISearchContextProvider, CosmosHistoryProvider, DurableAIAgentClient+Worker+OrchestrationContext+AgentCallbackContext, AgentFunctionApp)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v15/" />
  <LinkCard title="Class deep dives Vol. 16 — 1.8.1 (FoundryAgent+FoundryAgentOptions, FoundryLocalClient, FoundryMemoryProvider, FoundryEvals+GeneratedEvaluatorRef, BedrockChatClient+BedrockChatOptions+BedrockGuardrailConfig, BedrockEmbeddingClient+BedrockEmbeddingOptions, MagenticManagerBase, BaseGroupChatOrchestrator+GroupChatRequestSentEvent+GroupChatResponseReceivedEvent, AgentRequestInfoResponse+CacheProvider, Purview exception hierarchy+acquire_token)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v16/" />
  <LinkCard title="Class deep dives Vol. 17 — 1.9.0 (ToolApprovalMiddleware+ToolApprovalRule+ToolApprovalState, AgentLoopMiddleware+JudgeVerdict+todos_remaining+background_tasks_running, SamplingApprovalCallback+MCP sampling security, to_prompt_agent, FoundryEmbeddingClient+FoundryEmbeddingOptions+FoundryEmbeddingSettings, ContentUnderstandingContextProvider+AnalysisSection+DocumentStatus, FileSearchConfig+FileSearchBackend, AgentFrameworkTracer, TaskRunner+patch_env_set_state, 8 new FoundryChatClient hosted tool factories)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v17/" />
  <LinkCard title="Class deep dives Vol. 18 — 1.9.0 (Skill+SkillFrontmatter+SkillScriptRunner, InlineSkill, skills source pipeline, AgentFileStore+InMemoryAgentFileStore+DTOs, FileAccessProvider, BackgroundAgentsProvider+BackgroundTaskInfo+BackgroundTaskStatus, MemoryStore+MemoryTopicRecord+MemoryIndexEntry, WorkflowGraphValidator+error types, MagenticBuilder+MagenticManagerBase+MagenticProgressLedger, LocalEvaluator+EvalItem+ConversationSplit+ExpectedToolCall)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v18/" />
  <LinkCard title="Class deep dives Vol. 19 — 1.9.0 (ConcurrentBuilder, SequentialBuilder, HandoffBuilder+HandoffConfiguration, HandoffAgentUserRequest, OrchestrationState, AgentModeProvider+get_agent_mode+set_agent_mode, TodoItem+TodoInput+TodoCompleteInput, TodoStore+TodoSessionStore+TodoFileStore, TodoProvider, MagenticResetSignal+StandardMagenticManager)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v19/" />
  <LinkCard title="Class deep dives Vol. 20 — 1.9.0 (capability Protocols: SupportsCodeInterpreterTool/SupportsWebSearchTool/SupportsImageGenerationTool/SupportsMCPTool/SupportsFileSearchTool/SupportsShellTool/SupportsGetEmbeddings; feature staging: ReleaseCandidateFeature/FeatureStageWarning/ExperimentalWarning; EmbeddingGenerationOptions/Embedding/GeneratedEmbeddings; WorkflowEventSource; SubWorkflowRequestMessage/SubWorkflowResponseMessage; RequestInfoMixin/response_handler; WorkflowAgent.RequestInfoFunctionArgs; EdgeGroupDeliveryStatus; IntegrityLabel/LabelTrackingFunctionMiddleware; MiddlewareTermination/WorkflowConvergenceException)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v20/" />
  <LinkCard title="Class deep dives Vol. 21 — 1.9.0 (WorkflowContext[OutT,W_OutT]; FanInEdgeGroup+FanOutEdgeGroup; SwitchCaseEdgeGroup+SwitchCaseEdgeGroupCase+SwitchCaseEdgeGroupDefault; TokenBudgetComposedStrategy; SelectiveToolCallCompactionStrategy+ToolResultCompactionStrategy; SlidingWindowStrategy+SummarizationStrategy; StepWrapper+FunctionalWorkflow+RunContext; MCPWebsocketTool+MCPStreamableHTTPTool; MCPTaskOptions; AgentResponseUpdate+ChatResponseUpdate+ContinuationToken)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v21/" />
  <LinkCard title="Class deep dives Vol. 22 — 1.9.0 (AgentFrameworkException hierarchy; WorkflowInterrupted+get_run_context(); enable/disable_instrumentation+enable_sensitive_telemetry; get_tracer+get_meter+span helpers; WorkflowState PowerFx; HttpRequestInfo+DefaultHttpRequestHandler; ExternalInputRequest+AgentExternalInputRequest HITL; MCPToolInvocation+MCPToolResult+DefaultMCPToolHandler; combine_labels+check_confidentiality_allowed+store_untrusted_content; quarantined_llm+inspect_variable+get_security_tools)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v22/" />
  <LinkCard title="Class deep dives Vol. 23 — agent-framework-declarative 1.0.0rc2 (DeclarativeActionExecutor+DeclarativeWorkflowState+DeclarativeEnvConfig+discover_env_references(); ConditionGroupEvaluatorExecutor+IfConditionEvaluatorExecutor+ConditionResult; ForeachInitExecutor+ForeachNextExecutor+BreakLoopExecutor+ContinueLoopExecutor+LoopIterationResult+LoopControl; SetValueExecutor+SetVariableExecutor+CreateConversationExecutor+SendActivityExecutor+ParseValueExecutor; AgentManifest+AgentDefinition+PromptAgent+EnvironmentVariable+agent_schema_dispatch(); Property+ArrayProperty+ObjectProperty+PropertySchema; Connection hierarchy; McpTool+McpServerApprovalMode hierarchy; Template+Format+Parser+Model+ModelOptions; InvokeAzureAgentExecutor+AgentResult+AgentExternalInputRequest+AgentExternalInputResponse)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v23/" />
  <LinkCard title="Class deep dives Vol. 24 — 1.9.0 (is_type_compatible+serialize_type+deserialize_type+normalize_type_to_list+resolve_type_annotation+try_coerce_to_type+is_instance_of+is_chat_agent; encode_checkpoint_value+decode_checkpoint_value; Runner+create_edge_runner; ShouldContinueCallable+ShouldContinueResult+FeedbackCallable+NextMessageCallable+JUDGE constants; ToolApprovalScope+ToolApprovalRuleCallback+ALWAYS_APPROVE constants; GroupChatRequestMessage+ORCH_MSG_KIND constants+clean_conversation_for_handoff+create_completion_message; MagenticOrchestratorEvent+MagenticOrchestratorEventType+MagenticAgentExecutor; evaluate_traces+RawFoundryEmbeddingClient; RawOpenAIChatCompletionClient; normalize_messages_input+ensure_author+latest_user_message)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v24/" />
  <LinkCard title="Class deep dives Vol. 25 — declarative 1.0.0rc2 (FunctionTool+OpenApiTool+WebSearchTool+FileSearchTool+CodeInterpreterTool+Binding; AgentFactory+DeclarativeLoaderError+ProviderLookupError+ProviderTypeMapping; WorkflowFactory+DeclarativeWorkflowBuilder; QuestionExecutor+RequestExternalInputExecutor+ExternalInputRequest+ExternalInputResponse; HttpRequestActionExecutor; InvokeMcpToolActionExecutor+MCPToolApprovalRequest; BaseToolExecutor+InvokeFunctionToolExecutor+ToolApprovalRequest+ToolApprovalResponse+ToolInvocationResult; JoinExecutor+EndWorkflowExecutor+EndConversationExecutor+CancelDialogExecutor+CancelAllDialogsExecutor; ActionComplete+ActionTrigger+DeclarativeStateData+ConversationData; ClearAllVariablesExecutor+EditTableExecutor+ResetVariableExecutor+SetTextVariableExecutor)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v25/" />
  <LinkCard title="Class deep dives Vol. 26 — agent-framework-durabletask 1.0.0b260521 (DurableAIAgentWorker+DurableAIAgentClient; DurableAIAgent+DurableAgentExecutor; DurableAIAgentOrchestrationContext; AgentEntity+AgentEntityStateProviderMixin; AgentCallbackContext+AgentResponseCallbackProtocol; RunRequest; AgentSessionId+DurableAgentSession; DurableAgentState+DurableAgentStateData; entry+usage hierarchy; content hierarchy+DurableStateFields+ContentTypes)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v26/" />
  <LinkCard title="azure-ai-agents integration reference Vol. 3 (10 classes)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v3/" />
  <LinkCard title="azure-ai-agents integration reference Vol. 4 (10 patterns)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v4/" />
  <LinkCard title="azure-ai-agents integration reference Vol. 5 (data model + streaming)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v5/" />
  <LinkCard title="azure-ai-agents integration reference Vol. 6 (auth, Bing config, token usage, data sources, chunking, tool choice, file-search results, streaming events, manual dispatch, multimodal)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v6/" />
</CardGrid>

## Reference

<CardGrid>
  <LinkCard title="Model providers" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_model_providers/" description="Every first-party chat client — real constructors." />
  <LinkCard title="Multi-agent orchestration" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_orchestration/" description="Sequential, Concurrent, Handoff, GroupChat, Magentic." />
  <LinkCard title="Middleware" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_middleware/" description="Agent / chat / function pipelines, short-circuit patterns." />
  <LinkCard title="Tools" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_tools/" description="@tool decorator, FunctionTool, approval, schemas, context." />
  <LinkCard title="MCP integration" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_mcp/" description="Stdio, streamable HTTP, websocket — approval and headers." />
  <LinkCard title="Skills" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_skills/" description="Progressive-disclosure knowledge via Skill / SkillsProvider." />
  <LinkCard title="Human in the loop" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_hitl/" description="request_info, response_handler, tool approval, resume." />
  <LinkCard title="Checkpointing" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_checkpointing/" description="File / in-memory storage, custom backends, resume semantics." />
  <LinkCard title="Compaction" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_compaction/" description="Sliding window, summarization, token-budget strategies." />
  <LinkCard title="Sessions & history" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sessions/" description="AgentSession, history providers, multi-provider audit logs, cross-process serialization." />
  <LinkCard title="Observability & telemetry" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_observability/" description="OpenTelemetry spans, metrics, logs, Azure Monitor integration." />
  <LinkCard title="Evaluation" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_evaluation/" description="evaluate_agent, evaluate_workflow, LocalEvaluator, @evaluator." />
  <LinkCard title="Comprehensive guide" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_comprehensive_guide/" description="Single-page technical reference — all core concepts." />
  <LinkCard title="2025 features" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_2025_features/" description="A2A, graph workflows, declarative agents, OpenTelemetry." />
  <LinkCard title="Functional workflows" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_functional_workflows/" description="@workflow, @step, RunContext HITL, as_agent(), checkpointing." />
  <LinkCard title="Advanced" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_advanced/" description="Azure AI Agents, production deployment patterns." />
  <LinkCard title="Recipes" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_recipes/" description="Copy-paste code for common scenarios." />
  <LinkCard title="Diagrams" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_diagrams/" description="System architecture, lifecycle, orchestration." />
  <LinkCard title="Production guide" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_production_guide/" description="Azure Container Apps, Kubernetes, CI/CD, compliance." />
  <LinkCard title="Class deep dives Vol. 1" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives/" description="Agent, RawAgent, FunctionTool, WorkflowBuilder, RunContext, InlineSkill, MCPStdioTool — 10 classes from source." />
  <LinkCard title="Class deep dives Vol. 2" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v2/" description="FileHistoryProvider, AgentMiddleware, ChatMiddleware, FunctionMiddleware, CompactionProvider, ToolResultCompactionStrategy, TokenBudgetComposedStrategy, FileCheckpointStorage, LocalEvaluator, WorkflowRunResult — 10 more classes from source." />
  <LinkCard title="Class deep dives Vol. 3 (1.7.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v3/" description="BackgroundAgentsProvider, MemoryContextProvider + MemoryFileStore, TodoProvider + TodoFileStore, AgentModeProvider, SummarizationStrategy, ContextWindowCompactionStrategy, SlidingWindowStrategy, SelectiveToolCallCompactionStrategy, WorkflowViz, MCPStreamableHTTPTool + MCPWebsocketTool — 10 classes source-verified at 1.7.0." />
  <LinkCard title="Class deep dives Vol. 4 (1.7.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v4/" description="Message + Content, ChatOptions + ChatResponse + ChatResponseUpdate + UsageDetails, ResponseStream, AgentContext, FunctionalWorkflow + StepWrapper, WorkflowEvent taxonomy, SkillsSource composition layer, EvalItem + EvalResults + EvalScoreResult, TokenizerProtocol + CharacterEstimatorTokenizer, ConversationSplit + ConversationSplitter — 10 class groups source-verified at 1.7.0." />
  <LinkCard title="Class deep dives Vol. 5 (1.7.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v5/" description="Executor + @handler + @executor, AgentExecutor + AgentExecutorRequest + AgentExecutorResponse, FanOutEdgeGroup + FanInEdgeGroup + SwitchCaseEdgeGroup, Runner + WorkflowMessage, SessionContext, AgentSession + register_state_type, BaseChatClient + SupportsChatGetResponse, SecretString + load_settings, WorkflowCheckpoint + CheckpointStorage, full exception hierarchy — 10 class groups source-verified at 1.7.0." />
  <LinkCard title="Class deep dives Vol. 6 (1.7.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v6/" description="ExperimentalFeature + @experimental / @release_candidate, WorkflowRunState + WorkflowErrorDetails, WorkflowExecutor + SubWorkflow message pair, AgentResponse + AgentResponseUpdate + ContinuationToken, BaseEmbeddingClient + embedding type family, FunctionInvocationConfiguration, ClassSkill + SkillFrontmatter + FileSkillsSource + SkillsProvider, Annotation + TextSpanRegion, provider capability protocols (5 Supports* interfaces), MiddlewareType + AgentMiddlewareLayer + ChatMiddlewareLayer — 10 class groups source-verified at 1.7.0." />
  <LinkCard title="Class deep dives Vol. 7 (1.7.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v7/" description="ContextProvider custom authoring, BackgroundTaskInfo + BackgroundTaskStatus, GroupChatBuilder + TerminationCondition + GroupChatSelectionFunction, HandoffBuilder + HandoffConfiguration (autonomous mode), MagenticBuilder + StandardMagenticManager + MagenticProgressLedger, SequentialBuilder + ConcurrentBuilder (fan-out/synthesise), AgentFactory + WorkflowFactory (declarative YAML), SecureAgentConfig + ContentLabel + IntegrityLabel + LabelTrackingFunctionMiddleware (prompt injection defence), FunctionalWorkflowAgent, ObservabilitySettings + configure_otel_providers — 10 class groups source-verified at 1.7.0." />
  <LinkCard title="Class deep dives Vol. 8 (1.8.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v8/" description="AgentFileStore + FileSystemAgentFileStore + InMemoryAgentFileStore (file storage ABC + 2 backends), FileAccessProvider + FileSearchResult + FileSearchMatch (5-tool file workspace harness), MCPSkill + MCPSkillResource + MCPSkillsSource (MCP-native SEP-2640 skill discovery), ToolMode + validate_tool_mode (tool-choice TypedDict), AgentEvalConverter + ExpectedToolCall + CheckResult + RubricScore (evaluation helper layer), ChatContext (full chat-middleware context + stream hooks), WorkflowAgent + WorkflowContext (workflow-as-agent with typed generic context), TruncationStrategy (oldest-first token/message compaction), HistoryProvider + InMemoryHistoryProvider (history provider ABC + in-memory default), DelegatingSkillsSource + InMemorySkillsSource + FunctionInvocationContext + progressive tools — 10 class groups source-verified at 1.8.0." />
  <LinkCard title="Class deep dives Vol. 9 (1.8.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v9/" description="OllamaChatClient + OllamaChatOptions (local LLM bridge), PurviewPolicyMiddleware + PurviewChatPolicyMiddleware + PurviewSettings (enterprise content compliance), DurableAIAgent + DurableAIAgentClient + DurableAIAgentWorker (durable workflow actors), GitHubCopilotAgent + GitHubCopilotOptions (Copilot routing), HyperlightExecuteCodeTool + AllowedDomain + FileMount (Wasm sandboxed code execution), HyperlightCodeActProvider (CodeAct context provider), Mem0ContextProvider (cloud semantic memory), RedisContextProvider + RedisHistoryProvider (Redis persistence), StandardMagenticManager + MagenticContext + MagenticProgressLedger (Magentic-One internals), FileSkillsSource + FilteringSkillsSource + AggregatingSkillsSource — 10 class groups source-verified at 1.8.0." />
  <LinkCard title="Class deep dives Vol. 10 (1.8.1)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v10/" description="Workflow + InProcRunnerContext (graph execution engine; run(), as_agent(), as_tool()), FunctionExecutor (@executor decorator internals; explicit type override), FunctionInvocationLayer (tool-calling loop mixin for custom chat clients), MemoryStore + MemoryIndexEntry + MemoryTopicRecord (memory harness ABC + data model), TodoStore + TodoItem + TodoInput + TodoFileStore + TodoSessionStore (todo harness; file-backed and session-backed), DeduplicatingSkillsSource (first-one-wins deduplication), SkillsProvider (context provider; from_paths; instruction_template; require_script_approval), MCPTaskOptions (SEP-2663 long-running task lifecycle), InMemoryCheckpointStorage (test/dev checkpointing; save/load/delete/get_latest), EvalScoreResult + CompactionStrategy + BaseAgent (eval dimensions, custom compaction protocol, minimal agent base) — 10 class groups source-verified at 1.8.1." />
  <LinkCard title="Class deep dives Vol. 11 (1.8.1)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v11/" description="AgentTelemetryLayer + ChatTelemetryLayer + EmbeddingTelemetryLayer (OTel mixin layer — AGENT_PROVIDER_NAME, token/duration histograms, sensitive telemetry toggle), Edge + EdgeGroup + SingleEdgeGroup + InternalEdgeGroup (workflow graph primitives — conditional routing, serialisation round-trip, EdgeGroup.register custom subclasses), Case + Default (runtime switch-case wrappers for add_switch_case_edge_group), EdgeRunner hierarchy (SingleEdgeRunner, FanOutEdgeRunner, FanInEdgeRunner, SwitchCaseEdgeRunner — EdgeGroupDeliveryStatus enum, buffer semantics, concurrent fan-out), ExecutionContext (sub-workflow tracking — HITL pending_requests / collected_responses / expected_response_count), WorkflowGraphValidator (7 pre-build checks: edge duplication, type compatibility, connectivity, self-loops, dead-ends, output validation — error taxonomy), MCPTool + MCPSpecificApproval (shared base for all 3 MCP transports — full constructor parameter table, per-tool result parsing, additional_tool_argument_names forwarding), SerializationMixin + SerializationProtocol (DTO round-trip foundation — DEFAULT_EXCLUDE, INJECTABLE, dependency injection patterns, custom serialisable tool), Evaluator + EvalItemResult + EvalNotPassedError (evaluation backend Protocol — custom evaluator implementation, CI gate pattern), PerServiceCallHistoryPersistingMiddleware (per-service-call history persistence — service_stores_history flag, sentinel conversation ID, audit logging use-cases) — 10 class groups source-verified at 1.8.1." />
  <LinkCard title="Class deep dives Vol. 12 (1.8.1)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v12/" description="Skill + SkillResource + SkillScript ABCs (contract methods: get_content, get_resource, get_script, read, run, parameters_schema — custom skill backends), FileSkill (SKILL.md content cache, scripts XML block injection, case-insensitive resource/script lookup), InlineSkillResource + InlineSkillScript (static vs callable resources, mutual-exclusivity validation, in-process script execution, lazy auto-generated parameters_schema, _accepts_kwargs precomputation), FileSkillScript + SkillScriptRunner (file-backed scripts, fixed CLI array schema, async runner protocol, isinstance verification), SupportsAgentRun (structural protocol — custom agents without framework inheritance, isinstance guard, workflow and A2A integration), RunnerContext (full workflow execution context protocol — message routing, event pipeline, checkpointing API, streaming mode toggle, HITL request/response, custom test stub), SwitchCaseEdgeGroupCase + SwitchCaseEdgeGroupDefault (serialisable branch descriptors vs runtime Case/Default — condition_name auto-extraction, to_dict/from_dict round-trip, missing-callable placeholder, condition re-registration pattern), ValidationTypeEnum + WorkflowValidationError hierarchy (6 enum values, EdgeDuplicationError + edge_id, TypeCompatibilityError + source/target type lists, GraphConnectivityError, match-based dispatcher, custom reporting), A2AAgent + A2AAgentSession + A2AExecutor (URL/AgentCard/client init, dual streaming/non-streaming httpx clients, auth interceptors, timeout config, A2AAgentSession context_id/task_id/task_state round-trip, A2A server exposure with A2AExecutor + run_kwargs), WorkflowRunnerException + WorkflowCheckpointException + MiddlewareException (leaf exception classes — checkpoint I/O failure patterns, MiddlewareException vs MiddlewareTermination distinction, supervisor exception dispatcher) — 10 class groups source-verified at 1.8.1." />
  <LinkCard title="Class deep dives Vol. 13 (1.8.1)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v13/" description="OpenAIChatClient + OpenAIChatOptions + OpenAISettings + RawOpenAIChatClient (agent_framework.openai — Responses API 4-layer MRO, STORES_BY_DEFAULT server-side state, x-ms-served-model Azure header, per-request reasoning options, Responses vs Completions comparison table), OpenAIChatCompletionClient + OpenAIChatCompletionOptions + RawOpenAIChatCompletionClient (Chat Completions API — logprobs, prediction, stateless design), OpenAIEmbeddingClient + OpenAIEmbeddingOptions + OpenAIContinuationToken + ContentFilterResultSeverity + OpenAIContentFilterException (embeddings+RAG+MemoryContextProvider, background polling, Azure content filter exception hierarchy), AnthropicClient + AnthropicChatOptions + RawAnthropicClient (agent_framework.anthropic — Anthropic Messages API, extended thinking ThinkingConfigEnabled, top_k, service_tier, additional_beta_flags per-request), ClaudeAgent + ClaudeAgentOptions + RawClaudeAgent (Claude Code CLI agent — allowed_tools/disallowed_tools, mcp_servers McpServerConfig, permission_mode, sandbox, multi-agent workflow participation), AnthropicFoundryClient + AnthropicBedrockClient + AnthropicVertexClient (multi-cloud Claude — Azure Foundry AAD token provider, Bedrock IAM profile, Vertex ADC/GoogleCredentials, multi-cloud failover pattern), GroupChatOrchestrator + AgentBasedGroupChatOrchestrator + GroupChatState + GroupChatRequestMessage + AgentOrchestrationOutput (orchestrations internals — function-driven vs LLM-driven selection, frozen GroupChatState snapshot, structured AgentOrchestrationOutput Pydantic model), HandoffAgentExecutor + HandoffAgentUserRequest + HandoffSentEvent + OrchestrationState (handoff internals — autonomous_mode + turn_limit, HITL create_response/terminate helpers, event observability, OrchestrationState to_dict/from_dict checkpoint), MagenticOrchestrator + MagenticAgentExecutor + MagenticOrchestratorEvent + MagenticPlanReviewRequest + MagenticPlanReviewResponse + MagenticProgressLedgerItem (Magentic deeper — outer/inner loop, MagenticResetSignal for replanning, plan signoff HITL approve/revise, ledger item serialisation), HttpRequestHandler + DefaultHttpRequestHandler + HttpRequestInfo + HttpRequestResult + MCPToolHandler + DefaultMCPToolHandler + MCPToolInvocation + MCPToolResult + ToolApprovalRequest + ToolApprovalResponse + ExternalInputRequest + ExternalInputResponse + AgentExternalInputRequest + AgentExternalInputResponse (declarative handlers — 3 construction modes for DefaultHttpRequestHandler, SSRF guard pattern, MCP LRU cache+LIST_TOOLS_TOOL_NAME discovery, tool approval gate, agent HITL) — 10 class groups source-verified at 1.8.1." />
  <LinkCard title="Class deep dives Vol. 14 (1.8.1)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v14/" description="State (superstep cache — set/get/commit/discard/delete sentinel/export_state/import_state, BSP semantics, checkpoint integration), OutputDesignation (output routing rule — is_terminal/is_intermediate/classify, compatibility mode vs explicit output_from/intermediate_output_from), MessageType enum + WorkflowMessage (STANDARD vs RESPONSE, plural trace_contexts for fan-in OTel link propagation, to_dict/from_dict, backward-compat trace_context/source_span_id accessors), DictConvertible + encode_value (dict/JSON round-trip mixin — to_dict/from_dict/clone deep-shallow/to_json/from_json, recursive nested encoding), MiddlewareWrapper + BaseMiddlewarePipeline (callable→class adapter, has_middlewares gate, _register_middleware_with_wrapper auto-wrapping), AgentMiddlewarePipeline + ChatMiddlewarePipeline + FunctionMiddlewarePipeline (concrete pipelines — execute() for streaming/non-streaming, matches() cache-key check, MiddlewareTermination short-circuit), MiddlewareDict + categorize_middleware (TypedDict typed middleware bag — agent/function/chat buckets, callable type inference, multi-source merge), FunctionRequestResult TypedDict (action return/continue/stop, errors_in_a_row, result_message, function_call_count — tool-loop diagnostics), OtelAttr + MessageListTimestampFilter (GenAI semantic convention enum — all 35 attribute name constants; logging.Filter for 1µs timestamp offset ordering in log aggregators), PolicyEnforcementFunctionMiddleware + ConfidentialityLabel + ContentVariableStore + VariableReferenceContent + LabeledMessage + InspectVariableInput (IFC prompt-injection defence — variable indirection store, PUBLIC/PRIVATE/USER_IDENTITY confidentiality, block vs approval_on_violation modes, audit_log, SecureAgentConfig one-liner) — 10 class groups source-verified at 1.8.1." />
  <LinkCard title="Class deep dives Vol. 15 (1.8.1)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v15/" description="AGUIChatClient + AGUIEventConverter + AGUIHttpService (AG-UI streaming stateful UI protocol — run(), stateless mode, SSE event delivery, per-session converter requirement), AgentFrameworkAgent + AgentFrameworkWorkflow + state_update() + add_agent_framework_fastapi_endpoint() (FastAPI/Teams hosting — pending-approval registry max 10,000, tags+dependencies params, CORS, env-var wiring), ThreadItemConverter + simple_to_agent_input() + stream_agent_response() (ChatKit thread-item streaming — multimodal ThreadItem mapping, delta merging), DevServer + serve() + register_cleanup() (local OpenAI-compatible debug server — multi-agent dict, CORS removed as security fix, graceful teardown), GAIA + GAIATelemetryConfig + TaskRunner + TaskResult + Prediction + Evaluation (GAIA benchmark lab — telemetry via GAIA(telemetry_config=...), async callable TaskRunner.__call__(), is_correct + score aggregation; install: agent-framework-lab), CopilotStudioAgent (Copilot Studio bridge — env-var prefix COPILOTSTUDIOAGENT__, propagate_session as_tool option), AzureAISearchContextProvider (semantic/agentic retrieval — env-var prefix AZURE_SEARCH_, top_k/semantic_configuration_name, retrieval_reasoning_effort: minimal|low|medium), CosmosHistoryProvider (Cosmos DB history — env-var prefix AZURE_COSMOS_, store_context_messages, store_context_from set), DurableAIAgentClient + DurableAIAgentWorker + DurableAIAgentOrchestrationContext + AgentCallbackContext + AgentResponseCallbackProtocol (durable task hosting — get_agent().run() blocking poll, yield Task in orchestration, add_agent() callback), AgentFunctionApp (Azure Functions serverless — agents=[]/workflow= init, enable_http_endpoints, enable_mcp_tool_trigger, enable_health_check) — 10 class groups source-verified at 1.8.1." />
  <LinkCard title="Class deep dives Vol. 16 (1.8.1)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v16/" description="FoundryAgent + FoundryAgentOptions + RawFoundryAgent + RawFoundryAgentChatClient (hosted Azure AI Foundry agents — PromptAgent requires agent_version, HostedAgent uses allow_preview + isolation_key, client_type override for raw chat client), FoundryLocalClient + FoundryLocalChatOptions + FoundryLocalSettings (on-device LLM via Foundry Local SDK — bootstrap, prepare_model, device NPU/GPU/CPU auto-detect, FOUNDRY_LOCAL_MODEL env var, structured output), FoundryMemoryProvider (Azure AI Foundry Memory Store semantic memory context provider — memory_store_name, scope, update_delay, before_run search + after_run async write lifecycle), FoundryEvals + GeneratedEvaluatorRef + evaluate_foundry_target + evaluate_traces (Foundry cloud evaluation harness — 19 built-in evaluator name constants, smart defaults, tool_call_accuracy auto-added for tool-using agents, poll_interval, GeneratedEvaluatorRef for rubric evaluators), BedrockChatClient + BedrockChatOptions + BedrockGuardrailConfig + BedrockSettings (Amazon Bedrock Converse API — BEDROCK_REGION/BEDROCK_CHAT_MODEL env vars, guardrailConfig TypedDict, sync/async streamProcessingMode, unsupported-options table), BedrockEmbeddingClient + BedrockEmbeddingOptions + BedrockEmbeddingSettings (Bedrock Titan Embeddings v2 — BEDROCK_EMBEDDING_MODEL, normalize flag for unit-length vectors, MemoryContextProvider wiring), MagenticManagerBase (abstract base for custom Magentic managers — plan/replan/create_progress_ledger/prepare_final_answer abstract methods, max_stall_count/max_reset_count/max_round_count, on_checkpoint_save/restore hooks, wiring with MagenticBuilder), BaseGroupChatOrchestrator + GroupChatRequestSentEvent + GroupChatResponseReceivedEvent (group chat orchestrator base class — max_rounds coercion to ≥1, 4 @handler overloads for str/Message/list/response, event observability via dataclasses, TerminationCondition support), AgentRequestInfoResponse + CacheProvider (HITL approval data model — approve()/from_messages()/from_strings() static factories; CacheProvider async get/set/remove Protocol for Purview cache injection), Purview exception hierarchy + acquire_token (PurviewAuthenticationError 401/403, PurviewPaymentRequiredError 402, PurviewRateLimitError 429, PurviewRequestError other 4xx/5xx, PurviewServiceError base; acquire_token MSAL silent-then-interactive flow, custom scopes, on-disk SerializableTokenCache pattern) — 10 class groups source-verified at 1.8.1." />
  <LinkCard title="Class deep dives Vol. 17 (1.9.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v17/" description="ToolApprovalMiddleware + ToolApprovalRule + ToolApprovalState (interactive tool approval — standing rules, argument wildcard matching, queue drain logic, SerializationMixin round-trip), AgentLoopMiddleware + JudgeVerdict + todos_remaining + background_tasks_running (LLM-judged self-improvement loops — fresh_context snapshot/restore, inject_progress, with_judge criteria placeholder, return_final_only aggregation), SamplingApprovalCallback + MCP sampling security (confused-deputy mitigation — deny-by-default callback, token cap, per-connection request counter, async callback support), to_prompt_agent (Foundry PromptAgent publisher — @experimental, RawFoundryChatClient required, model resolution order, local MCP raises ValueError), FoundryEmbeddingClient + FoundryEmbeddingOptions + FoundryEmbeddingSettings + RawFoundryEmbeddingClient (multimodal text+image embeddings — mixed batch split/reassemble, image detection via MIME prefix, image_model precedence, async context manager), ContentUnderstandingContextProvider + AnalysisSection + DocumentStatus (Azure Content Understanding — MEDIA_TYPE_ANALYZER_MAP auto-detect, before_run lifecycle, deferred vs blocking analysis, auto-registered list_documents tool), FileSearchConfig + FileSearchBackend + OpenAIFileSearchBackend + FoundryFileSearchBackend (vector store upload/delete backends — _OpenAICompatBackend shared surface, create_and_poll indexing wait, custom backend example), AgentFrameworkTracer (Agent Lightning OTel bridge — OBSERVABILITY_SETTINGS singleton toggle, init/teardown lifecycle order), TaskRunner + patch_env_set_state + unpatch_env_set_state (Tau2 airline benchmark harness — three-part conversation assembly, reinit() chaining, termination reasons, EvaluationType.ALL reward calculation), 8 new FoundryChatClient hosted tool factories (get_mcp_tool, get_azure_ai_search_tool, get_sharepoint_tool, get_fabric_tool, get_memory_search_tool, get_computer_use_tool, get_browser_automation_tool, get_a2a_tool — validation rules, hosted vs local MCP distinction) — 10 class groups source-verified at agent-framework 1.9.0." />
  <LinkCard title="Class deep dives Vol. 18 (1.9.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v18/" description="Skill + SkillFrontmatter + SkillScriptRunner (Agent Skills spec — name/description/compatibility validation, file-script runner protocol, SkillFrontmatter from_yaml, custom Skill subclass), InlineSkill (@skill.resource/@skill.script decorators — XML content synthesis, _accepts_kwargs precomputation, content caching, lazy parameters_schema), InMemorySkillsSource + AggregatingSkillsSource + FilteringSkillsSource + DeduplicatingSkillsSource (skills pipeline — fan-in, predicate filter, first-one-wins dedup, shared pipeline factory), AgentFileStore + InMemoryAgentFileStore + FileSearchMatch + FileSearchResult (file storage ABC + in-memory backend — exclusive-create asyncio.Lock, __slots__ DTOs, custom Azure Blob backend), FileAccessProvider (6-tool file workspace — save/read/delete/list/search, require_delete_approval gate, shared across sessions), BackgroundAgentsProvider + BackgroundTaskInfo + BackgroundTaskStatus (background sub-agent delegation — asyncio.Task fan-out, continue_task session resume, LOST status on process restart), MemoryStore + MemoryTopicRecord + MemoryIndexEntry (durable memory backing store ABC — Markdown serialization, slug normalization, pointer line rendering, custom dict-backed store), WorkflowGraphValidator + EdgeDuplicationError + GraphConnectivityError + TypeCompatibilityError (7-check pre-build validation — DFS reachability, self-loop warnings, FanIn list-wrapping type check, full error taxonomy), MagenticBuilder + MagenticManagerBase + MagenticProgressLedger + MagenticProgressLedgerItem (Magentic One internals — 5-item progress ledger, abstract plan/replan/create_progress_ledger/prepare_final_answer, custom manager example), LocalEvaluator + EvalItem + EvalScoreResult + ConversationSplit + ExpectedToolCall (local evaluation framework — per_turn_items, LAST_TURN/FULL splits, @evaluator custom checks, evaluate_agent end-to-end) — 10 class groups source-verified at agent-framework 1.9.0." />
  <LinkCard title="Class deep dives Vol. 19 (1.9.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v19/" description="ConcurrentBuilder (fan-out/fan-in — with_aggregator sync/async callback, output_from/intermediate_output_from routing, with_request_info, checkpoint), SequentialBuilder (shared-conversation chain — chain_only_agent_responses, output_from, with_request_info per-agent filter, checkpoint), HandoffBuilder + HandoffConfiguration + HandoffSentEvent (decentralised routing — add_handoff graph, with_start_agent, with_autonomous_mode per-agent turn limits, output_from, event observability), HandoffAgentUserRequest (HITL factories — create_response str/Message/list, terminate), OrchestrationState (unified checkpoint dataclass — conversation/round_index/metadata/task, to_dict/from_dict, pattern-specific extension), AgentModeProvider + get_agent_mode + set_agent_mode (plan/execute mode harness — mode_descriptions Mapping, {available_modes}/{current_mode} template, default_mode, external-change notification), TodoItem + TodoInput + TodoCompleteInput (DTO primitives — SerializationMixin, stripping/validation, __eq__/__repr__), TodoStore + TodoSessionStore + TodoFileStore (backing store ABC — session state, file JSON with path-traversal guard, Windows reserved name encoding, custom backend), TodoProvider (5-tool harness — todos_add/complete/remove/get_remaining/get_all, per-session asyncio.Lock via WeakKeyDictionary, combined with AgentModeProvider), MagenticResetSignal + StandardMagenticManager (stall-recovery sentinel + 7 prompt overrides, max_stall/reset/round_count, progress_ledger_retry_count, observe REPLANNED events) — 10 class groups source-verified at agent-framework 1.9.0." />
  <LinkCard title="Class deep dives Vol. 20 (1.9.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v20/" description="6 hosted-tool capability Protocols (SupportsCodeInterpreterTool, SupportsWebSearchTool, SupportsImageGenerationTool, SupportsMCPTool, SupportsFileSearchTool, SupportsShellTool — @runtime_checkable isinstance guards + factory method signatures), SupportsGetEmbeddings (typed embedding duck-typing Protocol — get_embeddings Generic params, custom client example), ReleaseCandidateFeature + FeatureStageWarning + ExperimentalWarning (feature staging — _WARNED_FEATURES deduplication, single-line formatter install, pytest filter patterns), EmbeddingGenerationOptions + Embedding + GeneratedEmbeddings (embedding DTOs — TypedDict options, Generic[T] vector type, list[Embedding] subclass with usage+options), WorkflowEventSource (EXECUTOR vs FRAMEWORK str-Enum — OTel span attribute, match pattern), SubWorkflowRequestMessage + SubWorkflowResponseMessage (hierarchical HITL bridge — create_response with isinstance type check, executor_id for multi-instance routing), RequestInfoMixin + response_handler decorator (executor-level HITL wiring — handler discovery via _discover_response_handlers, introspection vs explicit type modes, forward reference support, is_request_supported guard), WorkflowAgent.RequestInfoFunctionArgs (HITL tool-call serialization dataclass — to_dict/from_dict round-trip, empty request_id ValueError, REQUEST_INFO_FUNCTION_NAME sentinel constant), EdgeGroupDeliveryStatus (OTel edge-delivery enum — DELIVERED/BUFFERED/DROPPED_TYPE_MISMATCH/DROPPED_CONDITION_FALSE/DROPPED_TARGET_MISMATCH/EXCEPTION, span processor aggregation), IntegrityLabel + LabelTrackingFunctionMiddleware (IFC security @experimental — TRUSTED/UNTRUSTED integrity enum, 3-tier label propagation priority table, variable indirection auto-hide, get_security_tools + reset_context_label + list_variables), MiddlewareTermination + WorkflowConvergenceException (MiddlewareTermination.result early-return payload; WorkflowConvergenceException.inner_exception + log_level=DEBUG) — 10 class groups source-verified at agent-framework 1.9.0." />
  <LinkCard title="Class deep dives Vol. 21 (1.9.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v21/" description="WorkflowContext[OutT, W_OutT] (per-executor execution context — send_message/yield_output/request_info API, state R/W with commit/discard, source_executor_ids fan-in list, is_streaming flag), FanInEdgeGroup + FanOutEdgeGroup (converging/broadcasting edge groups — min_sources/min_targets validation, selection_func dynamic routing, to_dict/from_dict serialization), SwitchCaseEdgeGroup + SwitchCaseEdgeGroupCase + SwitchCaseEdgeGroupDefault (switch/case routing — sequential predicate evaluation, exactly-one-default invariant, missing-condition placeholder, condition re-registration), TokenBudgetComposedStrategy (multi-strategy composition — early_stop short-circuit, annotate_message_groups pipeline pass-through, strict-budget double-fallback), SelectiveToolCallCompactionStrategy + ToolResultCompactionStrategy (tool history reduction — exclude list vs inline summary replacement, keep_last_tool_call_groups sliding retention), SlidingWindowStrategy + SummarizationStrategy (sliding window + LLM-backed summarization — preserve_system, target_count+threshold trigger, bidirectional summary trace metadata), StepWrapper + FunctionalWorkflow + RunContext (functional workflow internals — per-step caching by call_index, executor_bypassed on cache hit, checkpoint_storage per step, as_agent() wrapper), MCPWebsocketTool + MCPStreamableHTTPTool (MCP network transports — WebSocket wss:// reconnect loop, streamable HTTP/SSE with header_provider ContextVar injection), MCPTaskOptions (SEP-2663 long-running task lifecycle — tasks/call → tasks/get polling → tasks/result, default_ttl validation, max_task_wait, cancel_remote_task_on_local_cancellation), AgentResponseUpdate + ChatResponseUpdate + ContinuationToken (streaming chunk types — .text property, continuation_token for background polling resumption, author_name in multi-agent streams, finish_reason) — 10 class groups source-verified at agent-framework 1.9.0." />
  <LinkCard title="Class deep dives Vol. 22 (1.9.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v22/" description="AgentFrameworkException hierarchy (4-branch tree: Agent/ChatClient/Integration/Content + Tool/Workflow/Settings), WorkflowInterrupted + get_run_context(), enable/disable_instrumentation + enable_sensitive_telemetry, get_tracer + get_meter + span helpers, WorkflowState PowerFx variable store, HttpRequestInfo + DefaultHttpRequestHandler, ExternalInputRequest + AgentExternalInputRequest HITL pair, MCPToolInvocation + MCPToolResult + DefaultMCPToolHandler, combine_labels + check_confidentiality_allowed + store_untrusted_content, quarantined_llm + inspect_variable + get_security_tools — 10 class groups source-verified at 1.9.0." />
  <LinkCard title="Class deep dives Vol. 23 (agent-framework-declarative 1.0.0rc2)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v23/" description="DeclarativeActionExecutor + DeclarativeWorkflowState + DeclarativeEnvConfig + discover_env_references() (PowerFx graph-node base layer — Env allowlist, dot-path access, eval()), ConditionGroupEvaluatorExecutor + IfConditionEvaluatorExecutor + ConditionResult (declarative branching — first-match, ELSE_BRANCH_INDEX=-1), ForeachInitExecutor + ForeachNextExecutor + BreakLoopExecutor + ContinueLoopExecutor + LoopIterationResult + LoopControl (loop graph nodes — LOOP_STATE_KEY, current_item/index, break/continue signals), SetValueExecutor + SetVariableExecutor + CreateConversationExecutor + SendActivityExecutor + ParseValueExecutor (variable and messaging graph nodes), AgentManifest + AgentDefinition + PromptAgent + EnvironmentVariable + agent_schema_dispatch() (declarative schema types), Property + ArrayProperty + ObjectProperty + PropertySchema (property schema layer — from_dict dispatch, to_json_schema()), Connection + 4 subclasses (kind-based dispatch — ReferenceConnection, RemoteConnection, ApiKeyConnection, AnonymousConnection), McpTool + McpServerApprovalMode hierarchy (MCP tool with always/never/specify approval gating), Template + Format + Parser + Model + ModelOptions (declarative model config — additionalProperties **kwargs), InvokeAzureAgentExecutor + AgentResult + AgentExternalInputRequest + AgentExternalInputResponse + _extract_json_from_response() (Azure AI Foundry HITL Yield/Resume) — 10 class groups source-verified." />
  <LinkCard title="Class deep dives Vol. 24 (1.9.0)" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v24/" description="is_type_compatible + serialize_type + deserialize_type + normalize_type_to_list + resolve_type_annotation + try_coerce_to_type + is_instance_of + is_chat_agent (workflow edge type-validation engine — 7-case compatibility algorithm, Union splitting, forward-reference eval, dict→Pydantic coercion), encode_checkpoint_value + decode_checkpoint_value (checkpoint serde — JSON-native pass-through, recursive collection encoding, pickle+base64 for custom types, allowed_types security allowlist), Runner + create_edge_runner (Pregel superstep runner — run_until_convergence loop, 50 ms event polling, convergence detection, EdgeGroup→EdgeRunner factory dispatch), ShouldContinueCallable + ShouldContinueResult + FeedbackCallable + NextMessageCallable + JUDGE constants (AgentLoopMiddleware callable types — keyword injection table, DEFAULT_JUDGE_INSTRUCTIONS+CRITERIA_PLACEHOLDER template, FeedbackCallable progress log), ToolApprovalScope + ToolApprovalRuleCallback + ALWAYS_APPROVE constants + create_always_approve helpers (approval protocol internals — Literal['tool','tool_with_arguments'] scope, DEFAULT_TOOL_APPROVAL_SOURCE_ID session key, pre-approval callback), GroupChatRequestMessage + ORCH_MSG_KIND constants + clean_conversation_for_handoff + create_completion_message (orchestration protocol utilities — request envelope, kind classification strings, text-only handoff cleaning, standardised completion), MagenticOrchestratorEvent + MagenticOrchestratorEventType + MagenticAgentExecutor (Magentic event observability — PLAN_CREATED/REPLANNED/PROGRESS_LEDGER_UPDATED enum, reset-aware executor), evaluate_traces + RawFoundryEmbeddingClient (Foundry trace-based evaluation + multimodal embedding batch split/reassemble), RawOpenAIChatCompletionClient (raw Chat Completions layer — FunctionInvocationLayer→ChatMiddlewareLayer→ChatTelemetryLayer ordering contract, INJECTABLE async_client), normalize_messages_input + ensure_author + latest_user_message (workflow message utilities — heterogeneous AgentRunInputs normalisation, author fallback, reverse-scan user message) — 10 class groups source-verified at 1.9.0." />
  <LinkCard title="Class deep dives Vol. 25 — declarative 1.0.0rc2 / 1.9.0" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v25/" description="FunctionTool + OpenApiTool + WebSearchTool + FileSearchTool + CodeInterpreterTool + Binding (5 non-MCP tool subclasses — Binding state-wiring, kind-dispatch, FileSearchTool score-threshold+ranker), AgentFactory + DeclarativeLoaderError + ProviderLookupError + ProviderTypeMapping (built-in provider table, safe_mode, env_file_path, additional_mappings TypedDict), WorkflowFactory + DeclarativeWorkflowBuilder (max_iterations/maxTurns 3-tier precedence, http+mcp handler build-time guards, checkpointing), QuestionExecutor + RequestExternalInputExecutor + ExternalInputRequest + ExternalInputResponse (declarative HITL — choices+allowFreeText, requestType discriminator, request_id round-trip), HttpRequestActionExecutor (PowerFx-evaluated HTTP params, JSON-first parse, DeclarativeActionError wrapping, cancellation propagation), InvokeMcpToolActionExecutor + MCPToolApprovalRequest (requireApproval gate, is_error non-raise contract vs HTTP raise), BaseToolExecutor + InvokeFunctionToolExecutor + ToolApprovalRequest + ToolApprovalResponse + ToolInvocationResult (dual registry — factory + State; isawaitable async support; rejected field), JoinExecutor + EndWorkflowExecutor + EndConversationExecutor + CancelDialogExecutor + CancelAllDialogsExecutor (JoinExecutor sends ActionComplete(); dead-end nodes halt graph by silence), ActionComplete + ActionTrigger + DeclarativeStateData + ConversationData (8-key TypedDict incl. Custom+_declarative_loop_state; history→messages deprecation), ClearAllVariablesExecutor + EditTableExecutor + ResetVariableExecutor + SetTextVariableExecutor (local scope wipe; table add/insert/remove/clear; nil reset; str-coerced set) — 10 class groups source-verified at declarative 1.0.0rc2 / 1.9.0." />
  <LinkCard title="Class deep dives Vol. 26 — agent-framework-durabletask 1.0.0b260521" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v26/" description="DurableAIAgentWorker + DurableAIAgentClient (entity registration — dafx-{name} prefix, poll-retry clamping), DurableAIAgent + DurableAgentExecutor (synchronous TaskT shim — stream=True raises ValueError, options key extraction), DurableAIAgentOrchestrationContext (orchestration yield pattern — get_agent() returns DurableAgentTask), AgentEntity + AgentEntityStateProviderMixin (streaming-first execution kernel — lazy state cache, is_error in-memory only), AgentCallbackContext + AgentResponseCallbackProtocol (frozen callback context — exceptions swallowed, sync callbacks accepted), RunRequest (canonical request DTO — correlationId required in from_dict(), coerce_role(), fire-and-forget mode), AgentSessionId + DurableAgentSession (@name@key wire format — dafx-{name} entity address, from_dict() defensive copy), DurableAgentState + DurableAgentStateData (schema v1.1.0 — missing schemaVersion resets silently, try_get_agent_response()), DurableAgentStateEntry hierarchy + DurableAgentStateUsage ($type discriminator — is_error not serialised, extensionData camelCase), DurableAgentStateContent hierarchy + DurableStateFields + ContentTypes (11 content types — unknown→DurableAgentStateUnknownContent, reasoning stripped on replay) — 10 class groups source-verified at agent-framework-durabletask 1.0.0b260521." />
  <LinkCard title="Class deep dives Vol. 27 — 1.9.0" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v27/" description="ContentLabel + combine_labels + check_confidentiality_allowed (IFC label algebra — string coercion, 3-tier priority table, context ≤ allowed check); store_untrusted_content + get_security_tools + security tool constructors (FIDES tool wiring — approval_mode=never_require, WARNING-level audit log, _global_variable_store singleton); enable/disable_instrumentation + enable_sensitive_telemetry (sticky OTel lifecycle — _user_disabled flag, force=True override, OBSERVABILITY_SETTINGS singleton); create_resource + create_metric_views + histogram boundary constants (OTel provider setup — TOKEN_USAGE_BUCKET_BOUNDARIES 14 values 4× scale, OPERATION_DURATION_BUCKET_BOUNDARIES 14 values 2× scale); get_tracer + get_meter + INNER_ACCUMULATED_USAGE + INNER_RESPONSE_TELEMETRY_CAPTURED_FIELDS context vars (span helpers + inner-span dedup via ContextVar); create_mcp_client_span + set_mcp_span_error (MCP telemetry — {method} {target} span naming); group_messages + annotate_message_groups (compaction grouping — 4 group kinds, suffix-only re-annotation via _first_annotation_gaps); apply_compaction + project_included_messages + included_messages + included_token_count + annotate_token_counts (compaction pipeline — annotate→tokenize→strategy→project, EXCLUDED_KEY sentinel); normalize_messages + detect_media_type_from_base64 + merge_chat_options + prepend_instructions_to_messages (message utilities — None→[], magic-bytes MIME, \\n concat, None-skip override); normalize_tools + validate_chat_options + map_chat_to_agent_update + add_usage_details (validation + conversion — async validate, raw_representation=update, int-only usage keys) — 10 class groups source-verified at agent-framework 1.9.0." />
  <LinkCard title="Class deep dives Vol. 28 — 1.9.0 / agent-framework-claude 1.0.0b260609 / agent-framework-ag-ui 1.0.0rc5" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v28/" description="RawClaudeAgent + ClaudeAgent + ClaudeAgentOptions + ClaudeAgentSettings (Claude Code CLI integration — AGENT_PROVIDER_NAME='anthropic.claude', owns-client lifecycle, PermissionMode/SandboxSettings, env-prefix resolution); AgentFrameworkAgent + AgentConfig (AG-UI wrapper for any SupportsAgentRun — state_schema Pydantic dispatch, predict_state_config tool-argument interception, require_confirmation gate, snapshot_store wiring); AGUIThreadSnapshot + AGUIThreadSnapshotStore + InMemoryAGUIThreadSnapshotStore (replayable thread state — messages+state+interrupt triad, Protocol async save/get/delete, max_snapshots LRU eviction, scope+thread_id composite key, deep-copy on save); PredictiveStateHandler + PredictStateConfig (streaming predictive state — config dict[state_key→{tool,tool_arg}], streaming_tool_args accumulator, last_emitted_state dedup guard, pending_state_updates flush); AGUIRequest + AGUIChatOptions + AgentState + RunMetadata (AG-UI protocol types — AliasChoices camelCase aliases, client-side tools forwarding, thread_id metadata continuity, predict_state list[PredictStateConfig]); A2AAgentSession + A2AContinuationToken (A2A session management — _CONTEXT_ID_KEY/task_id/task_state slots, service_session_id=context_id, input-required continuation detection, to_dict/from_dict round-trip); ThreadItemConverter (ChatKit→agent-framework bridge — attachment_data_fetcher async callback, UserMessageItem→Message dispatch, to_agent_input() entry point, quoted_text last-message injection); AgentApprovalExecutor + AgentRequestInfoExecutor + AgentRequestInfoResponse (orchestration approval gate — allow_direct_output terminal mode, @handler/@response_handler routing pair, AgentRequestInfoResponse.approve() empty-messages shortcut); AgentFrameworkWorkflow (workflow-to-AG-UI bridge — workflow vs workflow_factory mutex, (scope,thread_id) cache key, snapshot_store requires scope resolver); FlowState + AGUIHttpService + run_workflow_stream (run-level streaming state — 14 dataclass fields incl. reasoning + interrupts, AGUIHttpService SSE parsing + resume, run_workflow_stream UUID generation + normalize_agui_input_messages) — 10 class groups source-verified at agent-framework 1.9.0." />
  <LinkCard title="Class deep dives Vol. 29 — 1.10.0 / agent-framework-ag-ui 1.0.0rc7" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v29/" description="SecureMCPToolProxy + apply_mcp_security_labels (FIDES MCP security proxy — wrap-or-URL init, AsyncClient static-header injection for initialize(), auto-label on connect, annotation_overrides per-tool, mark_write_tools_as_sinks, refresh_labels() for long-lived servers); BedrockGuardrailConfig + BedrockChatOptions.guardrailConfig (Bedrock content safety — guardrailIdentifier+guardrailVersion+trace+streamProcessingMode TypedDict, per-call and per-session wiring); GroupChatRequestMessage + GroupChatRequestSentEvent + GroupChatResponseReceivedEvent (observable group-chat events — additional_instruction envelope, round_index+participant_name for tracing); MagenticOrchestratorEvent + MagenticOrchestratorEventType + MagenticProgressLedgerItem (Magentic event taxonomy — PLAN_CREATED/REPLANNED/PROGRESS_LEDGER_UPDATED enum, Message|MagenticProgressLedger content union, reason+answer str|bool ledger item); ReleaseCandidateFeature + ExperimentalWarning + FeatureStageWarning (feature staging — RC enum empty in 1.10.0, ExperimentalWarning→FeatureStageWarning→FutureWarning hierarchy, CI filterwarnings pattern); ToolExecutionException + _MCPTaskAbandoned + _MCPDeadlineExpired (MCP error taxonomy — ToolException→ToolExecutionException→_MCPTaskAbandoned subclass chain, _MCPDeadlineExpired internal sentinel re-raised as abandoned); AgentMiddlewareLayer + ChatMiddlewareLayer (MRO mixin layers — run()/get_response() intercept, base+per-call merge, pipeline cache, ResponseStream.from_awaitable streaming bridge); DiscoveryResponse + EntityInfo + AgentFrameworkRequest + OpenAIError (DevUI protocol types — 18-field EntityInfo Pydantic model, model-as-entity_id routing, AgentFrameworkRequest conversation string/dict, OpenAIError.create() factory); GroupChatState + AgentOrchestrationOutput + create_completion_message + clean_conversation_for_handoff (orchestration utilities — frozen GroupChatState, strict forbid+required Pydantic output, terminal message factory, text-only handoff conversation cleaning); RawGitHubCopilotAgent + GitHubCopilotSettings + GitHubCopilotOptions (GitHub Copilot CLI agent — AGENT_PROVIDER_NAME='github.copilot', GITHUB_COPILOT_* env prefix, on_pre_tool_use/on_permission_request hook pair, BYOK provider config) — 10 class groups source-verified at agent-framework 1.10.0." />
  <LinkCard title="Class deep dives Vol. 30 — 1.10.0" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v30/" description="WorkflowViz (to_digraph/to_mermaid/export — include_internal_executors=True for routing internals, DOT+Mermaid+SVG/PNG/PDF, Jupyter rendering via save_svg+IPython.display); FunctionalWorkflow + FunctionalWorkflowAgent (@workflow run() streaming overloads + checkpoint_storage= per-run override + HITL responses=/checkpoint_id= resume, agent-compatible adapter with pending_requests dict); StepWrapper + RunContext (step caching by (step_name, call_index) — executor_bypassed vs live event pair, RunContext.request_info() HITL suspension + resume, get_state/set_state per-run KV, add_event custom events); CompactionProvider + ContextWindowCompactionStrategy (two-phase pipeline — before_strategy/after_strategy phases, DEFAULT_TOOL_EVICTION_THRESHOLD=0.5 / DEFAULT_TRUNCATION_THRESHOLD=0.8, keep_last_tool_call_groups, custom tokenizer); SlidingWindowStrategy + SelectiveToolCallCompactionStrategy + CharacterEstimatorTokenizer (targeted compaction — keep_last_groups + preserve_system, keep_last_tool_call_groups=0 removes all tool calls, 4-char/token heuristic default); AGUIChatClient + AGUIEventConverter (AG-UI chat integration — FunctionInvocationLayer+ChatMiddlewareLayer+ChatTelemetryLayer MRO stack, thread_id via additional_properties, convert_event 10+ event-type dispatch with per-run converter state); ClassSkill + AggregatingSkillsSource + FilteringSkillsSource + DelegatingSkillsSource (skills composition — @ClassSkill.resource/@ClassSkill.script auto-discovery decorators, fan-in via AggregatingSkillsSource, predicate filter, custom DelegatingSkillsSource subclass pattern); BackgroundAgentsProvider + BackgroundTaskInfo + BackgroundTaskStatus (background task delegation — 6 LLM-callable tools, source_id session key, wait_for_first_completion, continue_task resume, RUNNING/COMPLETED/FAILED/LOST status enum); TodoItem + TodoFileStore + TodoInput + TodoCompleteInput (structured todo harness — JSON-persisted TodoFileStore, TodoItem completed lifecycle, SessionContext per-session lock, TodoProvider 5-tool wiring: todos_add/todos_complete/todos_remove/todos_get_remaining/todos_get_all); AgentEvalConverter + ConversationSplitter + EvalResults + EvalItem + CheckResult (evaluation pipeline — convert_message typed content list, ConversationSplitter Protocol + custom splitter, EvalResults sub_results per-agent breakdown, EvalItem query/response derived properties, CheckResult pass/reason/check_name) — 10 class groups source-verified at agent-framework 1.10.0." />
  <LinkCard title="Class deep dives Vol. 31 — 1.10.0" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v31/" description="FileMemoryProvider (session-scoped file memory — 7 built-in tools: write/read/delete/ls/grep/replace/replace_lines, scope= for cross-session sharing, memories.md auto-index); create_harness_agent (batteries-included factory — TodoProvider + AgentModeProvider + FileMemoryProvider + FileAccessProvider + SkillsProvider + BackgroundAgentsProvider + ToolApprovalMiddleware + AgentLoopMiddleware + OTel all wired automatically, disable_* flags, loop_should_continue/loop_next_message); AgentLoopMiddleware advanced patterns (fresh_context snapshot/restore per iteration, return_final_only last-pass response, record_feedback progress log accumulation, inject_progress prepend log to next pass); ToolApprovalMiddleware + ToolApprovalRule + ToolApprovalState (standing rule pre-seeding, auto_approval_rules heuristic callbacks, ToolApprovalState.to_dict/from_dict round-trip across sessions); FileSkillsSource depth + filter predicates (search_depth 1–N levels, script_filter/resource_filter (skill_name, rel_path)->bool predicates — exclude tests, generated files, non-production resources; SkillsProvider.from_paths convenience wrapper); FoundryAgent (agent-framework-foundry — wraps existing Foundry PromptAgent/HostedAgent, agent_name+agent_version, WorkflowBuilder pipeline integration); AgentExecutorResponse.with_text() (text transform preserving full_conversation — prevents context chain break on custom executor transforms, immutable new instance, chaining safe); todos_remaining + background_tasks_running (loop helper factories — looping_modes= mode-gated todo loop, combined predicate pattern, todos_remaining_message/background_tasks_running_message next_message callables); custom ContextProvider + SessionContext.metadata (before_run/after_run lifecycle, extend_messages/extend_instructions/extend_tools/extend_middleware API, metadata dict for cross-provider communication, per-provider state dict); InMemoryCheckpointStorage (in-memory workflow checkpoint store — save/load/list_checkpoints/get_latest/delete/list_checkpoint_ids, pytest unit test patterns, resume from checkpoint_id) — 10 class groups source-verified at agent-framework 1.10.0." />
  <LinkCard title="Class deep dives Vol. 32 — 1.10.0" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v32/" description="evaluate_agent + evaluate_workflow + LocalEvaluator + EvalItem (evaluation orchestration — provider-agnostic Evaluator protocol, run+evaluate vs post-hoc responses= mode, num_repetitions, per-agent sub_results breakdown); EvalResults + EvalItemResult + EvalScoreResult + ConversationSplit + EvalNotPassedError (CI gate API — raise_for_status()/assert_score_at_least()/assert_dimension_score_at_least(), ConversationSplit.LAST_TURN/FULL built-in splitters, custom ConversationSplitter callable); keyword_check + tool_called_check + tool_calls_present + tool_call_args_match + @evaluator (local check builders — keyword_check case_sensitive=, tool_called_check mode=any/all, tool_calls_present name-only, tool_call_args_match subset arg match, @evaluator wraps sync/async functions with param injection); WorkflowEvent factory catalog (12 factory methods — started/status/failed/warning/error/request_info/superstep_started/superstep_completed/executor_invoked/executor_completed/executor_failed/executor_bypassed, AGENT_FORWARDED_EVENT_TYPES frozenset, WorkflowEventSource.FRAMEWORK/EXECUTOR origin context); WorkflowErrorDetails + WorkflowRunState + WorkflowEventSource (error/state types — WorkflowErrorDetails.from_exception() traceback capture, 7-value WorkflowRunState lifecycle, origin context var + _framework_event_origin() CM); compaction annotation constants + included_messages + included_token_count (GROUP_ANNOTATION_KEY/_ID/_KIND/_INDEX/_TOKEN_COUNT, EXCLUDED_KEY/EXCLUDE_REASON_KEY, SUMMARY_OF_* keys, COMPACTION_STATE_KEY — read/write compaction metadata, filter included messages, count tokens); background_tasks_running_message + todos_remaining_message (next_message callables — pair with loop predicates, return None when idle, resolve provider from agent.context_providers); ToolApprovalRuleCallback + create_always_approve_tool_response + create_always_approve_tool_with_arguments_response (approval protocol — Callable[[Content], bool|Awaitable[bool]] type alias, standing rules tool-scope vs tool+args-scope, async callback support); FileStoreEntry + DEFAULT_FILE_ACCESS_INSTRUCTIONS + DEFAULT_FILE_ACCESS_SOURCE_ID (file access entry type — FILE/DIRECTORY class vars, to_dict/from_dict round-trip, recursive directory walking, instruction customisation); BackgroundAgentsProvider + BackgroundTaskInfo + BackgroundTaskStatus + _RuntimeState (deep patterns — fan-out to multiple agents, continue_task follow-up on same session, LOST status on process restart, direct BackgroundTaskInfo.from_dict() session state inspection) — 10 class groups source-verified at agent-framework 1.10.0." />
  <LinkCard title="Class deep dives Vol. 33 — 1.10.0" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v33/" description="WorkflowBuilder 1.10.0 new API (add_chain/add_multi_selection_edge_group/intermediate_output_from); Workflow + WorkflowRunResult full result API (get_outputs/get_intermediate_outputs/status_timeline/get_final_state); FunctionTool advanced (max_invocations, declaration-only func=None, result_parser, input_model); Agent.run() overloads (structured output with Pydantic, stream=True, per-call middleware/compaction); MCPStdioTool advanced (allowed_tools, MCPSpecificApproval per-tool, additional_tool_argument_names, task_options); SecretString + load_settings (env_prefix, .env file, required_fields tuple constraint); FunctionInvocationConfiguration tool-loop control (max_function_calls, additional_tools, enabled=False); FileHistoryProvider + custom HistoryProvider ABC (session JSONL per-turn append, custom in-process dict backend); AgentMiddleware + @agent_middleware + MiddlewareTermination (caching, PII redaction, short-circuit); SkillsProvider + SkillsSource ABC + DeduplicatingSkillsSource (from_paths, custom remote source) — 10 class groups source-verified at agent-framework 1.10.0." />
  <LinkCard title="Class deep dives Vol. 34 — 1.10.0" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v34/" description="OpenAIChatCompletionOptions + Prediction + PredictionTextContent + RawOpenAIChatCompletionClient + OpenAIChatCompletionClient (Chat Completions API stateless layer); ReasoningOptions + StreamOptions + OpenAIContinuationToken (reasoning effort + streaming controls + background polling); OpenAIContentFilterException + ContentFilterResult + ContentFilterResultSeverity + ContentFilterCodes (Azure content filter error hierarchy); WorkflowOrchestrationContext Protocol (host-agnostic interface for Azure Functions + standalone durabletask); DurableWorkflowClient (external durable workflow client — start/stream/HITL); WorkflowRegistrationPlan + plan_workflow_registration (durable task registration plan helpers); TaskType + TaskMetadata + PendingHITLRequest + ExecutorResult (durable orchestrator scheduler internals); DurableTaskWorkflowContext (durabletask-native context — yield pattern, call_activity, HITL resume); ConversationStore + InMemoryConversationStore + CheckpointConversationManager (DevUI conversation store abstraction over OpenAI Conversations API); AgentTask + PreCompletedTask + AzureFunctionsAgentExecutor (Azure Functions serverless agent orchestration) — 10 class groups source-verified at agent-framework 1.10.0." />
  <LinkCard title="Class deep dives Vol. 35 — 1.10.0" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v35/" description="RawAgent (low-latency agent — Generic TOptions, streaming overload, tool use); ChatContext (chat middleware pipeline context — metadata/result mutation, PII redaction via stream_transform_hooks, caching short-circuit); SwitchCaseEdgeGroup + Case + Default (conditional workflow fan-out — add_switch_case_edge_group, ordered case evaluation, to_dict serialization); WorkflowContext (per-executor context — send_message/yield_output, source_executor_ids, OTel PRODUCER spans); AgentLoopMiddleware + JudgeVerdict (iterative agent loop — should_continue predicate, with_judge LLM judge, DEFAULT_MAX_ITERATIONS=10); MemoryStore + MemoryFileStore (abstract + file-backed durable memory — list/get/write/delete topics, export_provider_state/import_provider_state); MemoryContextProvider (memory-backed history — extraction_prompt JSON format, recent_turns, selection_limit); AgentModeProvider (plan/execute mode harness — mode_set/mode_get tools, {available_modes}/{current_mode} template); LocalEvaluator + EvalItem + EvalResults (local eval pipeline — EvalCheck callables, @evaluator wrapper, asyncio.gather parallel) — 9 class groups source-verified at agent-framework 1.10.0." />
  <LinkCard title="Class deep dives Vol. 40 — 1.11.0" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v40/" description="MagenticOrchestrator (two-level outer/inner loop — stall detection, require_plan_signoff HITL gate, MagenticOrchestratorEvent emission); MagenticContext + MagenticResetSignal (mutable state dataclass, round/stall/reset counts, DictConvertible round-trip); MagenticPlanReviewRequest + MagenticPlanReviewResponse (HITL plan approval — approve()/revise() helpers, iterative replanning); StandardMagenticManager + MagenticProgressLedger (LLM orchestration — 8 prompt overrides, 3 budget knobs, 5-field progress ledger); GroupChatOrchestrator (selection-function group chat — round-robin and keyword-stop patterns); ContentVariableStore + VariableReferenceContent (FIDES prompt-injection guard — var_{16hex} IDs, variable indirection); LabelTrackingFunctionMiddleware (3-tier label propagation — embedded > source_integrity > input join, auto_hide_untrusted); PolicyEnforcementFunctionMiddleware (policy gate — allow_untrusted_tools whitelist, block_on_violation, audit_log); FileMemoryProvider + FileStoreEntry (session-scoped file memory — 7 tools, scope= user grouping); AgentModeProvider (plan/execute mode switching — mode_set/mode_get, get_agent_mode/set_agent_mode helpers); GAIA + GAIATelemetryConfig + Task + TaskResult + Evaluator (GAIA benchmark — hf_token, file/OTLP tracing, custom Evaluator Protocol, run(task_runner, level, max_n, parallel)) — source-verified at agent-framework 1.11.0." />
  <LinkCard title="azure-ai-agents integration — class reference Vol. 1" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives/" description="Add-on: AgentsClient, FunctionTool, ToolSet, CodeInterpreterTool, FileSearchTool, BingGroundingTool, ConnectedAgentTool, AgentEventHandler, ThreadMessage, OpenApiTool — 10 source-verified classes." />
  <LinkCard title="azure-ai-agents integration — class reference Vol. 3" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v3/" description="Add-on: AsyncFunctionTool, AzureFunctionTool, AzureAISearchTool, VectorStore, ThreadRun, RunStep, ResponseFormatJsonSchema, TruncationObject, MessageAttachment, AsyncAgentEventHandler — 10 source-verified classes." />
  <LinkCard title="azure-ai-agents integration — class reference Vol. 4" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v4/" description="Add-on: enable_auto_function_calls, create_thread_and_process_run, FunctionTool, ToolSet, CodeInterpreterTool, FileSearchTool+VectorStore, AzureAISearchTool, BingGroundingTool, ConnectedAgentTool, AgentEventHandler, AsyncToolSet — 10 source-verified patterns." />
  <LinkCard title="azure-ai-agents integration — class reference Vol. 5" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v5/" description="Add-on: Agent model, AgentThread, ToolOutput, VectorStoreFileBatch + VectorStoreFileCount, VectorStoreFile + VectorStoreFileError, FileInfo, MessageDeltaChunk, RunStepDeltaChunk, SubmitToolOutputsAction, AgentRunStream + AgentEventHandler — 10 data model and streaming classes from source." />
  <LinkCard title="azure-ai-agents integration — class reference Vol. 6" href="/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_sdk_class_deep_dives_v6/" description="Add-on: OpenAPI auth hierarchy, BingGroundingSearchConfiguration, RunCompletionUsage, VectorStoreDataSource + VectorStoreConfigurations, expiry + chunking strategies, AgentsNamedToolChoice, file-search run step results, streaming event taxonomy, RequiredFunctionToolCall manual dispatch, multimodal ThreadMessageOptions — 40+ classes source-verified at 1.1.0." />
</CardGrid>

## What's shipped (July 2026 introspection)

- **Stable core APIs** — `agent-framework-core 1.10.0`. Upgraded from 1.9.0 on 2026-07-01. New in 1.10.0: `SecureMCPToolProxy`, `apply_mcp_security_labels` (FIDES MCP security proxy — experimental); `AgentMiddlewareLayer`, `ChatMiddlewareLayer` (reworked as public MRO mixin layers — supersedes Vol. 6 coverage); `DiscoveryResponse`, `EntityInfo`, `AgentFrameworkRequest`, `OpenAIError` promoted in DevUI module; `BedrockGuardrailConfig` refinements; `agent-framework-ag-ui` upgraded to `1.0.0rc7`. See [Class Deep Dives Vol. 29](/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v29/) for source-verified coverage of new 1.10.0 surface; [Class Deep Dives Vol. 30](/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v30/) for deep dives into `WorkflowViz`, functional workflow execution, compaction strategies, AG-UI chat integration, the skills composition system, background agent delegation, the todo harness, and the evaluation pipeline. Previously in 1.9.0 (2026-06-19): All public symbols verified against installed `agent-framework==1.9.0`; 275 total public symbols (+14 vs 1.8.1). New in 1.9.0: `ToolApprovalMiddleware`, `ToolApprovalRule`, `ToolApprovalState`, `AgentLoopMiddleware`, `JudgeVerdict`, `todos_remaining`, `background_tasks_running`, `SamplingApprovalCallback`, `to_prompt_agent`, `FoundryEmbeddingClient`, `FoundryEmbeddingOptions`, `FoundryEmbeddingSettings`, `RawFoundryEmbeddingClient`, plus 8 new `FoundryChatClient` hosted tool factory methods. See [Class Deep Dives Vol. 17](/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v17/) for source-verified coverage of all new 1.9.0 symbols. Previously stable in 1.8.1: 261 total public symbols. Previously new in 1.8.0: `AgentFileStore`, `FileSystemAgentFileStore`, `InMemoryAgentFileStore`, `FileAccessProvider`, `FileSearchResult`, `FileSearchMatch`, `MCPSkill`, `MCPSkillResource`, `MCPSkillsSource`, `ToolMode`, `AgentEvalConverter`, `CheckResult`, `ExpectedToolCall`, `RubricScore`, `ChatContext`, `WorkflowAgent`, `WorkflowContext`, `TruncationStrategy`, `HistoryProvider`, `InMemoryHistoryProvider`, `DelegatingSkillsSource`, `InMemorySkillsSource`, `FunctionInvocationContext`, `FunctionInvocationLayer`. See [Class Deep Dives Vol. 8](/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v8/) and [Vol. 10](/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_class_deep_dives_v10/) for source-verified coverage.
- **First-party chat clients** — `agent_framework.foundry.FoundryChatClient`, `agent_framework.openai.OpenAIChatClient`, `agent_framework.anthropic.AnthropicClient`, plus Bedrock / Ollama in the `1.0.0b` provider line.
- **Skills (experimental)** — `MemoryStore`, `SkillResource`, `InlineSkillResource`, `ClassSkill`, `InlineSkill`, `FileSkill` now emit `ExperimentalWarning` on import — do not depend on these APIs in production yet.
- **Middleware** — `@chat_middleware` / `@agent_middleware` / `@function_middleware` decorators and base classes; pass `middleware=[...]` (must be a list in 2026 releases).
- **Workflows** — `WorkflowBuilder` with `.add_edge` / `.add_chain` / `.add_fan_in_edges` / `.add_fan_out_edges` / `.add_switch_case_edge_group` / `.add_multi_selection_edge_group`; checkpoint via `FileCheckpointStorage` / `InMemoryCheckpointStorage`.
- **Functional workflows (experimental)** — `@workflow` / `@step` decorators and `RunContext` for HITL and state — write workflows as plain `async` Python functions without graph wiring.
- **Declarative agents (beta)** — `agent_framework.declarative.AgentFactory` / `WorkflowFactory` load from YAML.
- **A2A (beta)** — one class, `agent_framework.a2a.A2AAgent`, wraps a remote A2A endpoint as a local `Agent`.

<Aside type="caution" title="Breaking changes in 2026 releases">
  - `display_name` parameter removed from agents.
  - `middleware=` now requires a list — no longer accepts a single instance.
  - `AgentRunResponse` / `AgentRunResponseUpdate` renamed to `AgentResponse` / `AgentResponseUpdate`.
  - `AggregateContextProvider` removed — compose `ContextProvider` instances directly.
  - `context_providers` stays **plural** in `agent-framework-core==1.5.0` (verified against source). An earlier draft of this guide incorrectly said it was renamed to singular.
  - `output_executors=` in `WorkflowBuilder` is **deprecated** in 1.5.0 — replace with `output_from=`. The old parameter still works but emits a warning.
  - Migrating from standalone Semantic Kernel or AutoGen? See the [official guide](https://learn.microsoft.com/agent-framework/migration-guide/from-semantic-kernel/) — ~2–4 hours for a typical app.
</Aside>

## Install, minimal example

```python
import asyncio
from agent_framework import Agent
from agent_framework.openai import OpenAIChatClient

async def main():
    agent = Agent(
        client=OpenAIChatClient(),          # positional or keyword
        instructions="You are a helpful assistant.",
    )
    response = await agent.run("What is the Microsoft Agent Framework?")
    print(response.text)

asyncio.run(main())
```

> Previous drafts of this page used a `ChatAgent(chat_client=...)` minimal example. `ChatAgent` is not a public class in `agent-framework-core` 1.4.0 — the primary agent class is `Agent` and the keyword is `client=` (not `chat_client=`).

Ready for the full walk-through? **[Start with Core Fundamentals →](/microsoft-agent-framework-guide/python/microsoft_agent_framework_python_comprehensive_guide/#core-fundamentals)**

---

## Revision history

| Date | Version | Changes |
|------|---------|---------|
| 2026-07-09 | 1.3.8 | Added **Class Deep Dives Vol. 35** (source-verified at `agent-framework==1.10.0`): `RawAgent` (low-latency agent without middleware/telemetry — Generic `TOptions` IDE autocomplete, streaming overload, tool use); `ChatContext` (chat middleware pipeline context — `process(context, call_next)` abstract method, `metadata`/`stream_transform_hooks` mutable at runtime, PII redaction via transform hooks, caching short-circuit); `SwitchCaseEdgeGroup`+`SwitchCaseEdgeGroupCase`+`SwitchCaseEdgeGroupDefault`+`Case`+`Default` (conditional workflow fan-out — `WorkflowBuilder.add_switch_case_edge_group(source, [Case(...), Default(...)])` runtime API vs serialization form, `to_dict` inspection); `WorkflowContext` (per-executor context — `send_message`/`yield_output`, `source_executor_ids` full upstream list, `get_source_executor_id()` single-source shortcut, OTel PRODUCER spans); `AgentLoopMiddleware`+`JudgeVerdict` (iterative agent loop — `should_continue` predicate, fresh-context snapshots, `with_judge` LLM judge factory, `DEFAULT_MAX_ITERATIONS=10`/`DEFAULT_JUDGE_MAX_ITERATIONS=5`); `MemoryStore`+`MemoryFileStore` (abstract + file-backed durable memory — all topic APIs synchronous: `list_topics`/`get_topic`/`write_topic`/`delete_topic`/`rebuild_index`, owner path-traversal guard, `export_provider_state`/`import_provider_state`); `MemoryContextProvider` (memory-backed history — `extraction_prompt` JSON shape `{"memories":[{"topic":...,"memory":...}]}`, `recent_turns`, `selection_limit`, per-event-loop async locks); `AgentModeProvider` (plan/execute mode harness — `mode_set`/`mode_get` tools, `{available_modes}`/`{current_mode}` template, external `set_agent_mode` notification); `LocalEvaluator`+`EvalItem`+`EvalResults` (local eval pipeline — `EvalItem(conversation=[Message(...)])` constructor, `query`/`response` computed properties, variadic `EvalCheck` callables, `asyncio.gather` parallel, per-evaluator `result_counts` breakdown). Zero→Hero extended to step 57. Description bumped to 35 volumes / 350+ classes. |
| 2026-07-08 | 1.3.7 | Added **Class Deep Dives Vol. 34** (source-verified at `agent-framework==1.10.0`): `OpenAIChatCompletionOptions` + `Prediction` + `PredictionTextContent` + `RawOpenAIChatCompletionClient` + `OpenAIChatCompletionClient` (Chat Completions API stateless layer — `logprobs`, predicted outputs, Responses vs Completions design comparison); `ReasoningOptions` + `StreamOptions` + `OpenAIContinuationToken` (reasoning effort controls, streaming options, background polling continuation token); `OpenAIContentFilterException` + `ContentFilterResult` + `ContentFilterResultSeverity` + `ContentFilterCodes` (Azure content filter error hierarchy — 4-level severity enum, per-category `filtered`/`severity` results, single `ContentFilterCodes.RESPONSIBLE_AI_POLICY_VIOLATION` code constant); `WorkflowOrchestrationContext` Protocol (host-agnostic interface usable with both Azure Functions and standalone `durabletask` — `is_replaying`/`instance_id`/`current_utc_datetime`/`prepare_agent_task`/`prepare_activity_task`/`supports_event_streaming`); `DurableWorkflowClient` (external durable workflow client — `start_workflow`/`await_workflow_output`/`get_runtime_status`/`get_pending_hitl_requests`/`send_hitl_response`/`stream_workflow`); `WorkflowRegistrationPlan` + `plan_workflow_registration` (`plan_workflow_registration` introspects a `Workflow` and classifies `AgentExecutor` nodes as durable entities and other `Executor` nodes as activities; result is a `WorkflowRegistrationPlan(agent_executors, activity_executors, orchestrator_name)` dataclass read by each host); `TaskType` + `TaskMetadata` + `PendingHITLRequest` + `ExecutorResult` (durable orchestrator scheduler internals — task type enum, HITL request/response suspension, serializable executor result); `DurableTaskWorkflowContext` (durabletask-native orchestration context — `yield` pattern, `is_replaying`/`instance_id`/`prepare_agent_task`/`prepare_activity_task`/`create_timer`/`wait_for_external_event`/`set_custom_status`); `ConversationStore` + `InMemoryConversationStore` + `CheckpointConversationManager` (DevUI conversation store ABC — `create_conversation`/`add_items`/`list_items`/`get_item`/`delete_conversation` interface, thread-safe in-memory backend, per-conversation `InMemoryCheckpointStorage` via `CheckpointConversationManager`); `AgentTask` + `PreCompletedTask` + `AzureFunctionsAgentExecutor` (Azure Functions serverless agent orchestration — HTTP trigger wiring, pre-populated task results, `AgentFunctionApp` integration). Zero→Hero extended to step 56. |
| 2026-07-07 | 1.3.6 | Added **Class Deep Dives Vol. 33** (source-verified at `agent-framework==1.10.0`): `WorkflowBuilder` 1.10.0 new API (`add_chain`/`add_multi_selection_edge_group`/`intermediate_output_from`); `Workflow` + `WorkflowRunResult` full result API (`get_outputs`/`get_intermediate_outputs`/`status_timeline`/`get_final_state`; HITL resume via `responses={request_id: value}`); `FunctionTool` advanced (`max_invocations`, `func=None` declaration-only, `result_parser`, `input_model`); `Agent.run()` overloads (structured output via `response.value`, streaming, per-call `middleware=`/`compaction_strategy=`); `MCPStdioTool` advanced (`allowed_tools`, `MCPSpecificApproval` TypedDict, `additional_tool_argument_names` + explicit `function_invocation_kwargs` injection); `SecretString` + `load_settings` (`env_prefix`, `.env` file, `required_fields` mutually-exclusive constraint); `FunctionInvocationConfiguration` tool-loop control (`include_detailed_errors` field, `client.function_invocation_configuration` is the correct surface — not `function_invocation_kwargs`); `FileHistoryProvider` + `HistoryProvider` ABC (`get_messages`/`save_messages` interface, `context_providers=` wiring, `session.state[source_id]` layout); `AgentMiddleware` + `@agent_middleware` + `MiddlewareTermination` (correct pattern: set `ctx.result` before raising `MiddlewareTermination()`); `SkillsProvider` + `SkillsSource` ABC + `DeduplicatingSkillsSource` (`get_skills(self)` no-arg interface, positional source arg). Zero→Hero extended to step 55. |
| 2026-07-06 | 1.3.5 | Added **Class Deep Dives Vol. 32** (source-verified at `agent-framework==1.10.0`): `evaluate_agent` + `evaluate_workflow` + `LocalEvaluator` + `EvalItem` (evaluation subsystem — `@experimental(EVALS)`, `EvalItem.query`/`response` split semantics, `LocalEvaluator` as `Evaluator` protocol, `evaluate_workflow` per-agent breakdown, pre-cached response evaluation); `EvalResults` + `EvalItemResult` + `EvalScoreResult` + `ConversationSplit` + `EvalNotPassedError` (evaluation result types — `EvalResults.sub_results` mapping, `EvalItemResult.status`/`scores`/`error_code`, custom `ConversationSplitter` protocol, `EvalNotPassedError` CI gate, `raise_for_status()` shortcut); `keyword_check` + `tool_called_check` + `tool_calls_present` + `tool_call_args_match` + `@evaluator` (built-in check factories — `mode="any"` for at-least-one tool, `tool_calls_present`/`tool_call_args_match` standalone functions reading `item.expected_tool_calls`, `@evaluator` wrapping sync/async functions with named parameters); `WorkflowEvent` complete factory catalog — 12 class methods (`started`, `status`, `failed`, `warning`, `error`, `request_info`, `superstep_started`, `superstep_completed`, `executor_invoked`, `executor_completed`, `executor_failed`, `executor_bypassed`) + `AGENT_FORWARDED_EVENT_TYPES` frozenset `{"output","intermediate","data","request_info"}`; `WorkflowErrorDetails` + `WorkflowRunState` + `WorkflowEventSource` (error detail dataclass with `from_exception()`, 7-state `WorkflowRunState` lifecycle enum, `EXECUTOR`/`FRAMEWORK` source enum); compaction annotation constants + `included_messages` + `included_token_count` (`GROUP_ANNOTATION_KEY`, `EXCLUDED_KEY`, `EXCLUDE_REASON_KEY`, `GROUP_KIND_KEY`, `GROUP_TOKEN_COUNT_KEY`, `COMPACTION_STATE_KEY`, `included_messages()` filter helper, `included_token_count()` sum helper); `background_tasks_running_message` + `todos_remaining_message` (loop `NextMessageCallable` functions — lazy provider resolution from `agent.context_providers`, async `todos_remaining_message`, `None` return when no work pending); `ToolApprovalRuleCallback` + `create_always_approve_tool_response` + `create_always_approve_tool_with_arguments_response` (approval callback type alias `Callable[[Content], bool | Awaitable[bool]]`, standing-rule factory pair with optional `reason`); `FileStoreEntry` + `DEFAULT_FILE_ACCESS_INSTRUCTIONS` + `DEFAULT_FILE_ACCESS_SOURCE_ID` (directory-listing entry — `name`/`type` fields, `FILE`/`DIRECTORY` class var constants, `DEFAULT_FILE_ACCESS_SOURCE_ID="file_access"`, `DEFAULT_FILE_ACCESS_INSTRUCTIONS` injected by `FileAccessProvider`); `BackgroundAgentsProvider` + `BackgroundTaskInfo` deep patterns (6-tool LLM interface, `BackgroundTaskInfo` `SerializationMixin` fields, `LOST` status recovery, `continue_task` session resume). Zero→Hero extended to step 54. |
| 2026-07-03 | 1.3.4 | Added **Class Deep Dives Vol. 31** (source-verified at `agent-framework==1.10.0`): `FileMemoryProvider` (file-backed semantic memory — `AgentFileStore` wiring, `DEFAULT_SOURCE_ID="file_memory"`, `before_run` search + `after_run` consolidation lifecycle); `create_harness_agent` (opinionated harness factory — `client` first positional arg, all subsystems wired via kwargs: `todo_provider`, `mode_provider`, `loop_should_continue`, `loop_next_message`, `background_agents`, `skills_paths`, `auto_approval_rules`, `before_compaction_strategy`, `after_compaction_strategy`); `AgentLoopMiddleware` advanced patterns (fresh-context snapshot, `with_judge` class method with `CRITERIA_PLACEHOLDER`, `inject_progress` session vs no-session, `return_final_only=False` aggregation); `ToolApprovalMiddleware` + `ToolApprovalRule` + `ToolApprovalState` (standing rule lifecycle, argument wildcard matching, `server_label` MCP scoping, `SerializationMixin` round-trip); `FileSkillsSource` depth + filter predicates (`search_depth=2` two-level scan, `script_filter`/`resource_filter` predicates, `resource_extensions`/`script_extensions` override); `FoundryAgent` (PromptAgent vs HostedAgent mode, `isolation_key`, `allow_preview`); `AgentExecutorResponse.with_text()` (conversation-context preservation shortcut); `todos_remaining` + `background_tasks_running` (loop predicates — `ShouldContinueCallable` wrappers, session-state resolution); custom `ContextProvider` + `SessionContext.metadata` (authoring guide — `before_run`/`after_run`, provider-scoped state, `metadata` dict for cross-provider coordination); `InMemoryCheckpointStorage` (in-memory backend — full 6-method ABC, vs `FileCheckpointStorage` comparison, HITL resume example). Zero→Hero extended to step 53. |
| 2026-07-02 | 1.3.3 | Added **Class Deep Dives Vol. 30** (source-verified at `agent-framework==1.10.0`): `WorkflowViz` (workflow graph visualization — `to_digraph(include_internal_executors=False)` DOT output, `to_mermaid()` flowchart syntax, `export(format, filename)` SVG/PNG/PDF/DOT dispatch with `graphviz` Python package, `save_svg`/`save_png`/`save_pdf` convenience savers + `IPython.display.SVG` for Jupyter rendering); `FunctionalWorkflow` + `FunctionalWorkflowAgent` (`@workflow` decorator — at-most-one non-`RunContext` parameter validation, streaming `run()` overloads, `checkpoint_storage=` per-run override, HITL resume via `responses=` + `checkpoint_id=`, `FunctionalWorkflowAgent` adapter with `pending_requests` dict); `StepWrapper` + `RunContext` (per-step caching by `(step_name, call_index)` — `executor_bypassed` event on cache hit vs `executor_invoked`/`executor_completed`/`executor_failed` on live run, `RunContext.request_info()` HITL suspension/resume, `get_state()`/`set_state()` per-run KV store, `add_event()` custom event emission); `CompactionProvider` + `ContextWindowCompactionStrategy` (two-phase compaction pipeline — `before_strategy` pre-model / `after_strategy` post-run phases, `DEFAULT_TOOL_EVICTION_THRESHOLD=0.5` / `DEFAULT_TRUNCATION_THRESHOLD=0.8`, `keep_last_tool_call_groups=4`, custom tokenizer override); `SlidingWindowStrategy` + `SelectiveToolCallCompactionStrategy` + `CharacterEstimatorTokenizer` (targeted compaction — `keep_last_groups` + `preserve_system=True`; `keep_last_tool_call_groups=0` removes all tool-call groups; `max(1, len(text) // 4)` 4-char/token heuristic); `AGUIChatClient` + `AGUIEventConverter` (AG-UI chat integration — `FunctionInvocationLayer`→`ChatMiddlewareLayer`→`ChatTelemetryLayer`→`BaseChatClient` MRO stack, `thread_id` continuity via `additional_properties`, `AGUIEventConverter.convert_event()` dispatch for 10+ AG-UI event types including `RUN_STARTED`/`TEXT_MESSAGE_CONTENT`/`TOOL_CALL_START`/`TOOL_CALL_ARGS`/`TOOL_CALL_END`); `ClassSkill` + `AggregatingSkillsSource` + `FilteringSkillsSource` + `DelegatingSkillsSource` (`@ClassSkill.resource(name=)` / `@ClassSkill.script(name=)` auto-discovery decorators, fan-in via `AggregatingSkillsSource([sources])`, predicate filter via `FilteringSkillsSource(inner_source, predicate)`, custom decorator base via `DelegatingSkillsSource`); `BackgroundAgentsProvider` + `BackgroundTaskInfo` + `BackgroundTaskStatus` (background task delegation — 6 LLM-callable tools: `start_task`/`wait_for_first_completion`/`get_task_results`/`get_all_tasks`/`continue_task`/`clear_completed_task`; `source_id` session key; `BackgroundTaskStatus.LOST` for process-restart scenarios; `BackgroundTaskInfo` `SerializationMixin` slot class); `TodoItem` + `TodoFileStore` + `TodoInput` + `TodoCompleteInput` (structured todo harness — `TodoFileStore` JSON-persisted to disk, `TodoItem(id, title, description=None, is_complete=False)`, `TodoInput(title, description=None)` / `TodoCompleteInput(id, reason)` DTO pair, `TodoProvider` 5-tool wiring: `todos_add`/`todos_complete`/`todos_remove`/`todos_get_remaining`/`todos_get_all` with per-session asyncio lock via `WeakKeyDictionary`); `AgentEvalConverter` + `ConversationSplitter` + `EvalResults` + `EvalItem` + `CheckResult` (evaluation pipeline — `AgentEvalConverter.convert_message()` typed content list, `ConversationSplitter` `(list[Message]) → tuple` Protocol + custom splitter pattern, `EvalResults.sub_results` per-agent workflow breakdown, `EvalItem.query`/`EvalItem.response` derived from split strategy, `CheckResult(passed, reason, check_name)` local check outcome). Zero→Hero extended to step 52; Zero→Hero and Reference grids updated. Description bumped to 30 volumes / 300+ classes. |
| 2026-07-01 | 1.3.2 | Added **Class Deep Dives Vol. 29** (source-verified at `agent-framework==1.10.0` / `agent-framework-ag-ui==1.0.0rc7`): `SecureMCPToolProxy` + `apply_mcp_security_labels` (FIDES experimental MCP security proxy — wrap-or-URL init, `AsyncClient` static-header injection covers `session.initialize()`, auto-label on connect, `annotation_overrides` per-tool, `mark_write_tools_as_sinks`, `refresh_labels()` for long-lived connections, per-tool `_wrap_mcp_function_for_ifc` wraps dynamic `_meta.ifc` payloads); `BedrockGuardrailConfig` + `BedrockChatOptions.guardrailConfig` (Amazon Bedrock content safety — `guardrailIdentifier`/`guardrailVersion`/`trace`/`streamProcessingMode` TypedDict, `"sync"` vs `"async"` blocking semantics, per-call or `default_options` wiring); `GroupChatRequestMessage` + `GroupChatRequestSentEvent` + `GroupChatResponseReceivedEvent` (observable group-chat events — `additional_instruction` envelope for per-round guidance injection, `round_index`/`participant_name` for tracing/latency measurement); `MagenticOrchestratorEvent` + `MagenticOrchestratorEventType` + `MagenticProgressLedgerItem` (Magentic event stream — `PLAN_CREATED`/`REPLANNED`/`PROGRESS_LEDGER_UPDATED` enum, `content: Message | MagenticProgressLedger` union, ledger item `answer: str | bool` with `from_dict` fallback to `""`); `ReleaseCandidateFeature` + `ExperimentalWarning` + `FeatureStageWarning` (feature staging hierarchy — RC enum intentionally empty in 1.10.0, `ExperimentalWarning → FeatureStageWarning → FutureWarning`, CI `filterwarnings("error", category=ExperimentalWarning)` pattern); `ToolExecutionException` + `_MCPTaskAbandoned` + `_MCPDeadlineExpired` (MCP error taxonomy — `ToolException → ToolExecutionException → _MCPTaskAbandoned` subclass chain, `_MCPDeadlineExpired` internal non-`ToolException` sentinel re-raised as `_MCPTaskAbandoned` after cancellation protocol); `AgentMiddlewareLayer` + `ChatMiddlewareLayer` (MRO mixin layers — `AgentMiddlewareLayer.run()` categorizes/merges base+per-call middleware, bypasses pipeline when `has_middlewares=False`, `ResponseStream.from_awaitable` streaming bridge; `ChatMiddlewareLayer.get_response()` pops `middleware` from `client_kwargs`, caches pipeline via `matches()`); `DiscoveryResponse` + `EntityInfo` + `AgentFrameworkRequest` + `OpenAIError` (DevUI protocol types — 18-field `EntityInfo` Pydantic model incl. `deployment_supported`/`required_env_vars`/`workflow_dump`, `AgentFrameworkRequest` uses `model` as entity ID + `conversation` string/dict, `OpenAIError.create()` factory method); `GroupChatState` + `AgentOrchestrationOutput` + `create_completion_message` + `clean_conversation_for_handoff` (orchestration utilities — `frozen=True` `GroupChatState` dataclass with `OrderedDict` participants, `AgentOrchestrationOutput` strict `extra="forbid"` + all fields in `json_schema_extra.required` for OpenAI strict mode, `create_completion_message` stores `reason` in `additional_properties`, `clean_conversation_for_handoff` strips tool-control content + drops zero-text messages); `RawGitHubCopilotAgent` + `GitHubCopilotSettings` + `GitHubCopilotOptions` (GitHub Copilot CLI agent — `AGENT_PROVIDER_NAME="github.copilot"`, 5-key `GitHubCopilotSettings` with `GITHUB_COPILOT_*` env prefix, `on_pre_tool_use`/`on_permission_request` hook pair, deprecated `on_function_approval` mutually exclusive with `on_pre_tool_use`, BYOK `provider` config for Azure/OpenAI/Anthropic routing, `instruction_directories` for team-shared prompts). Zero→Hero extended to step 51; Zero→Hero and Reference grids updated. Description bumped to 1.10.0 / 29 volumes / 290+ classes. |
| 2026-06-30 | 1.3.1 | Added **Class Deep Dives Vol. 28** (source-verified at `agent-framework==1.9.0` / `agent-framework-claude==1.0.0b260609` / `agent-framework-ag-ui==1.0.0rc5`): `RawClaudeAgent` + `ClaudeAgent` + `ClaudeAgentOptions` + `ClaudeAgentSettings` (Claude Code CLI integration — `AGENT_PROVIDER_NAME="anthropic.claude"`, `ClaudeSDKClient` owns-client lifecycle, `PermissionMode` literals `"default"`/`"acceptEdits"`/`"plan"`/`"bypassPermissions"`, `SandboxSettings`, `ClaudeAgentSettings` env-prefix `CLAUDE_AGENT_*` resolution); `AgentFrameworkAgent` + `AgentConfig` (AG-UI protocol wrapper for any `SupportsAgentRun` — `state_schema` Pydantic dispatch, `predict_state_config` tool-argument interception, `require_confirmation` gate, `snapshot_store` wiring); `AGUIThreadSnapshot` + `AGUIThreadSnapshotStore` + `InMemoryAGUIThreadSnapshotStore` (replayable thread state — `messages`+`state`+`interrupt` triad, Protocol async `save`/`get`/`delete`, `max_snapshots` LRU eviction, `(scope, thread_id)` composite key, deep-copy on save); `PredictiveStateHandler` + `PredictStateConfig` (streaming tool-call predictive state — `predict_state_config` dict, `streaming_tool_args` accumulator, `state_delta_count` throttle, `pending_state_updates` flush); `AGUIRequest` + `AGUIChatOptions` + `AgentState` + `RunMetadata` (AG-UI protocol Pydantic types — `AliasChoices` camelCase aliases, client-side tools forwarding, `thread_id` metadata continuity, `predict_state` list); `A2AAgentSession` + `A2AContinuationToken` (A2A session management — `_CONTEXT_ID_KEY`/`task_id`/`task_state` slot keys, `service_session_id=context_id`, `TaskState` input-required continuation detection, round-trip serialisation); `ThreadItemConverter` (ChatKit→agent-framework bridge — `attachment_data_fetcher` async callback, `UserMessageItem` dispatch, `to_agent_input()` entry point, quoted-text injection); `AgentApprovalExecutor` + `AgentRequestInfoExecutor` + `AgentRequestInfoResponse` (orchestration approval gate — `allow_direct_output` terminal mode, `@handler`/`@response_handler` routing pair, `AgentRequestInfoResponse.approve()` shortcut); `AgentFrameworkWorkflow` (workflow-to-AG-UI bridge — `workflow` vs `workflow_factory` mutex, `(scope, thread_id)` dict cache key, `snapshot_store` activation guard); `FlowState` + `AGUIHttpService` + `run_workflow_stream` (run-level streaming state — 14-field `@dataclass` incl. `reasoning` + `interrupts`, `AGUIHttpService` SSE parsing + resume, `run_workflow_stream` UUID generation + input normalisation). Zero→Hero extended to step 50; Zero→Hero and Reference grids updated. |
| 2026-06-27 | 1.3.0 | Added **Class Deep Dives Vol. 25** (source-verified at `agent-framework-declarative==1.0.0rc2` / `agent-framework==1.9.0`): `FunctionTool` + `OpenApiTool` + `WebSearchTool` + `FileSearchTool` + `CodeInterpreterTool` + `Binding` (5 non-MCP tool subclasses — Binding.name selects the callable; Binding.input is not read by AgentFactory; `FileSearchTool` `scoreThreshold`/`ranker`; `CodeInterpreterTool` `fileIds` list; `OpenApiTool` `specification`+`connection` (data-model only — `AgentFactory` raises `ValueError` for `openapi` kind); all dispatched by `SerializationMixin.from_dict` kind key); `AgentFactory` + `DeclarativeLoaderError` + `ProviderLookupError` + `ProviderTypeMapping` (declarative agent loader — built-in provider table maps provider string → `(chat_client_class, settings_class)`; `ProviderTypeMapping` `TypedDict` for custom registration via `additional_mappings`; `safe_mode` gates PowerFx `Env.*`/`os.environ` access in expressions; `create_agent_from_yaml`/`create_agent_from_yaml_path` dual API; `env_file_path` dotenv injection); `WorkflowFactory` + `DeclarativeWorkflowBuilder` (workflow from YAML path/string/dict — agent/tool pre-registered registries; `max_iterations`/`maxTurns` 3-tier precedence: factory kwarg > YAML `maxTurns` > default 100; `http_request_handler` + `mcp_tool_handler` build-time guard raises if not provided when actions present; `DeclarativeEnvConfig` `restrict_to_configuration`; `FileCheckpointStorage` wiring); `QuestionExecutor` + `RequestExternalInputExecutor` + `ExternalInputRequest` + `ExternalInputResponse` (declarative HITL — `QuestionExecutor` adds `choices` + `allowFreeText` to `ExternalInputRequest`; `requestType` discriminator for typed resume; `request_id` round-trip via `ctx.request_info()`; `response_handler` wiring; resume via `workflow.run(responses={request_id: ExternalInputResponse(...)})`); `HttpRequestActionExecutor` (HTTP action dispatch — all params PowerFx-evaluated from state; JSON body coercion; `responseHeaders` multi-value comma-join; appends `Tool`-role `Message` to conversation; wraps 4xx/5xx as `DeclarativeActionError`; propagates `asyncio.CancelledError` unchanged); `InvokeMcpToolActionExecutor` + `MCPToolApprovalRequest` (MCP tool invocation — `requireApproval` gate yields `MCPToolApprovalRequest` then resumes; `is_error` field is non-raising contract — result stored as Tool-role message regardless; `autoSend` controls conversation-append); `BaseToolExecutor` + `InvokeFunctionToolExecutor` + `ToolApprovalRequest` + `ToolApprovalResponse` + `ToolInvocationResult` (dual tool registry lookup: factory registry `self._tools` first, then `State[FUNCTION_TOOL_REGISTRY_KEY]` fallback; async support via `inspect.isawaitable(result)`; approval gate yields `ToolApprovalRequest` then resumes; `ToolInvocationResult.rejected: bool` + `rejection_reason: str | None`); `JoinExecutor` + `EndWorkflowExecutor` + `EndConversationExecutor` + `CancelDialogExecutor` + `CancelAllDialogsExecutor` (`JoinExecutor.handle_action` accepts multiple message types as trigger then sends `ActionComplete()`; `EndWorkflowExecutor`/`EndConversationExecutor` both `pass` in `handle_action` — dead-end by silence halts the graph; `CancelDialogExecutor`/`CancelAllDialogsExecutor` same dead-end pattern, distinguished by scope); `ActionComplete` + `ActionTrigger` + `DeclarativeStateData` + `ConversationData` (inter-executor message contracts; `DeclarativeStateData` 8-key `TypedDict` — `Inputs`/`Outputs`/`Local`/`System`/`Agent`/`Conversation`/`Custom`/`_declarative_loop_state`; `ConversationData.history` docstring explicitly marks it as `Deprecated: use messages instead`; `ActionTrigger` carries the triggering message type for conditional dispatch); `ClearAllVariablesExecutor` + `EditTableExecutor` + `ResetVariableExecutor` + `SetTextVariableExecutor` (`ClearAllVariablesExecutor` wipes all Local-scope variables in one call; `EditTableExecutor` supports `"add"` append / `"insert"` at index / `"remove"` by value / `"clear"` empty — operation defaults to `"add"`; `ResetVariableExecutor` sets target to `None`; `SetTextVariableExecutor` coerces value to `str` before assignment). Zero→Hero extended to step 47; Jump-to-topic, Zero→Hero, and Reference grids updated. Description bumped to 25 volumes / 260+ classes. |
| 2026-06-23 | 1.2.8 | Added **Class Deep Dives Vol. 21** (source-verified at `agent-framework==1.9.0`): `WorkflowContext[OutT, W_OutT]` (per-executor execution context — `send_message`/`yield_output`/`request_info` API, state R/W with `commit`/`discard`, `source_executor_ids` fan-in list, `is_streaming` flag, `current_agent_name`); `FanInEdgeGroup` + `FanOutEdgeGroup` (converging/broadcasting edge groups — `min_sources`/`min_targets` positive-value validation, `selection_func` dynamic routing callback, `to_dict`/`from_dict` serialization round-trip); `SwitchCaseEdgeGroup` + `SwitchCaseEdgeGroupCase` + `SwitchCaseEdgeGroupDefault` (switch/case routing — sequential predicate evaluation, exactly-one-default invariant, missing-condition placeholder raises `RuntimeError` on call, condition re-registration pattern); `TokenBudgetComposedStrategy` (multi-strategy composition — `early_stop` short-circuit, `annotate_message_groups` pipeline pass-through, strict-budget double-fallback with final truncation); `SelectiveToolCallCompactionStrategy` + `ToolResultCompactionStrategy` (tool history reduction — `exclude` list vs inline summary replacement, `keep_last_tool_call_groups` sliding retention, `exclude_tools`/`include_tools` selector); `SlidingWindowStrategy` + `SummarizationStrategy` (sliding window + LLM-backed summarization — `preserve_system`, `target_count`+`threshold` trigger, bidirectional summary trace metadata injection); `StepWrapper` + `FunctionalWorkflow` + `RunContext` (functional workflow internals — per-step caching by `call_index`, `executor_bypassed` event on cache hit, `checkpoint_storage` per step, `as_agent()` wrapper, `RunContext.set_state`/`get_state`); `MCPWebsocketTool` + `MCPStreamableHTTPTool` (MCP network transports — WebSocket `wss://` reconnect loop, streamable HTTP/SSE with `header_provider` ContextVar injection, shared `MCPTool` base); `MCPTaskOptions` (SEP-2663 long-running task lifecycle — `tasks/call` → `tasks/get` polling → `tasks/result`, `default_ttl` positive-value validation, `max_task_wait`, `cancel_remote_task_on_local_cancellation`, fire-and-forget pattern); `AgentResponseUpdate` + `ChatResponseUpdate` + `ContinuationToken` (streaming chunk types — `.text` property, `continuation_token` for background polling resumption, `author_name` in multi-agent streams, `finish_reason`). Zero→Hero extended to step 43; Jump-to-topic, Zero→Hero, and Reference grids updated. Description bumped to 21 volumes / 220+ classes. |
| 2026-06-22 | 1.2.7 | Added **Class Deep Dives Vol. 20** (source-verified at `agent-framework==1.9.0`): 6 hosted-tool capability Protocols (`SupportsCodeInterpreterTool`, `SupportsWebSearchTool`, `SupportsImageGenerationTool`, `SupportsMCPTool`, `SupportsFileSearchTool`, `SupportsShellTool` — all `@runtime_checkable`, provider-agnostic `isinstance` guard + factory method call patterns, custom client satisfying multiple Protocols); `SupportsGetEmbeddings` (typed embedding duck-typing Protocol — `get_embeddings` with contravariant input TypeVar, covariant `EmbeddingT`, opaque `EmbeddingProtocolOptionsT`, `additional_properties` required attribute, `isinstance` guard); `ReleaseCandidateFeature` + `FeatureStageWarning` + `ExperimentalWarning` (feature staging system — `_WARNED_FEATURES` set deduplication per `(category, feature_id)`, `_install_feature_stage_formatter` single-line formatter, `ExperimentalWarning` subclasses `FeatureStageWarning` subclasses `FutureWarning`, `ReleaseCandidateFeature` empty in 1.9.0, `_FEATURE_STAGE_ATTR`/`_FEATURE_ID_ATTR` runtime introspection, `pytest` `filterwarnings("error", ...)` CI pattern); `EmbeddingGenerationOptions` + `Embedding[T]` + `GeneratedEmbeddings[T, O]` (embedding DTO layer — `EmbeddingGenerationOptions` TypedDict with `model`/`dimensions`, `Embedding` Generic `dimensions` auto-computed from `len(vector)`, `GeneratedEmbeddings` extends `list[Embedding[T]]` with `options`/`usage`/`additional_properties`, cosine similarity and int8 quantization examples); `WorkflowEventSource` (EXECUTOR vs FRAMEWORK str-Enum — travels in `WorkflowEvent.source`, OTel span attribute usage, `match` dispatch pattern); `SubWorkflowRequestMessage` + `SubWorkflowResponseMessage` (`@dataclass` hierarchical HITL bridge — `create_response(data)` validates `isinstance(data, source_event.response_type)` raises `TypeError`, `executor_id` for multi-instance parent routing, `WorkflowExecutor` auto-appends `SubWorkflowRequestMessage` to output types); `RequestInfoMixin` + `response_handler` decorator (`_discover_response_handlers` scans class at `Executor.__init__`, `is_request_response_capable` bool, `is_request_supported(request_type, response_type)`, `response_handler` introspection vs explicit type mode with string forward reference support, duplicate `(request_type, response_type)` raises `ValueError`); `WorkflowAgent.RequestInfoFunctionArgs` (nested `@dataclass` — `request_id`/`request_event` fields, `to_dict()`/`from_dict()` JSON round-trip, `from_dict` raises `ValueError` on missing fields or empty `request_id`, `REQUEST_INFO_FUNCTION_NAME = "request_info"` class constant, middleware interceptor pattern); `EdgeGroupDeliveryStatus` (`Enum` with 6 members: `DELIVERED`/`BUFFERED`/`DROPPED_TYPE_MISMATCH`/`DROPPED_CONDITION_FALSE`/`DROPPED_TARGET_MISMATCH`/`EXCEPTION` — OTel `SpanProcessor` aggregation, production exception delivery alert); `IntegrityLabel` + `LabelTrackingFunctionMiddleware` (`@experimental(FIDES)` — `IntegrityLabel.TRUSTED`/`UNTRUSTED` str-Enum, 3-tier priority table for label propagation (embedded > `source_integrity` > input join), `LabelTrackingFunctionMiddleware(default_integrity, default_confidentiality, auto_hide_untrusted=True, hide_threshold)`, `get_context_label()`/`reset_context_label()`/`get_security_tools()`/`list_variables()`/`get_variable_metadata(var_id)`); `MiddlewareTermination` + `WorkflowConvergenceException` (`MiddlewareTermination(message, *, result=None)` raises to short-circuit middleware chain with optional early-return payload; `WorkflowConvergenceException(message, inner_exception, log_level=10)` raised when runner exceeds max iterations — blocklist middleware example, production convergence guard). Zero→Hero extended to step 42; Jump-to-topic, Zero→Hero, and Reference grids updated. Description bumped to 20 volumes / 210+ classes. |
| 2026-06-19 | 1.2.6 | Upgraded `agent-framework` **1.8.1 → 1.9.0** (275 total public symbols; +14 new). Added **Class Deep Dives Vol. 17** (source-verified at `agent-framework==1.9.0`): `ToolApprovalMiddleware` + `ToolApprovalRule` + `ToolApprovalState` + helper functions (`create_always_approve_tool_response`, `create_always_approve_tool_with_arguments_response`) — interactive tool approval gate with standing rules, argument wildcard matching (`arguments=None`), no-argument rule (`arguments={}`), `server_label` scoping for hosted tools, `SerializationMixin` round-trip; `AgentLoopMiddleware` + `JudgeVerdict` + `todos_remaining` + `background_tasks_running` — LLM-judged self-improvement loop with `DEFAULT_MAX_ITERATIONS=10`, `DEFAULT_JUDGE_MAX_ITERATIONS=5`, `JUDGE_VERDICT_DONE`/`JUDGE_VERDICT_MORE` sentinels, `fresh_context` snapshot via `to_dict()`, `inject_progress` session vs. no-session behaviour, `with_judge` class method with `CRITERIA_PLACEHOLDER="{{criteria}}"`, `return_final_only=False` aggregation; `SamplingApprovalCallback` + new MCP sampling security parameters — confused-deputy mitigation, deny-by-default, token cap, per-connection request counter, async callback support; `to_prompt_agent` (`@experimental(feature_id=ExperimentalFeature.TO_PROMPT_AGENT)`) — raises `TypeError` if client is not `RawFoundryChatClient`, model resolved from `agent.default_options.get("model") or agent.client.model`, local `MCPTool` instances raise `ValueError`; `FoundryEmbeddingClient` + `FoundryEmbeddingOptions` + `FoundryEmbeddingSettings` + `RawFoundryEmbeddingClient` — multimodal text+image embeddings, mixed batch split/reassemble, image detection via `"image/"` MIME prefix, `image_model` precedence, env vars `FOUNDRY_EMBEDDING_MODEL`/`FOUNDRY_IMAGE_EMBEDDING_MODEL`/`FOUNDRY_MODELS_ENDPOINT`; `ContentUnderstandingContextProvider` + `AnalysisSection` + `DocumentStatus` (`agent-framework-azure-contentunderstanding` package) — `MEDIA_TYPE_ANALYZER_MAP` auto-detect analyzer, `DocumentStatus` ANALYZING/UPLOADING/READY/FAILED enum, `deferred=True` for background analysis, auto-registered `list_documents` tool; `FileSearchConfig` + `FileSearchBackend` + `OpenAIFileSearchBackend` + `FoundryFileSearchBackend` — `FileSearchConfig.from_openai()`/`from_foundry()` static factories, `_OpenAICompatBackend` shared upload/delete surface, `create_and_poll` indexing wait, custom backend ABC; `AgentFrameworkTracer` (`agent_framework.lab.lightning`) — `AgentOpsTracer` subclass, `OBSERVABILITY_SETTINGS.enable_otel` toggle in `init()`/`teardown()` lifecycle; `TaskRunner` + `patch_env_set_state` + `unpatch_env_set_state` (`agent_framework.lab.tau2`) — three-part conversation assembly (initial greeting + full message store + final user message), `reinit()` returns `self`, `TerminationReason.MAX_STEPS`/`AGENT_STOP`/`USER_STOP`/`TOO_MANY_ERRORS`, `evaluate()` via `EvaluationType.ALL`; 8 new `FoundryChatClient` hosted tool factory methods (`get_mcp_tool`, `get_azure_ai_search_tool`, `get_sharepoint_tool`, `get_fabric_tool`, `get_memory_search_tool`, `get_computer_use_tool`, `get_browser_automation_tool`, `get_a2a_tool`) — each with validation rules and hosted vs. local MCP distinction. Zero→Hero extended to step 39; Jump-to-topic and Reference grids updated. Version card and description bumped to 1.9.0. |
| 2026-06-18 | 1.2.5 | Added **Class Deep Dives Vol. 16** (source-verified at `agent-framework==1.8.1`): `FoundryAgent` + `FoundryAgentOptions` + `RawFoundryAgent` + `RawFoundryAgentChatClient` (`agent_framework.foundry` hosted agents — PromptAgent requires `agent_version`, HostedAgent uses `allow_preview=True`, `isolation_key` per-session typed option, `client_type=RawFoundryAgentChatClient` override pattern), `FoundryLocalClient` + `FoundryLocalChatOptions` + `FoundryLocalSettings` (on-device LLM via Foundry Local SDK — `bootstrap`, `prepare_model`, `device` NPU/GPU/CPU, `FOUNDRY_LOCAL_MODEL` env var, structured Pydantic output via `response_format=`), `FoundryMemoryProvider` (Azure AI Foundry Memory Store semantic memory context provider — `memory_store_name`/`scope`/`update_delay`, `DEFAULT_SOURCE_ID="foundry_memory"`, `before_run` search + `after_run` async write lifecycle, async context manager for `AIProjectClient` ownership), `FoundryEvals` + `GeneratedEvaluatorRef` + `evaluate_foundry_target` + `evaluate_traces` (Foundry cloud eval harness — 19 source-verified built-in evaluator name constants across agent-behavior/tool-usage/quality/safety categories, smart default `relevance`+`coherence`+`task_adherence`, auto-add `tool_call_accuracy` when tools present, `poll_interval=5.0`/`timeout=180.0`, `GeneratedEvaluatorRef(name=..., version=...)` for rubric evaluators), `BedrockChatClient` + `BedrockChatOptions` + `BedrockGuardrailConfig` + `BedrockSettings` (Amazon Bedrock Converse API — `BEDROCK_REGION` default `"us-east-1"`, `BEDROCK_CHAT_MODEL` env var, `BedrockGuardrailConfig` TypedDict: `guardrailIdentifier`/`guardrailVersion`/`trace`/`streamProcessingMode`; 8 unsupported options declared as `None` typed), `BedrockEmbeddingClient` + `BedrockEmbeddingOptions` + `BedrockEmbeddingSettings` (Bedrock Titan Embeddings v2 — `OTEL_PROVIDER_NAME="aws.bedrock"`, `BEDROCK_EMBEDDING_MODEL` env var, `normalize: bool` for unit-length vectors, `MemoryContextProvider` wiring pattern), `MagenticManagerBase` (abstract base for custom Magentic-One managers — 4 abstract methods: `plan`/`replan`/`create_progress_ledger`/`prepare_final_answer`; `max_stall_count=3`/`max_reset_count`/`max_round_count`; `on_checkpoint_save()`→`dict`/`on_checkpoint_restore(state)` checkpoint hooks; wire with `MagenticBuilder(manager=...)`), `BaseGroupChatOrchestrator` + `GroupChatRequestSentEvent` + `GroupChatResponseReceivedEvent` (group chat abstract base — `max_rounds` coerced to `max(1, max_rounds)`, `TERMINATION_CONDITION_MET_MESSAGE`/`MAX_ROUNDS_MET_MESSAGE` class constants, 4 `@handler` overloads: `str`/`Message`/`list[Message]`/`AgentExecutorResponse`; `GroupChatRequestSentEvent(round_index, participant_name)` + `GroupChatResponseReceivedEvent(round_index, participant_name)` observability dataclasses), `AgentRequestInfoResponse` + `CacheProvider` (`AgentRequestInfoResponse` HITL approval data model — `approve()` pass-through / `from_messages(list[Message])` / `from_strings(list[str])` static factories; `CacheProvider` async `get`/`set(ttl_seconds)`/`remove` Protocol for Purview cache injection; in-memory test implementation pattern), Purview exception hierarchy + `acquire_token` (`PurviewAuthenticationError` 401/403 ← `IntegrationInvalidAuthException`, `PurviewPaymentRequiredError` 402, `PurviewRateLimitError` 429, `PurviewRequestError` other HTTP, `PurviewServiceError` base ← `IntegrationException`; `acquire_token()` MSAL `PublicClientApplication` silent-then-interactive flow, custom `scopes`, `SerializableTokenCache` on-disk pattern, exponential back-off retry). Zero→Hero extended to step 38; Jump-to-topic and Reference grids updated. |
| 2026-06-17 | 1.2.4 | Added **Class Deep Dives Vol. 15** (source-verified at `agent-framework==1.8.1`): `AGUIChatClient` + `AGUIEventConverter` + `AGUIHttpService` (AG-UI streaming stateful UI protocol — `run()`, stateless mode, SSE event delivery, per-session `AGUIEventConverter` instantiation requirement), `AgentFrameworkAgent` + `AgentFrameworkWorkflow` + `state_update()` + `add_agent_framework_fastapi_endpoint()` (FastAPI/Teams hosting — pending-approval registry max 10,000, `tags`/`dependencies` params, CORS, env-var wiring), `ThreadItemConverter` + `simple_to_agent_input()` + `stream_agent_response()` (ChatKit thread-item streaming — multimodal `ThreadItem` mapping, delta merging), `DevServer` + `serve()` + `register_cleanup()` (local OpenAI-compatible debug server — multi-agent dict, CORS removed as security fix, graceful teardown via `register_cleanup`; install: `agent-framework[devui]`), `GAIA` + `GAIATelemetryConfig` + `TaskRunner` + `TaskResult` + `Prediction` + `Evaluation` (GAIA benchmark lab — telemetry via `GAIA(telemetry_config=...)`, async callable `TaskRunner.__call__()`, `TaskResult.prediction` + `Evaluation.is_correct`; install: `agent-framework-lab`), `CopilotStudioAgent` (Copilot Studio bridge — env-var prefix `COPILOTSTUDIOAGENT__`, `propagate_session` as_tool param), `AzureAISearchContextProvider` (semantic/agentic retrieval — env-var prefix `AZURE_SEARCH_`, `top_k`/`semantic_configuration_name` params, `retrieval_reasoning_effort` `"minimal"`/`"low"`/`"medium"`), `CosmosHistoryProvider` (Cosmos DB-backed history — env-var prefix `AZURE_COSMOS_`, `store_context_messages`, `store_context_from`), `DurableAIAgentClient` + `DurableAIAgentWorker` + `DurableAIAgentOrchestrationContext` + `AgentCallbackContext` + `AgentResponseCallbackProtocol` (durable task hosting — `get_agent().run()` blocking poll, `yield Task` not `AgentResponse`, `add_agent()` callback), `AgentFunctionApp` (Azure Functions serverless — `agents=[]`/`workflow=` init, `enable_http_endpoints`, `enable_mcp_tool_trigger`, `enable_health_check`). Zero→Hero extended to step 37; Jump-to-topic and Reference grids updated. |
| 2026-06-16 | 1.2.3 | Added **Class Deep Dives Vol. 14** (source-verified at `agent-framework==1.8.1`): `State` (superstep-caching workflow state — `set()`/`get()`/`commit()`/`discard()`/`delete()` deletion sentinel/`export_state()`/`import_state()`, BSP visibility model, checkpoint integration), `OutputDesignation` (output routing rule — `is_terminal()`/`is_intermediate()`/`classify()`, compatibility mode when `outputs=None`, `output_from=`/`intermediate_output_from=` wiring), `MessageType` enum + `WorkflowMessage` enhanced coverage (STANDARD vs RESPONSE classification, plural `trace_contexts`/`source_span_ids` for fan-in OTel link propagation, `to_dict()`/`from_dict()` round-trip, backward-compat `.trace_context`/`.source_span_id` accessors), `DictConvertible` + `encode_value` (dict/JSON round-trip mixin — `to_dict()`/`from_dict()`/`clone(deep=True)`/`to_json()`/`from_json()`, `encode_value` recursive nested encoding), `MiddlewareWrapper` + `BaseMiddlewarePipeline` (callable-to-class adapter via `__init__(func)` + `process()`, abstract `_register_middleware`, `has_middlewares` gate, `_register_middleware_with_wrapper` auto-promotion), `AgentMiddlewarePipeline` + `ChatMiddlewarePipeline` + `FunctionMiddlewarePipeline` (concrete pipeline `execute()` — streaming/non-streaming support, `matches()` sequence cache-key, `MiddlewareTermination` short-circuit, empty pipeline fast path), `MiddlewareDict` TypedDict + `categorize_middleware` (typed `agent`/`function`/`chat` bucket structure, multi-source merge, callable signature inference, unknown-type fallback to `agent` bucket), `FunctionRequestResult` TypedDict (fields: `action` return/continue/stop, `errors_in_a_row`, `result_message`, `update_role`, `function_call_results`, `function_call_count` — `total=False` so all optional, stop-condition diagnostics), `OtelAttr` + `MessageListTimestampFilter` (`str`-Enum with all 35 GenAI semantic convention attribute names — OPERATION/PROVIDER_NAME/INPUT_TOKENS/OUTPUT_TOKENS/TOOL_NAME/AGENT_ID/LLM_OPERATION_DURATION etc; `logging.Filter` that adds `INDEX_KEY * 1µs` offset to `record.created` for correct log ordering in aggregators), `PolicyEnforcementFunctionMiddleware` + `ConfidentialityLabel` + `ContentVariableStore` + `VariableReferenceContent` + `LabeledMessage` + `InspectVariableInput` (IFC prompt-injection defence — `ContentVariableStore.store()`/`retrieve()`, `VariableReferenceContent` variable-ID indirection, `ConfidentialityLabel.PUBLIC`/`PRIVATE`/`USER_IDENTITY`, `LabeledMessage` label-tracked `Message`, `InspectVariableInput` Pydantic schema, `PolicyEnforcementFunctionMiddleware` `allow_untrusted_tools`/`block_on_violation`/`approval_on_violation`/`audit_log`, `SecureAgentConfig` one-liner setup). Zero→Hero extended to step 36; Jump-to-topic and Reference grids updated. |
| 2026-06-15 | 1.2.2 | Added **Class Deep Dives Vol. 13** (source-verified at `agent-framework==1.8.1`): `OpenAIChatClient` + `OpenAIChatOptions` + `OpenAISettings` + `RawOpenAIChatClient` (`agent_framework.openai` — Responses API 4-layer MRO, `STORES_BY_DEFAULT` server-side state, `x-ms-served-model` Azure header, per-request reasoning options, Responses vs Completions comparison table), `OpenAIChatCompletionClient` + `OpenAIChatCompletionOptions` + `RawOpenAIChatCompletionClient` (Chat Completions API — `logprobs`, `prediction`, `logit_bias`, stateless design, structured output), `OpenAIEmbeddingClient` + `OpenAIEmbeddingOptions` + `OpenAIContinuationToken` + `ContentFilterResultSeverity` + `OpenAIContentFilterException` (embeddings+RAG via `MemoryContextProvider`, background polling via `response_id`, Azure content filter exception hierarchy), `AnthropicClient` + `AnthropicChatOptions` + `RawAnthropicClient` (`agent_framework.anthropic` — Anthropic Messages API, extended thinking via `ThinkingConfigEnabled`, `top_k`, `service_tier`, per-request `additional_beta_flags`), `ClaudeAgent` + `ClaudeAgentOptions` + `RawClaudeAgent` (Claude Code CLI-backed agent — `allowed_tools`/`disallowed_tools`, `mcp_servers` `McpServerConfig`, `permission_mode`, `sandbox`, multi-agent workflow participation), `AnthropicFoundryClient` + `AnthropicBedrockClient` + `AnthropicVertexClient` (multi-cloud Claude — Azure Foundry `azure_ad_token_provider`, Bedrock `aws_profile`, Vertex ADC `GoogleCredentials`, multi-cloud failover pattern), `GroupChatOrchestrator` + `AgentBasedGroupChatOrchestrator` + `GroupChatState` + `GroupChatRequestMessage` + `AgentOrchestrationOutput` (orchestrations internals — function-driven vs LLM-driven selection, frozen `GroupChatState` snapshot, `AgentOrchestrationOutput` Pydantic model for structured selection), `HandoffAgentExecutor` + `HandoffAgentUserRequest` + `HandoffSentEvent` + `OrchestrationState` (handoff internals — `autonomous_mode` + `autonomous_mode_turn_limit`, HITL `create_response()`/`terminate()` helpers, `HandoffSentEvent` observability, `OrchestrationState.to_dict()`/`from_dict()` checkpoint), `MagenticOrchestrator` + `MagenticAgentExecutor` + `MagenticOrchestratorEvent` + `MagenticOrchestratorEventType` + `MagenticProgressLedgerItem` + `MagenticPlanReviewRequest` + `MagenticPlanReviewResponse` (Magentic deeper — outer/inner loop, `MagenticResetSignal` for replanning, plan signoff `approve()`/`revise()` HITL, ledger item `to_dict()`/`from_dict()`), `HttpRequestHandler` + `DefaultHttpRequestHandler` + `HttpRequestInfo` + `HttpRequestResult` + `MCPToolHandler` + `DefaultMCPToolHandler` + `MCPToolInvocation` + `MCPToolResult` + `ToolApprovalRequest` + `ToolApprovalResponse` + `ExternalInputRequest` + `ExternalInputResponse` + `AgentExternalInputRequest` + `AgentExternalInputResponse` (declarative handlers — 3 `DefaultHttpRequestHandler` construction modes, SSRF guard pattern, MCP LRU cache + `LIST_TOOLS_TOOL_NAME` discovery, tool approval gate, agent HITL). Zero→Hero extended to step 35; Jump-to-topic and Reference grids updated. |
| 2026-06-14 | 1.2.1 | Added **Class Deep Dives Vol. 12** (source-verified at `agent-framework==1.8.1`): `Skill` + `SkillResource` + `SkillScript` ABCs (the type-system foundation — `get_content()` / `get_resource()` / `get_script()` contracts; `SkillResource.read()` `**kwargs` forwarding; `SkillScript.run()` dict vs list dispatch; `parameters_schema` extension point; custom DB-backed `Skill` example), `FileSkill` (filesystem-backed skill — `SKILL.md` content cache; `<scripts>` XML block appended when scripts present; first-access cache lock; case-insensitive `get_resource()`/`get_script()`), `InlineSkillResource` + `InlineSkillScript` (code-defined primitives — `InlineSkillResource` content/function mutual exclusivity; `_accepts_kwargs` precomputed flag; `InlineSkillScript` lazy `parameters_schema` via `FunctionTool` introspection; `TypeError` on list args), `FileSkillScript` + `SkillScriptRunner` (file-backed execution — absolute-path validation; fixed CLI array schema `{"type":"array","items":{"type":"string"}}`; sync/async runner protocol; `isinstance(fn, SkillScriptRunner)` verification), `SupportsAgentRun` (structural agent protocol — `id`/`name`/`description` attributes; dual `run()` overloads; custom agent without framework inheritance; `isinstance()` guard; workflow graph integration), `RunnerContext` (full workflow execution context protocol — 25-method contract: message routing, event pipeline, checkpointing, streaming mode, HITL request/response, yield-output classification; minimal `TestRunnerContext` test stub; `set_runtime_checkpoint_storage` override pattern), `SwitchCaseEdgeGroupCase` + `SwitchCaseEdgeGroupDefault` (serialisable branch descriptors — `condition_name` auto-extracted from `callable.__name__`; `to_dict()`/`from_dict()` without live callable; missing-callable placeholder raises `RuntimeError` on invocation; condition re-registration pattern using a `registry` dict), `ValidationTypeEnum` + `WorkflowValidationError` hierarchy (`EDGE_DUPLICATION`, `EXECUTOR_DUPLICATION`, `TYPE_COMPATIBILITY`, `GRAPH_CONNECTIVITY`, `HANDLER_OUTPUT_ANNOTATION`, `OUTPUT_VALIDATION`; `EdgeDuplicationError.edge_id`; `TypeCompatibilityError.source_types`/`target_types`; `GraphConnectivityError`; `match`-based dispatcher; custom `ValidationReport` dataclass), `A2AAgent` + `A2AAgentSession` + `A2AExecutor` (A2A protocol integration — `url`/`agent_card`/`client` init paths; dual streaming/non-streaming `httpx` clients; `auth_interceptor` pattern; `float`/`httpx.Timeout` config; `supported_protocol_bindings`; `A2AAgentSession` `context_id`/`task_id`/`task_state` with `to_dict()`/`from_dict()` round-trip; `A2AExecutor` `stream` + `run_kwargs` server-side exposure), `WorkflowRunnerException` + `WorkflowCheckpointException` + `MiddlewareException` (leaf exception classes — `WorkflowCheckpointException` dual-inheritance from both `WorkflowRunnerException` and `WorkflowException`; checkpoint I/O failure graceful fallback; `MiddlewareException` vs `MiddlewareTermination` distinction; full supervisor dispatcher for all 7 framework exception types). Zero→Hero extended to step 34; Jump-to-topic and Reference grids updated. |
| 2026-06-13 | 1.2.0 | Added **Class Deep Dives Vol. 11** (source-verified at `agent-framework==1.8.1`): `AgentTelemetryLayer` + `ChatTelemetryLayer` + `EmbeddingTelemetryLayer` (OTel mixin layer — `AGENT_PROVIDER_NAME` resolution, token/duration histograms, `ObservabilitySettings.ENABLED` short-circuit, `enable_sensitive_telemetry`), `Edge` + `EdgeGroup` + `SingleEdgeGroup` + `InternalEdgeGroup` (workflow graph primitives — conditional routing with `should_route()`, serialisation round-trip without callable, `EdgeGroup.register` custom subclasses, internal source edge semantics), `Case` + `Default` (runtime switch-case wrappers — live callables companion to serialisable `SwitchCaseEdgeGroupCase`/`Default`), `EdgeRunner` hierarchy (`SingleEdgeRunner`, `FanOutEdgeRunner`, `FanInEdgeRunner`, `SwitchCaseEdgeRunner` — `EdgeGroupDeliveryStatus` enum with 6 values, buffer-until-all-ready fan-in semantics, concurrent asyncio fan-out), `ExecutionContext` (sub-workflow tracking dataclass — `execution_id`, `collected_responses`, `expected_response_count`, `pending_requests` for HITL resume), `WorkflowGraphValidator` (7 pre-build checks: edge duplication → `EdgeDuplicationError`, type compatibility → `TypeCompatibilityError`, connectivity → `GraphConnectivityError`, self-loops warning, dead-ends info, output validation → `WorkflowValidationError`), `MCPTool` + `MCPSpecificApproval` (base class for all 3 MCP transports — full 15-parameter constructor table, `functions` list post-connect, `per-tool` `result_parser` override, `additional_tool_argument_names` hidden-context forwarding), `SerializationMixin` + `SerializationProtocol` (DTO round-trip foundation — `DEFAULT_EXCLUDE`, `INJECTABLE`, `_SHALLOW_COPY_FIELDS`, three dependency injection patterns, custom serialisable tool example, `AgentSession` round-trip), `Evaluator` + `EvalItemResult` + `EvalNotPassedError` (`@runtime_checkable` Protocol for evaluation backends, `EvalItemResult` per-item `status`/`scores`/`error_code`, CI gate pattern with `evaluate_and_assert`), `PerServiceCallHistoryPersistingMiddleware` (`service_stores_history` flag, sentinel conversation ID semantics, per-service-call vs full-turn persistence trade-offs). Zero→Hero extended to step 33; Jump-to-topic and Reference grids updated. |
| 2026-06-12 | 1.1.9 | Upgraded `agent-framework` **1.8.0 → 1.8.1** (patch release; no new public symbols). Added **Class Deep Dives Vol. 10** (source-verified at `agent-framework==1.8.1`): `Workflow` + `InProcRunnerContext` (execution engine API — `run()` mutual-exclusion rules, `input_types`/`output_types`, `as_agent()`, `as_tool()`, state preservation, checkpoint resume, streaming), `FunctionExecutor` (`@executor` decorator internals — explicit `input`/`output`/`workflow_output` type override, sync function thread-pool wrapping, multi-node wiring), `FunctionInvocationLayer` (tool-calling loop mixin — `get_response()` API, custom chat client authoring via `_get_response_core`, per-call middleware merge, per-executor `function_invocation_kwargs`), `MemoryStore` + `MemoryIndexEntry` + `MemoryTopicRecord` (memory harness ABC — full 8-method contract, `to_dict`/`from_dict` round-trip, `MemoryIndexEntry.from_topic_record`, custom `DictMemoryStore` backend plugged into `MemoryContextProvider`), `TodoStore` + `TodoItem` + `TodoInput` + `TodoFileStore` + `TodoSessionStore` (todo harness — `TodoFileStore` path-safety and per-user scoping, `TodoSessionStore` session-state embedding, `TodoInput` title normalisation, ABC `load_state`/`save_state`/`load_items`), `DeduplicatingSkillsSource` (case-insensitive first-one-wins deduplication in AggregatingSkillsSource pipelines, custom key override subclass), `SkillsProvider` (context provider — `from_paths` class method with all parameters, `instruction_template` placeholder rules, `require_script_approval`, `disable_caching`, multi-source composition), `MCPTaskOptions` (SEP-2663 long-running task lifecycle — `default_ttl`, `cancel_remote_task_on_local_cancellation`, `max_task_wait`, positive-value validation, fire-and-forget pattern), `InMemoryCheckpointStorage` (in-memory checkpoint backend — `save`/`load`/`list_checkpoints`/`list_checkpoint_ids`/`get_latest`/`delete`; vs `FileCheckpointStorage` comparison table; HITL resume example), `EvalScoreResult` + `CompactionStrategy` + `BaseAgent` (`EvalScoreResult` rubric-dimension fields and custom `@evaluator`; `CompactionStrategy` protocol — custom in-place mutation + `TokenBudgetComposedStrategy` composition; `BaseAgent` — `run()` implementation contract, `FixedResponseAgent` test double, `SequentialBuilder` integration, `to_dict()` serialisation). Zero→Hero renumbered to step 32; Jump-to-topic, Zero→Hero, and Reference grids updated. Version card and description bumped to 1.8.1. |
| 2026-06-05 | 1.1.8 | Upgraded `agent-framework` 1.7.0 → **1.8.0**. Added **Class Deep Dives Vol. 8** (source-verified at `agent-framework==1.8.0`): `AgentFileStore` + `FileSystemAgentFileStore` + `InMemoryAgentFileStore` (file storage ABC and two first-party backends with path-safety and symlink rejection), `FileAccessProvider` + `FileSearchResult` + `FileSearchMatch` (5-tool CRUD+search harness provider, `require_delete_approval` flag, `SerializationMixin` DTOs), `MCPSkill` + `MCPSkillResource` + `MCPSkillsSource` (SEP-2640 Agent Skills over MCP — lazy `SKILL.md` fetch, resource name security validation, index-JSON discovery), `ToolMode` + `validate_tool_mode` (tool-choice TypedDict with `mode`/`required_function_name`/`allowed_tools`, normalization helper), `AgentEvalConverter` + `ExpectedToolCall` + `CheckResult` + `RubricScore` (eval bridge: `convert_messages`, `to_eval_item`, per-dimension `RubricScore`, structured `CheckResult`), `ChatContext` (full chat-middleware context — `metadata`, `result` override, `stream_transform_hooks`, `stream_result_hooks`, `stream_cleanup_hooks`), `WorkflowAgent` + `WorkflowContext` (`BaseAgent` wrapping a `Workflow`, `pending_requests` HITL, typed `WorkflowContext[OutT, W_OutT]` with `send_message`/`yield_output`), `TruncationStrategy` (oldest-first token/message compaction, `preserve_system` flag, `compact_to` target, `TokenizerProtocol` integration), `HistoryProvider` + `InMemoryHistoryProvider` (history-provider ABC with `load_messages`/`store_inputs`/`store_outputs` flags; `InMemoryHistoryProvider` default state-backed implementation with `skip_excluded`), `DelegatingSkillsSource` + `InMemorySkillsSource` + `FunctionInvocationContext` + progressive tools (`add_tools`/`remove_tools` @experimental PROGRESSIVE_TOOLS). Version card, description, and "What's shipped" bumped to 1.8.0. Zero→Hero renumbered to step 30; Jump-to-topic and Reference grids updated. |
| 2026-06-04 | 1.1.7 | Added **Class Deep Dives Vol. 7** (source-verified at `agent-framework==1.7.0`): `ContextProvider` custom authoring (`before_run`/`after_run` lifecycle, provider-scoped `state`, multi-provider ordering), `BackgroundTaskInfo` + `BackgroundTaskStatus` (task metadata data model, `LOST` status re-issue pattern, reading state from `session.state`), `GroupChatBuilder` + `TerminationCondition` + `GroupChatSelectionFunction` (LLM orchestrator, round-robin selection, HITL pause), `HandoffBuilder` + `HandoffConfiguration` (tool-based routing, `autonomous_mode`, multi-hop handoff topology), `MagenticBuilder` + `StandardMagenticManager` + `MagenticProgressLedger` (Magentic-One task/progress ledgers, stall detection, plan review HITL, custom `progress_ledger_prompt`), `SequentialBuilder` + `ConcurrentBuilder` (pipeline chaining, `chain_only_agent_responses`, fan-out/synthesise composition), `AgentFactory` + `WorkflowFactory` (`agent-framework-declarative`, YAML agent and workflow definitions, `ProviderTypeMapping`, `WorkflowState`, `safe_mode`), `SecureAgentConfig` + `ContentLabel` + `IntegrityLabel` + `LabelTrackingFunctionMiddleware` (`agent_framework.security`, IFC-based prompt injection defence, 3-tier label propagation, `ContentVariableStore`, approval-on-violation pattern), `FunctionalWorkflowAgent` (`run()` overloads, `pending_requests` HITL, drop-in orchestration participant), `ObservabilitySettings` + `configure_otel_providers` (OTLP bootstrap, custom exporters, metric `View` filters, `enable_sensitive_telemetry`). Zero→Hero updated to step 29; Jump-to-topic and Reference grids updated. |
| 2026-06-03 | 1.1.6 | Added **Class Deep Dives Vol. 6** (source-verified at `agent-framework-core==1.7.0`): `ExperimentalFeature` + `ReleaseCandidateFeature` + `@experimental`/`@release_candidate` decorators (feature staging, warning suppression, per-feature-ID introspection, 9 experimental feature IDs at 1.7.0), `WorkflowRunState` + `WorkflowErrorDetails` (7-state run state machine, `from_exception()` factory, structured error logging patterns), `WorkflowExecutor` + `SubWorkflowRequestMessage` + `SubWorkflowResponseMessage` (hierarchical workflow composition, HITL across workflow boundaries, `allow_direct_output` semantics), `AgentResponse` + `AgentResponseUpdate` + `ContinuationToken` (full field reference, serialisation round-trip, structured output, streaming collapse, background-operation polling), `BaseEmbeddingClient` + `SupportsGetEmbeddings` + `Embedding` + `EmbeddingGenerationOptions` + `GeneratedEmbeddings` (custom embedding provider implementation, RAG duck-typing patterns), `FunctionInvocationConfiguration` TypedDict (all 7 fields, default values, `max_iterations` vs `max_function_calls` semantics, per-request override, `additional_tools` hidden-tool pattern), `ClassSkill` + `SkillFrontmatter` + `FileSkillsSource` + `SkillsProvider` (`@ClassSkill.resource`/`@ClassSkill.script` auto-discovery, directory-scanned `SKILL.md` files, mixed code+file `SkillsProvider` wiring), `Annotation` + `TextSpanRegion` (citation metadata, grounding attribution, region filtering), provider capability protocols (`SupportsCodeInterpreterTool`, `SupportsWebSearchTool`, `SupportsImageGenerationTool`, `SupportsMCPTool`, `SupportsFileSearchTool` — adaptive tool builder + capability probe patterns), `MiddlewareType` + `AgentMiddlewareLayer` + `ChatMiddlewareLayer` (pipeline execution order diagram, `MiddlewareTermination` short-circuit). Zero→Hero renumbered to 28 steps; Jump-to-topic and Reference grids updated. |
| 2026-06-02 | 1.1.5 | Added **Class Deep Dives Vol. 5** (source-verified at `agent-framework-core==1.7.0`): `Executor` + `@handler` + `@executor` (introspection vs explicit type modes, `WorkflowContext` generic variants, checkpoint hooks, `FunctionExecutor`), `AgentExecutor` + `AgentExecutorRequest` + `AgentExecutorResponse` (all five built-in handlers, context modes, `with_text()` conversation-context preservation), `FanOutEdgeGroup` + `FanInEdgeGroup` + `SwitchCaseEdgeGroup` (dynamic selection functions, switch/case single-dispatch semantics, construction constraints), `Runner` + `WorkflowMessage` (Pregel superstep engine, convergence detection, concurrent message delivery, trace context fan-in), `SessionContext` (per-invocation provider pipeline, `extend_messages` attribution, cross-provider metadata), `AgentSession` + `register_state_type` (cross-call state, Pydantic round-trip, cold-start registration), `BaseChatClient` + `SupportsChatGetResponse` (custom model client implementation, structural protocol verification), `SecretString` + `load_settings` (repr masking, `.env` resolution order), `WorkflowCheckpoint` + `CheckpointStorage` (full dataclass fields, topology hash compatibility, HITL resume via pending events, custom storage protocol), full exception hierarchy (`AgentFrameworkException` tree, `MiddlewareTermination` short-circuit, `WorkflowConvergenceException`, `UserInputRequiredException`). Zero→Hero renumbered to 27 steps; Jump-to-topic and Reference grids updated. |
| 2026-06-01 | 1.1.4 | Added **Class Deep Dives Vol. 4** (source-verified at `agent-framework-core==1.7.0`): `Message` + `Content` (multimodal factory methods, serialization), `ChatOptions` + `ChatResponse` + `ChatResponseUpdate` + `UsageDetails` (raw chat layer), `ResponseStream` (transform/cleanup/result hooks, `.map()`, `.with_finalizer()`), `AgentContext` (middleware context — all 14 mutable fields, short-circuit and streaming patterns), `FunctionalWorkflow` + `StepWrapper` (HITL replay, step-level caching, per-step checkpointing, `.as_agent()`), `WorkflowEvent` + `WorkflowEventType` + `WorkflowEventSource` (full event taxonomy, HITL dispatch loop pattern), `SkillsSource` + `AggregatingSkillsSource` + `FilteringSkillsSource` + `DeduplicatingSkillsSource` (skill composition pipeline), `EvalItem` + `EvalItemResult` + `EvalResults` + `EvalScoreResult` (evaluation data model), `TokenizerProtocol` + `CharacterEstimatorTokenizer` (tiktoken and HuggingFace integration examples), `ConversationSplit` + `ConversationSplitter` (LAST_TURN vs FULL splits, custom splitter protocol). Zero→Hero renumbered to 26 steps; Jump-to-topic and Reference grids updated. |
| 2026-05-31 | 1.1.3 | Added **`azure-ai-agents` integration Class Deep Dives Vol. 6** (source-verified at `azure-ai-agents==1.1.0`): OpenAPI auth hierarchy (`OpenApiAnonymousAuthDetails`, `OpenApiManagedAuthDetails`, `OpenApiConnectionAuthDetails`), `BingGroundingSearchConfiguration` + `BingGroundingSearchToolParameters`, `RunCompletionUsage` + `RunStepCompletionUsage`, `VectorStoreDataSource` + `VectorStoreConfigurations`, expiry policy + static/auto chunking strategies, `AgentsNamedToolChoice` + `AgentsToolChoiceOptionMode`, file-search run step results (`RunStepFileSearchToolCall` → `FileSearchToolCallContent`), streaming event taxonomy (`AgentStreamEvent` + 4 sub-enums), `RequiredFunctionToolCall` + `SubmitToolOutputsDetails` manual dispatch, multimodal `ThreadMessageOptions` with `MessageInputImageFileBlock` + `MessageInputImageUrlBlock`. Zero→Hero renumbered to 25 steps; Jump-to-topic and Reference grids updated. |
| 2026-05-30 | 1.1.2 | Upgraded `agent-framework` 1.6.0 → **1.7.0**. Added **Class Deep Dives Vol. 3** (10 source-verified classes from 1.7.0): `BackgroundAgentsProvider`, `MemoryContextProvider` + `MemoryFileStore`, `TodoProvider` + `TodoFileStore`, `AgentModeProvider`, `SummarizationStrategy`, `ContextWindowCompactionStrategy`, `SlidingWindowStrategy`, `SelectiveToolCallCompactionStrategy`, `WorkflowViz`, `MCPStreamableHTTPTool` + `MCPWebsocketTool`. Updated version card, description, and "What's shipped" section. Zero→Hero renumbered; Jump-to-topic and Reference grids updated. |
| 2026-05-29 | 1.1.1 | Corrected erroneous reframing of this guide as an `azure-ai-agents` site. `agent-framework` is the primary framework; `azure-ai-agents` is an optional integration add-on. Restored correct title, description, install command, minimal example, and framing. Removed false "Package name correction" Aside and migration notice. Reframed `azure-ai-agents` class deep dive volumes (1, 3, 4, 5) as integration add-on reference material rather than framework documentation. |
| 2026-05-22 | 1.0.9 | Version bumped 1.5.0 → 1.6.0; version card, description frontmatter, and "What's shipped" section updated to 1.6.0. All 23 core symbols verified against installed `agent-framework-core==1.6.0` (`.routine-envs/check-0522-ms-agent`); 242 public symbols confirmed; no API regressions. `frameworks.ts` updated `v1.4.0` → `v1.6.0`. Note: `DeprecationWarning` from opentelemetry internal `importlib.metadata` dict interface (Python 3.11 compat) persists — upstream opentelemetry bug, not an agent-framework issue. |
| 2026-05-20 | 1.0.8 | Bumped all version references to `agent-framework-core==1.5.0`. Added `output_from`/`intermediate_output_from` documentation to `WorkflowBuilder` (replaces deprecated `output_executors`). Added full `MemoryContextProvider` section to HARNESS Providers guide including injected tools, `consolidation_client`, `history_message_filter`, and custom `MemoryStore` backend examples. Added `RunContext.set_state` key restriction (`_` prefix raises `ValueError`) to Functional Workflows guide. Added combined `checkpoint_id + responses` HITL resume pattern. Added `include_status_events` explanation. Fixed `FunctionInvocationConfiguration` defaults (`max_iterations=40`, `max_consecutive_errors_per_request=3`). Added full `FunctionTool` constructor reference table with `declaration_only`, `additional_properties`, and middleware cost-tagging examples. |
| 2026-05-17 | 1.0.7 | Bumped all version references to `agent-framework-core==1.4.0`. Added `get_run_context()` code example to Functional Workflows guide (accessing RunContext from nested utility functions). Added `annotate_message_groups` low-level helper documentation to Compaction guide. Re-verified all API surfaces against installed `agent-framework-core==1.4.0` source. |
| 2026-05-09 | 1.0.6 | Bumped all guides to `agent-framework-core==1.3.0`. `agent-framework-foundry` and `agent-framework-openai` promoted to stable 1.3.0. Rewrote Skills guide with breaking API changes: `InlineSkill` replaces `Skill`, `SkillsProvider(source)` positional arg replaces `skills=`/`skill_paths=`, `SkillsProvider.from_paths()` for file-based skills. Added `AggregatingSkillsSource`, `FilteringSkillsSource`, `DeduplicatingSkillsSource` coverage. Added `RawAgent` to comprehensive guide. Added `MemoryStore`/`MemoryContextProvider` long-term memory section. Fixed `FunctionInvocationConfiguration` usage (client-level only, not `agent.run()` parameter). Added `WorkflowBuilder.add_chain()` and `add_multi_selection_edge_group()` detail. |
| 2026-05-04 | 1.0.5 | Added dedicated Functional Workflows page covering `@workflow`, `@step`, `RunContext`, `FunctionalWorkflow.as_agent()`. Enhanced Graphs & Declarative page with `WorkflowViz` examples, `add_chain`, `add_multi_selection_edge_group`, `@executor` decorator, and `Agent.as_tool()`. All verified against `agent-framework-core==1.2.2` source. |
| 2026-05-01 | 1.0.4 | Bumped pinned core/provider versions to `agent-framework-core==1.2.2` and `1.0.0b260429` provider line. Expanded streaming, function-middleware, functional-workflow, and feature-stage code examples after re-introspecting the installed library. |
| 2026-04-22 | 1.0.3 | Added dedicated pages: Tools, Skills, Checkpointing, Compaction, Observability & Telemetry, Evaluation. Zero→Hero reflow to 18 steps; Jump-to-topic grid and Reference card grid updated. |
| 2026-04-22 | 1.0.2 | Added dedicated pages: Model Providers, Orchestration, Middleware, MCP, Human-in-the-loop. Verified against source of `agent-framework-core==1.1.0`; corrected `context_providers` plural-vs-singular errata. |
| 2026-04-21 | 1.0.1 | Index redesigned into Zero → Hero reading order + Jump-to-topic grid using Starlight cards. |
| 2026-04-20 | 1.0.1 | Version pin corrected to `v1.0.1`; verified via `agent-framework` PyPI package. |
| April 16, 2026 | 1.0 GA | Graduated from Preview; `ChatClientAgentOptions.Instructions` removed; first-party connectors; Azure App Service support. |
| November 2025 | 1.0 Preview | Initial Python guide; multi-agent orchestration. |
