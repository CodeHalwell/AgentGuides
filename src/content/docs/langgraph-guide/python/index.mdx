---
title: "LangGraph (Python)"
description: "Stateful, graph-based agents for Python — zero to hero with chaptered tutorials and topic-jump references."
framework: langgraph
language: python
sidebar:
  label: "Overview"
  order: 0
---

import { Card, CardGrid, LinkCard, Aside, Tabs, TabItem } from '@astrojs/starlight/components';

**Low-level orchestration for stateful, long-running LLM agents.** LangGraph models your agent as a graph of nodes and edges with explicit state, durable checkpoints, and first-class human-in-the-loop.

<CardGrid>
  <Card title="Install" icon="rocket">
    ```bash
    pip install langgraph langchain-anthropic
    ```
  </Card>
  <Card title="Version" icon="information">
    **v1.2.7** · June 2026 · Python 3.10 – 3.14
  </Card>
  <Card title="Best for" icon="puzzle">
    Cycles, conditional routing, multi-agent coordination, HITL workflows.
  </Card>
</CardGrid>

<Aside type="tip" title="Where to start">
  New to LangGraph? Follow the **Zero → Hero** path below in order (~3–4 hours). Already building? Use **Jump to a topic** to land on the chapter you need.
</Aside>

## Zero → Hero

A chaptered tutorial that takes you from a first graph to a production-ready agent. Each chapter has its own page with **Next →** pagination.

<CardGrid>
  <LinkCard title="1 · Setup & core concepts" href="/langgraph-guide/python/chapter-01-setup-and-core-concepts/" description="Install, state, nodes, edges, compilation · 15 min" />
  <LinkCard title="2 · Your first agent" href="/langgraph-guide/python/chapter-02-simple-agents/" description="Linear chat, conditional routing, looping, streaming · 20 min" />
  <LinkCard title="3 · Multi-agent systems" href="/langgraph-guide/python/chapter-03-multi-agent/" description="Supervisor, parallel workers, handoff · 25 min" />
  <LinkCard title="4 · Tools" href="/langgraph-guide/python/chapter-04-tools/" description="ToolNode, custom executors, conditional tool use · 20 min" />
  <LinkCard title="5 · Memory & persistence" href="/langgraph-guide/python/chapter-05-memory/" description="Checkpointers, Store, cross-thread memory · 25 min" />
  <LinkCard title="6 · Streaming & debugging" href="/langgraph-guide/python/chapter-06-streaming-and-debugging/" description="All 7 stream modes, typed StreamPart v2, GraphOutput, token streaming, custom events · 30 min" />
  <LinkCard title="7 · Human-in-the-loop" href="/langgraph-guide/python/chapter-07-human-in-the-loop/" description="Interrupts, multi-stage approval, interactive debug · 25 min" />
  <LinkCard title="8 · Middleware" href="/langgraph-guide/python/chapter-08-middleware-hooks/" description="langchain.agents.middleware — PII, summarization, retries, HITL, cost limits · 20 min" />
  <LinkCard title="9 · Advanced patterns" href="/langgraph-guide/python/chapter-09-advanced-patterns/" description="ReAct, Tree-of-Thoughts, reflection, structured output, caching, Functional API · 25 min" />
  <LinkCard title="10 · Production & troubleshooting" href="/langgraph-guide/python/chapter-10-production/" description="RetryPolicy, TimeoutPolicy, CachePolicy, Durability, IsLastStep, Topic channels, Docker, CLI · 30 min" />
</CardGrid>

<Aside type="note" title="Looking for a reference?">
  The Zero → Hero chapters are task-oriented tutorials. If you already know the
  shape of LangGraph and want exact signatures, feature tables, and breaking-change
  history for a single primitive, jump to the **Reference** section near the bottom
  of this page — one page per API (StateGraph, Checkpointers, Store, Functional
  API, Command/Send, streaming modes). Each is verified line-by-line against the
  installed `langgraph==1.2.7` source.
</Aside>

## Jump to a topic

Need one specific concept? Land directly on the chapter that owns it.

<CardGrid>
  <LinkCard title="State graph" href="/langgraph-guide/python/chapter-01-setup-and-core-concepts/#core-concepts" />
  <LinkCard title="Conditional routing" href="/langgraph-guide/python/chapter-02-simple-agents/#example-2-conditional-routing" />
  <LinkCard title="Supervisor pattern" href="/langgraph-guide/python/chapter-03-multi-agent/#example-1-supervisor-pattern" />
  <LinkCard title="Parallel workers" href="/langgraph-guide/python/chapter-03-multi-agent/#example-2-parallel-worker-pattern" />
  <LinkCard title="ToolNode" href="/langgraph-guide/python/chapter-04-tools/#example-1-basic-tool-node" />
  <LinkCard title="Checkpointers" href="/langgraph-guide/python/chapter-05-memory/#short-term-memory-checkpointers" />
  <LinkCard title="Store (long-term memory)" href="/langgraph-guide/python/chapter-05-memory/#long-term-memory-store" />
  <LinkCard title="Type-safe v2 API" href="/langgraph-guide/python/chapter-06-streaming-and-debugging/#type-safe-v2-api-v11x" />
  <LinkCard title="Interrupts (HITL)" href="/langgraph-guide/python/chapter-07-human-in-the-loop/#basic-interrupts" />
  <LinkCard title="Middleware (create_agent)" href="/langgraph-guide/python/chapter-08-middleware-hooks/" />
  <LinkCard title="ReAct pattern" href="/langgraph-guide/python/chapter-09-advanced-patterns/#pattern-1-react-reasoning--acting" />
  <LinkCard title="Functional API" href="/langgraph-guide/python/reference-functional-api/" />
  <LinkCard title="Command / Send" href="/langgraph-guide/python/reference-command-and-send/" />
  <LinkCard title="IsLastStep / RemainingSteps" href="/langgraph-guide/python/reference-managed-values/" />
  <LinkCard title="Runtime / ToolRuntime injection" href="/langgraph-guide/python/reference-runtime-and-managed-values/" />
  <LinkCard title="Stream modes (v1 / v2)" href="/langgraph-guide/python/reference-streaming-modes/" />
  <LinkCard title="StateGraph API" href="/langgraph-guide/python/reference-state-graph/" />
  <LinkCard title="Checkpointers matrix" href="/langgraph-guide/python/reference-checkpointers/" />
  <LinkCard title="Store (long-term memory)" href="/langgraph-guide/python/reference-store/" />
  <LinkCard title="Prebuilt Nodes" href="/langgraph-guide/python/reference-prebuilt-nodes/" />
  <LinkCard title="Channels" href="/langgraph-guide/python/reference-channels/" />
  <LinkCard title="RemoteGraph" href="/langgraph-guide/python/reference-remote-graph/" />
  <LinkCard title="v3 streaming (stream_events)" href="/langgraph-guide/python/langgraph_class_deep_dives_v16/#background-the-v3-streaming-protocol" />
  <LinkCard title="GraphRunStream / AsyncGraphRunStream" href="/langgraph-guide/python/langgraph_class_deep_dives_v16/#1--graphrunstream" />
  <LinkCard title="ChatModelStream (per-LLM streaming)" href="/langgraph-guide/python/langgraph_class_deep_dives_v16/#7--messagestransformer--chatmodelstream" />
  <LinkCard title="SubgraphRunStream (subgraph navigation)" href="/langgraph-guide/python/langgraph_class_deep_dives_v16/#3--subgraphrunstream--asyncsubgraphrunstream" />
  <LinkCard title="Class deep-dives Vol. 1" href="/langgraph-guide/python/langgraph_class_deep_dives/" />
  <LinkCard title="Class deep-dives Vol. 2" href="/langgraph-guide/python/langgraph_class_deep_dives_v2/" />
  <LinkCard title="Class deep-dives Vol. 3" href="/langgraph-guide/python/langgraph_class_deep_dives_v3/" />
  <LinkCard title="Class deep-dives Vol. 4" href="/langgraph-guide/python/langgraph_class_deep_dives_v4/" />
  <LinkCard title="Class deep-dives Vol. 5" href="/langgraph-guide/python/langgraph_class_deep_dives_v5/" />
  <LinkCard title="Class deep-dives Vol. 6" href="/langgraph-guide/python/langgraph_class_deep_dives_v6/" />
  <LinkCard title="Class deep-dives Vol. 7" href="/langgraph-guide/python/langgraph_class_deep_dives_v7/" />
  <LinkCard title="Class deep-dives Vol. 8" href="/langgraph-guide/python/langgraph_class_deep_dives_v8/" />
  <LinkCard title="Class deep-dives Vol. 9" href="/langgraph-guide/python/langgraph_class_deep_dives_v9/" />
  <LinkCard title="Class deep-dives Vol. 10" href="/langgraph-guide/python/langgraph_class_deep_dives_v10/" />
  <LinkCard title="Class deep-dives Vol. 11" href="/langgraph-guide/python/langgraph_class_deep_dives_v11/" />
  <LinkCard title="Class deep-dives Vol. 12" href="/langgraph-guide/python/langgraph_class_deep_dives_v12/" />
  <LinkCard title="Class deep-dives Vol. 13" href="/langgraph-guide/python/langgraph_class_deep_dives_v13/" />
  <LinkCard title="Class deep-dives Vol. 14" href="/langgraph-guide/python/langgraph_class_deep_dives_v14/" />
  <LinkCard title="Class deep-dives Vol. 15" href="/langgraph-guide/python/langgraph_class_deep_dives_v15/" />
  <LinkCard title="Class deep-dives Vol. 16" href="/langgraph-guide/python/langgraph_class_deep_dives_v16/" />
  <LinkCard title="Class deep-dives Vol. 17" href="/langgraph-guide/python/langgraph_class_deep_dives_v17/" />
  <LinkCard title="Class deep-dives Vol. 18" href="/langgraph-guide/python/langgraph_class_deep_dives_v18/" />
  <LinkCard title="Class deep-dives Vol. 19" href="/langgraph-guide/python/langgraph_class_deep_dives_v19/" />
  <LinkCard title="Class deep-dives Vol. 20" href="/langgraph-guide/python/langgraph_class_deep_dives_v20/" />
  <LinkCard title="Class deep-dives Vol. 21" href="/langgraph-guide/python/langgraph_class_deep_dives_v21/" />
  <LinkCard title="Class deep-dives Vol. 22" href="/langgraph-guide/python/langgraph_class_deep_dives_v22/" />
  <LinkCard title="Class deep-dives Vol. 23" href="/langgraph-guide/python/langgraph_class_deep_dives_v23/" />
  <LinkCard title="Class deep-dives Vol. 24" href="/langgraph-guide/python/langgraph_class_deep_dives_v24/" />
  <LinkCard title="Class deep-dives Vol. 25" href="/langgraph-guide/python/langgraph_class_deep_dives_v25/" />
  <LinkCard title="Class deep-dives Vol. 26" href="/langgraph-guide/python/langgraph_class_deep_dives_v26/" />
  <LinkCard title="Class deep-dives Vol. 27" href="/langgraph-guide/python/langgraph_class_deep_dives_v27/" />
  <LinkCard title="Class deep-dives Vol. 28" href="/langgraph-guide/python/langgraph_class_deep_dives_v28/" />
  <LinkCard title="Class deep-dives Vol. 29" href="/langgraph-guide/python/langgraph_class_deep_dives_v29/" />
  <LinkCard title="Class deep-dives Vol. 30" href="/langgraph-guide/python/langgraph_class_deep_dives_v30/" />
  <LinkCard title="Class deep-dives Vol. 31" href="/langgraph-guide/python/langgraph_class_deep_dives_v31/" />
  <LinkCard title="Class deep-dives Vol. 32" href="/langgraph-guide/python/langgraph_class_deep_dives_v32/" />
  <LinkCard title="Class deep-dives Vol. 33" href="/langgraph-guide/python/langgraph_class_deep_dives_v33/" />
  <LinkCard title="Class deep-dives Vol. 34" href="/langgraph-guide/python/langgraph_class_deep_dives_v34/" />
</CardGrid>

## Reference

Every page below is introspected against the installed `langgraph==1.2.7` /
`langgraph-checkpoint==4.1.1` / `langgraph-checkpoint-sqlite==3.0.3` /
`langgraph-checkpoint-postgres==3.0.5` source. If a signature disagrees with an
older blog post or docs page, the source wins.

<CardGrid>
  <LinkCard title="StateGraph API reference" href="/langgraph-guide/python/reference-state-graph/" description="Constructor, add_node (all 4 overloads), add_edge, add_conditional_edges, add_sequence, compile, Runtime, RetryPolicy, CachePolicy, TimeoutPolicy, RunControl, ExecutionInfo." />
  <LinkCard title="Checkpointers reference" href="/langgraph-guide/python/reference-checkpointers/" description="Feature matrix for InMemorySaver, Sqlite, AsyncSqlite, Postgres, AsyncPostgres, ShallowPostgres — plus durability modes and connection strings." />
  <LinkCard title="Store (long-term memory) reference" href="/langgraph-guide/python/reference-store/" description="BaseStore, InMemoryStore, namespaces, IndexConfig (vector search), TTLConfig, InjectedStore for tools." />
  <LinkCard title="Functional API reference" href="/langgraph-guide/python/reference-functional-api/" description="@entrypoint, @task, entrypoint.final, previous, injecting Runtime, feature matrix vs StateGraph." />
  <LinkCard title="Command / Send reference" href="/langgraph-guide/python/reference-command-and-send/" description="Command(update, goto, resume, graph), Send for fan-out, interrupt + resume, Command.PARENT, Overwrite." />
  <LinkCard title="Streaming modes reference" href="/langgraph-guide/python/reference-streaming-modes/" description="All eight stream_mode values (including tools + ToolCallTransformer), v1 vs v2 typing, StreamPart discriminated union, GraphOutput, subgraphs=True." />
  <LinkCard title="Prebuilt nodes reference" href="/langgraph-guide/python/reference-prebuilt-nodes/" description="ToolNode (all constructor params, wrap_tool_call, handle_tool_errors), InjectedState, InjectedStore, ToolRuntime, ToolCallRequest, tools_condition, ToolCallTransformer, ToolCallStream." />
  <LinkCard title="Channels reference" href="/langgraph-guide/python/reference-channels/" description="LastValue, BinaryOperatorAggregate, Topic, EphemeralValue, NamedBarrierValue, AnyValue — concurrent-write semantics, Annotated[] shorthands, fan-in patterns." />
  <LinkCard title="Managed values reference" href="/langgraph-guide/python/reference-managed-values/" description="IsLastStep and RemainingSteps — read-only state fields injected by the Pregel executor for recursion-limit detection and graceful truncation." />
  <LinkCard title="Runtime, ToolRuntime & Managed Values reference" href="/langgraph-guide/python/reference-runtime-and-managed-values/" description="Runtime context injection, ToolRuntime for tools, ExecutionInfo, IsLastStep, RemainingSteps — accessing execution metadata and injecting immutable context into nodes and tools." />
  <LinkCard title="RemoteGraph reference" href="/langgraph-guide/python/reference-remote-graph/" description="Call a LangGraph Platform deployment as a local graph — same invoke/stream/get_state surface, interrupt/resume, and subgraph node wiring." />
  <LinkCard title="Class deep-dives Vol. 1" href="/langgraph-guide/python/langgraph_class_deep_dives/" description="Source-verified deep dives into 10 core types — StateGraph, CompiledStateGraph, InMemorySaver, ToolNode, create_react_agent, Command, Send, @task/@entrypoint, BinaryOperatorAggregate/Topic, InMemoryStore — each with multiple runnable examples." />
  <LinkCard title="Class deep-dives Vol. 2" href="/langgraph-guide/python/langgraph_class_deep_dives_v2/" description="Source-verified deep dives into 10 more types — RetryPolicy, CachePolicy/InMemoryCache, TimeoutPolicy, add_messages/MessagesState, tools_condition, ToolCallTransformer/ToolCallStream, StateSnapshot, IsLastStep/RemainingSteps, ToolRuntime, Runtime/RunControl — verified against langgraph==1.2.2." />
  <LinkCard title="Class deep-dives Vol. 3" href="/langgraph-guide/python/langgraph_class_deep_dives_v3/" description="Source-verified deep dives into 10 more types — interrupt/Interrupt, DeltaChannel, EphemeralValue, NamedBarrierValue, RemoveMessage/push_message, Pregel, NodeBuilder, GraphOutput, PregelTask, IndexConfig/TTLConfig — verified against langgraph==1.2.2." />
  <LinkCard title="Class deep-dives Vol. 4" href="/langgraph-guide/python/langgraph_class_deep_dives_v4/" description="Source-verified deep dives into 10 more types — set_node_defaults, add_sequence, input_schema/output_schema, context_schema/Runtime.context, get_stream_writer/StreamWriter, push_ui_message, entrypoint.final, REMOVE_ALL_MESSAGES, error_handler on add_node, error taxonomy — verified against langgraph==1.2.2." />
  <LinkCard title="Class deep-dives Vol. 5" href="/langgraph-guide/python/langgraph_class_deep_dives_v5/" description="Source-verified deep dives into 10 more types — RedisCache, EncryptedSerializer, JsonPlusSerializer, UntrackedValue, AnyValue, EmbeddingsLambda/ensure_embeddings, BaseCheckpointSaver, typed StreamParts, task.clear_cache, HumanInterrupt protocol — verified against langgraph==1.2.2." />
  <LinkCard title="Class deep-dives Vol. 6" href="/langgraph-guide/python/langgraph_class_deep_dives_v6/" description="Source-verified deep dives into 10 more types — GraphRunStream/AsyncGraphRunStream, StreamTransformer, StreamChannel, ValuesTransformer/CustomTransformer/UpdatesTransformer, GraphCallbackHandler, GraphInterruptEvent/GraphResumeEvent, GraphDrained, NodeTimeoutError, delete_ui_message/ui_message_reducer, ProtocolEvent — verified against langgraph==1.2.2." />
  <LinkCard title="Class deep-dives Vol. 7" href="/langgraph-guide/python/langgraph_class_deep_dives_v7/" description="Source-verified deep dives into 10 more types — PregelProtocol/StreamProtocol, BackgroundExecutor/AsyncBackgroundExecutor, AsyncBatchedBaseStore/_dedupe_ops, get_text_at_path/tokenize_path, SerdeEvent/register_serde_event_listener, BaseChannel, call()/SyncAsyncFuture, PregelScratchpad, StateNodeSpec/node Protocols, identifier/get_runnable_for_task — verified against langgraph==1.2.2." />
  <LinkCard title="Class deep-dives Vol. 8" href="/langgraph-guide/python/langgraph_class_deep_dives_v8/" description="Source-verified deep dives into 10 more types — ExecutionInfo/Runtime.heartbeat, ServerInfo/BaseUser, ReplayState, StreamMux, Call (functional API internals), ChannelWrite/ChannelWriteEntry, PregelRunner/FuturesDict, WritesProtocol/PregelTaskWrites, SyncPregelLoop/AsyncPregelLoop, and DuplexStream — verified against langgraph==1.2.4." />
  <LinkCard title="Class deep-dives Vol. 9" href="/langgraph-guide/python/langgraph_class_deep_dives_v9/" description="Source-verified deep dives into 10 classes new or expanded in 1.2.4 — ToolCallRequest.override(), Send+timeout, create_react_agent hooks (pre_model_hook/post_model_hook/version=v2/response_format), RetryPolicy chaining, CachePolicy custom key_func, InMemoryStore raw embedding callables, context_schema+Runtime.context, Command.PARENT cross-subgraph routing, TimeoutPolicy.coerce(), and @entrypoint multi-policy retry — verified against langgraph==1.2.4." />
  <LinkCard title="Class deep-dives Vol. 10" href="/langgraph-guide/python/langgraph_class_deep_dives_v10/" description="Source-verified deep dives into Durability checkpoint modes, NodeError/NodeCancelledError, TaskPayload/TaskResultPayload, CheckpointPayload/CheckpointTask, Item/SearchItem, GetOp/PutOp/SearchOp/ListNamespacesOp/MatchCondition, UIMessage/RemoveUIMessage, GraphOutput v2, StreamPart variants, and PregelExecutableTask/CacheKey — verified against langgraph==1.2.4." />
  <LinkCard title="Class deep-dives Vol. 11" href="/langgraph-guide/python/langgraph_class_deep_dives_v11/" description="Source-verified deep dives into InjectedState, InjectedStore, MessagesState, Overwrite, ToolOutputMixin, CheckpointMetadata, CheckpointTuple, StateUpdate, PersistentDict, and DeltaChannelHistory — verified against langgraph==1.2.4." />
  <LinkCard title="Class deep-dives Vol. 12" href="/langgraph-guide/python/langgraph_class_deep_dives_v12/" description="Source-verified deep dives into RemoteGraph/RemoteException, PostgresSaver/ShallowPostgresSaver, AsyncPostgresSaver, PostgresStore/PoolConfig, AsyncPostgresStore, ANNIndexConfig/HNSWConfig/IVFFlatConfig/PostgresIndexConfig, GraphRunStream/SubgraphRunStream, ToolCallWithContext/ToolInvocationError, LifecyclePayload/LifecycleTransformer, MessagesTransformer/CheckpointsTransformer/TasksTransformer — verified against langgraph==1.2.4." />
  <LinkCard title="Class deep-dives Vol. 13" href="/langgraph-guide/python/langgraph_class_deep_dives_v13/" description="Source-verified deep dives into ToolRuntime, ToolNode wrap_tool_call/awrap_tool_call, create_react_agent v2, TimeoutPolicy, CachePolicy, BinaryOperatorAggregate, Topic, EphemeralValue, NamedBarrierValue, and AnyValue — verified against langgraph==1.2.4." />
  <LinkCard title="Class deep-dives Vol. 14" href="/langgraph-guide/python/langgraph_class_deep_dives_v14/" description="Source-verified deep dives into BranchSpec, LastValue/LastValueAfterFinish, ManagedValue (custom managed values), @task decorator/_TaskFunction, DeltaChannel, node input schema narrowing, _NodeDefaults/set_node_defaults(), InMemoryCache/BaseCache, @entrypoint full parameter guide, and CompiledStateGraph internals — verified against langgraph==1.2.4." />
  <LinkCard title="Class deep-dives Vol. 15" href="/langgraph-guide/python/langgraph_class_deep_dives_v15/" description="Source-verified deep dives into Runtime/ExecutionInfo/RunControl/ServerInfo, BaseStore/Item/SearchItem, store batch ops (GetOp/SearchOp/PutOp/ListNamespacesOp/MatchCondition), IndexConfig/TTLConfig, UIMessage/push_ui_message/delete_ui_message, StreamTransformer/ProtocolEvent, RemoteGraph, error hierarchy (NodeError/NodeTimeoutError/NodeCancelledError/GraphDrained), IsLastStep/RemainingSteps, and HumanResponse — verified against langgraph==1.2.5." />
  <LinkCard title="Class deep-dives Vol. 16" href="/langgraph-guide/python/langgraph_class_deep_dives_v16/" description="Source-verified deep dives into the v3 streaming protocol (beta, langgraph==1.2.5): GraphRunStream, AsyncGraphRunStream, SubgraphRunStream/AsyncSubgraphRunStream, ValuesTransformer, UpdatesTransformer, CustomTransformer, MessagesTransformer/ChatModelStream, SubgraphTransformer, LifecycleTransformer/LifecyclePayload, and StreamChannel." />
  <LinkCard title="Class deep-dives Vol. 17" href="/langgraph-guide/python/langgraph_class_deep_dives_v17/" description="Source-verified deep dives into 10 practical patterns (langgraph==1.2.5): RetryPolicy sequence chaining, TimeoutPolicy+Runtime.heartbeat(), Overwrite (bypass reducers), interrupt() multi-value + selective resume by ID, add_sequence() linear pipelines, update_state()/bulk_update_state()+StateUpdate, get_stream_writer()/StreamWriter, stream_mode='checkpoints'+CheckpointPayload/CheckpointTask, stream_mode='tasks'+TaskPayload/TaskResultPayload, and set_node_defaults() graph-wide policies." />
  <LinkCard title="Class deep-dives Vol. 18" href="/langgraph-guide/python/langgraph_class_deep_dives_v18/" description="Source-verified deep dives into 10 classes with richer examples (langgraph==1.2.5): DebugTransformer+stream_mode='debug' (first full coverage), StateSnapshot history navigation, Command full API (update+goto+Send+PARENT combinations), add_messages advanced patterns (ID dedup/RemoveMessage/REMOVE_ALL_MESSAGES/format), Topic channel accumulate modes, NamedBarrierValue fan-in synchronization, entrypoint+task+previous stateful accumulation, push_message manual emission, EphemeralValue trigger channels+guard modes, and CachePolicy+InMemoryCache combined patterns with custom key_func and cache invalidation." />
  <LinkCard title="Class deep-dives Vol. 19" href="/langgraph-guide/python/langgraph_class_deep_dives_v19/" description="Source-verified deep dives into streaming internals, error taxonomy, HITL protocol & execution model (langgraph==1.2.5): CheckpointsTransformer+TasksTransformer (v3 native stream projections), StreamMessagesHandler+StreamMessagesHandlerV2 (stream_mode='messages' callback internals), _TimedAttemptScope+_AttemptContext+_AttemptEvent (timeout/retry lifecycle observer protocol), ChannelRead imperative state access, ErrorCode enum+extended exception hierarchy (EmptyInputError/TaskNotFound/GraphBubbleUp/ParentCommand), ValidationNode schema validation with migration guide, MessageGraph legacy API migration, BackgroundExecutor+AsyncBackgroundExecutor+Submit protocol, HumanInterrupt full protocol (ActionRequest/HumanInterruptConfig/HumanResponse from langchain.agents.interrupt), and PregelNode+StateNodeSpec node representation internals." />
  <LinkCard title="Class deep-dives Vol. 20" href="/langgraph-guide/python/langgraph_class_deep_dives_v20/" description="Source-verified deep dives into execution engine internals (langgraph==1.2.6): StreamToolCallHandler+ToolCallWriter (stream_mode='tools' lifecycle events and ContextVar writer), ReplayState (time-travel subgraph coordination + _is_first_visit namespace stripping), PregelScratchpad (step/stop counters, LazyAtomicCounter fields, interrupt resume list), LangGraphDeprecationWarning hierarchy (since/expected_removal versioned warnings), create_checkpoint+delta_channels_to_snapshot+empty_checkpoint (checkpoint v4 construction + DeltaChannel snapshot cadence), chain_future+run_coroutine_threadsafe (cross-loop coroutine bridge + lazy/eager task factory), validate_graph+validate_keys (reserved name checks, subscription validation, interrupt node checks), read_channel+read_channels+map_input+map_command (I/O layer mapping inputs and Commands to pending writes), should_interrupt+apply_writes+prepare_next_tasks (core Pregel algorithm: interrupt decision, deterministic write ordering, node scheduling), and LazyAtomicCounter+task_path_str+_uuid5_str/_xxhash_str (task identity utilities)." />
  <LinkCard title="Class deep-dives Vol. 21" href="/langgraph-guide/python/langgraph_class_deep_dives_v21/" description="Source-verified deep dives with expanded coverage and 3 runnable examples each (langgraph==1.2.6): BaseChannel lifecycle contract (full ABC interface + custom channel creation pattern), BinaryOperatorAggregate (Annotated reducer internals, Overwrite sentinel dual detection, _operators_equal lambda guard), UntrackedValue (guard=True/False write semantics, checkpoint()→MISSING never persisted, in-memory scratchpad pattern), register_serde_event_listener+SerdeEvent (first dedicated coverage — thread-safe serde observability hooks for audit logging and allowlist building), SerializerProtocol+CipherProtocol+SerializerCompat (custom serializer contracts, maybe_add_typed_methods backwards-compat bridge, bring-your-own cipher pattern), AsyncBatchedBaseStore (background asyncio.Queue coalescing, _check_loop deadlock prevention, weakref drainer lifetime), UIMessage+RemoveUIMessage+ui_message_reducer (full UI streaming — merge=True prop delta, remove-ui ValueError on unknown IDs, state_key=None stream-only pattern), ManagedValue+IsLastStepManager+RemainingStepsManager (custom managed value creation, PregelScratchpad.step/.stop linkage, is_managed_value TypeGuard), get_field_default+get_cached_annotated_keys+get_enhanced_type_hints (TypedDict/Pydantic/dataclass default resolution, Required/NotRequired precedence, WeakKeyDictionary MRO cache, get_update_as_tuples Pydantic model_fields_set filtering), and PregelProtocol+StreamProtocol (graph executor interface — full abstract method inventory, v1/v2 stream overloads, StreamChunk tuple anatomy, StreamProtocol __slots__ design)." />
  <LinkCard title="Class deep-dives Vol. 22" href="/langgraph-guide/python/langgraph_class_deep_dives_v22/" description="Source-verified deep dives into v3 streaming internals, custom transformers, encryption & embedding helpers (langgraph==1.2.6): StreamTransformer base contract (custom projection authoring, before_builtins, schedule()), StreamChannel (drainable queue, tee/atee fan-out, lazy-subscribe), StreamMux (central event dispatcher, factory/child pattern), GraphRunStream+AsyncGraphRunStream (v3 caller-driven streaming, interleave(), abort()), SubgraphRunStream+AsyncSubgraphRunStream (subgraph handles, parent-pump delegation), ValuesTransformer+CustomTransformer+UpdatesTransformer (native v3 projections), NamedBarrierValueAfterFinish (finish-gated fan-in), EncryptedSerializer (AES-EAX checkpoint encryption), EmbeddingsLambda+ensure_embeddings (store embedding wrappers), and push_ui_message+delete_ui_message (runtime UI streaming API)." />
  <LinkCard title="Class deep-dives Vol. 23" href="/langgraph-guide/python/langgraph_class_deep_dives_v23/" description="Source-verified deep dives into node wrappers, concurrency primitives, remote streaming & migration guide (langgraph==1.2.6): RunnableCallable+RunnableSeq (node function wrappers — sync/async dual dispatch, func_accepts injection, trace/recurse/explode_args flags), _IdleProgressCallbackHandler (idle-timeout heartbeat via every LangChain callback event — how touch() resets the idle clock, run_inline semantics), _GraphCallbackManager+_AsyncGraphCallbackManager (graph lifecycle event dispatch — configure() from config callbacks, on_interrupt/on_resume fan-out to GraphCallbackHandler subclasses), DataclassLike+TypedDictLikeV1+TypedDictLikeV2 (state schema typing protocols — runtime discrimination of TypedDict/dataclass/Pydantic), AsyncQueue+SyncQueue+Semaphore (internal concurrency primitives — wait()-without-consuming non-destructive peek, Pregel coordinator-worker signaling), FunctionNonLocals+NonLocals (AST-based closure inspection — loads-stores captures, @task/@entrypoint outer-scope analysis), Edge+TriggerEdge (graph topology NamedTuples powering draw_mermaid/draw_ascii — custom renderers and topology diffs), _RemoteGraphRunStream+_ChannelProjection+_ProjectionRegistry (remote graph streaming adapter — sync/async lanes, extensions channel projection registry), AgentState+AgentStatePydantic deprecation migration guide (moved to langchain.agents — custom TypedDict migration with add_messages+RemainingSteps+response_format), and _SubgraphRunStreamMixin (subgraph streaming internals — path/graph_name/trigger_call_id metadata, status updated by SubgraphTransformer, parent-pump delegation)." />
  <LinkCard title="Class deep-dives Vol. 24" href="/langgraph-guide/python/langgraph_class_deep_dives_v24/" description="Source-verified deep dives into debug stream payload anatomy, ToolNode injection internals, task ID algorithms, deprecation warning hierarchy, deprecated-kwarg static analysis, strict-msgpack error handling, streaming lifecycle base class, DeltaChannel snapshot sentinel, cache entry identity, and ToolCallRequest override TypedDict (langgraph==1.2.6): _DebugCheckpointPayload+_DebugTaskPayload+_DebugTaskResultPayload+DebugPayload (stream_mode='debug' typed envelopes — payload field anatomy, combined-mode tracing patterns), _InjectedArgs+_DirectlyInjectedToolArg (ToolNode injection map built at __init__ — InjectedState/InjectedStore/InjectedToolArg source discrimination, _DirectlyInjectedToolArg sentinel NamedTuple prevents double-injection), _TaskIDFn+_uuid5_str+_xxhash_str+task_path_str (task ID Protocol — UUID5 SHA-1 vs XXH3-128 deterministic generation, path-tuple to sortable dot-separated string, LANGGRAPH_TASK_ID_HASH env var), LangGraphDeprecationWarning+LangGraphDeprecatedSinceV05/V10/V11 (versioned warning hierarchy — machine-readable since/expected_removal fields, V11 new in 1.2.6, stacklevel=3 correct attribution, catch-and-filter patterns), DeprecatedKwargs (empty TypedDict enabling pyright/mypy static-analysis warnings on deprecated kwargs — zero-runtime-overhead deprecation), InvalidModuleError (strict-msgpack unknown-type error raised when LANGGRAPH_STRICT_MSGPACK=true — allowlist-building workflow with SerdeEvent listeners), _TasksLifecycleBase (shared template-method base for LifecycleTransformer+SubgraphTransformer — _task_started/_task_finished/_task_errored hooks, custom lifecycle observer pattern), _DeltaSnapshot (NamedTuple stored in checkpoint blobs as DeltaChannel ancestor-walk sentinel — fields: channel, step, value, how it terminates history reconstruction), CacheKey (ns+key+ttl NamedTuple identifying cache entries — custom key_func producing CacheKey, TTL-isolated user namespaces, task.clear_cache integration), and _ToolCallRequestOverrides (TypedDict driving ToolCallRequest.override() — tool_call/tool/state fields, interceptor chain pattern, testing with override())." />
  <LinkCard title="Class deep-dives Vol. 25" href="/langgraph-guide/python/langgraph_class_deep_dives_v25/" description="Source-verified deep dives into cache architecture, node protocols, stream bridges & internal constants (langgraph==1.2.6): BaseCache+FullKey+Namespace (pluggable cache ABC — 6-method sync/async contract, (Namespace, str) tuple-addressed key space, custom serde injection, implementing a TTL-aware DictCache backend), StateNode union type + full Protocol family (all 9 add_node() Protocol variants — _Node through _NodeWithRuntime, StateNodeSpec dataclass with defer/ends/is_error_handler/error_handler_node fields), _freeze+default_cache_key (cache key canonicalization — depth-limited deep-freeze, dict key-sorting, numpy escape via .tobytes(), protocol-5 pickle determinism), UUID v6+uuid6 (checkpoint ID generation — monotonic timestamp encoding for DB index locality, _last_v6_timestamp guard, subsec fractional field, why v6 beats v4 for checkpoints), convert_to_protocol_event (v2→v3 stream bridge — StreamPart→ProtocolEvent conversion, interrupts passthrough, wall-clock timestamp vs monotonic seq ordering), default_retry_on (default RetryPolicy predicate — blocklist of non-retryable builtins, httpx/requests 5xx detection, retry-everything-not-in-blocklist strategy), internal Pregel constants (sys.intern() reserved strings — INPUT/INTERRUPT/RESUME/ERROR/TASKS/RETURN, CONFIG_KEY_SEND/READ/CALL/CHECKPOINTER/STREAM, roles in write protocol and config.configurable routing), map_debug_tasks+is_multiple_channel_write (debug stream helpers — metadata key filtering, TAG_HIDDEN suppression, $writes multi-write payload detection), ensure_message_ids+_is_message_dict+_state_values (pre-checkpoint message coercion — in-place ID stamping before background thread, OpenAI-role/LangChain-type detection, dict/BaseModel/dataclass value extraction), and coerce_timeout_policy+sync_timeout_unsupported (TimeoutPolicy normalization — float/timedelta/policy coercion, sync-cancellation hard limit, async-only timeout pattern)." />
  <LinkCard title="Class deep-dives Vol. 26" href="/langgraph-guide/python/langgraph_class_deep_dives_v26/" description="Source-verified deep dives into channels, callbacks, cache & prebuilt streaming (langgraph==1.2.6): GraphCallbackHandler+GraphInterruptEvent+GraphResumeEvent (public lifecycle callback surface — frozen event dataclasses, on_interrupt/on_resume hooks, get_sync/async_graph_callback_manager_for_config() factories, config-based injection via config['callbacks']), DeltaChannel (beta reducer channel — replay_writes() batching-invariant contract, snapshot_frequency cadence, three-path from_checkpoint() handling MISSING/DeltaSnapshot/plain-value migration, Overwrite reset semantics), InMemoryCache (thread-safe TTL cache — threading.RLock, UTC timestamp expiry, (enc, bytes, expiry) storage triple, serde round-trip via dumps_typed/loads_typed, namespace-scoped clear()), LastValue+LastValueAfterFinish (per-step channels — INVALID_CONCURRENT_GRAPH_UPDATE guard on >1 write/step, finish() gates is_available(), consume() resets, (value, bool) checkpoint tuple), BranchSpec (conditional edge NamedTuple — path/ends/input_schema, from_path() factory for dict/list path_map, _get_branch_path_input_schema() type-hint introspection, open vs fixed-map routing), JsonPlusSerializer (ormsgpack primary format — pickle_fallback, LANGGRAPH_STRICT_MSGPACK allowlist, _is_safe_json_type legacy-resume gate, _warn_once circuit breaker, with_msgpack_allowlist() extension), ToolCallStream+ToolCallTransformer (per-call streaming handles — output_deltas StreamChannel, _bind_pump/_bind_apump wiring, process() event routing, finalize()/fail() teardown, scope namespace tuple), AnyValue (last-write-wins channel — update([]) clears to MISSING unlike LastValue, __eq__ matches all AnyValue instances), NamedBarrierValue+NamedBarrierValueAfterFinish (barrier fan-in channels — names/seen set tracking, consume() resets seen=set(), explicit finish() two-stage gate, (seen, bool) checkpoint tuple), and EphemeralValue (single-step ephemeral channel — guard=True one-write-per-step enforcement, update([]) auto-clear to MISSING, trigger-channel pass-through pattern)." />
  <LinkCard title="Class deep-dives Vol. 27" href="/langgraph-guide/python/langgraph_class_deep_dives_v27/" description="Source-verified deep dives into channels, store primitives, managed values, tool interceptors & node errors (langgraph==1.2.6): BinaryOperatorAggregate (reducer channels — Overwrite escape hatch, MISSING bootstrap, lambda equality rule, 3 runnable examples), Topic (PubSub channel — accumulate=True vs False, list vs scalar write, backwards-compat tuple checkpoint, fan-in barrier pattern), UntrackedValue (never-checkpointed transient channel — guard=True singleton enforcement, checkpoint always returns MISSING, guard=False last-write-wins), BaseStore+Item+SearchItem (cross-thread long-term memory base — batch(ops) two-method contract, namespace/key/value data model, score field on SearchItem, InvalidNamespaceError rules), PutOp+GetOp+SearchOp+ListNamespacesOp (store batch NamedTuples — TTL minutes, index path syntax, filter operators $eq/$ne/$gt/$gte/$lt/$lte, max_depth truncation, pagination via offset), InMemoryStore (in-memory KV+cosine store — _data/_vectors defaultdict layout, ThreadPoolExecutor sync embedding, asyncio.gather async embedding, numpy fast path, max-pooling dedup for multi-field keys), ManagedValue+ManagedValueSpec (abstract scratchpad protocol — @staticmethod get(scratchpad), read-only by design, custom managed value authoring pattern), IsLastStepManager+RemainingStepsManager (built-in managed values — step/stop scratchpad fields, is_last=step==stop-1, remaining=stop-step, one-step warning window before GraphRecursionError), ToolCallRequest+ToolCallWrapper+ToolInvocationError (ToolNode interceptor API — immutable .override() dataclass method, sync/async wrapper callable types, retry-on-error patterns, ToolInvocationError filters injected-arg errors), and NodeError+NodeCancelledError+NodeTimeoutError (node-level failure context — frozen dataclass injection via error_handler, asyncio.CancelledError boxing, idle vs run timeout discrimination with kind/elapsed/timeout fields)." />
  <LinkCard title="Class deep-dives Vol. 28" href="/langgraph-guide/python/langgraph_class_deep_dives_v28/" description="Source-verified deep dives into config utilities, TypeVars, internal tags, checkpoint internals & serialization helpers (langgraph==1.2.7): merge_configs/patch_config/patch_configurable (RunnableConfig composition), ensure_config (safe config normalization), ContextT/StateT TypeVars (generic node typing), TAG_NOSTREAM/TAG_HIDDEN (suppress stream output per-node), recast_checkpoint_ns/filter_to_user_tags (subgraph namespace rewriting), InMemorySaver storage internals (in-memory checkpoint layout), PendingWrite/WRITES_IDX_MAP (write queue internals), get_checkpoint_id/get_checkpoint_metadata (checkpoint identity helpers), build_serde_allowlist (safe-msgpack allowlist construction), and internal constants." />
  <LinkCard title="Class deep-dives Vol. 29" href="/langgraph-guide/python/langgraph_class_deep_dives_v29/" description="Source-verified deep dives into graph topology, introspection, tool exposure & message helpers (langgraph==1.2.7): Edge/TriggerEdge/draw_graph (graph topology NamedTuples powering Mermaid/ASCII renderers), get_graph(xray=)/draw_mermaid (subgraph expansion and Mermaid diagram generation), Pregel.as_tool() beta (expose a compiled graph as a LangChain tool), get_subgraphs (enumerate embedded subgraph handles), schema introspection JSONSchema methods (input_schema/output_schema/config_schema), clear_cache/aclear_cache (@task cache invalidation), _messages_delta_reducer/_add_messages_wrapper (add_messages internals — ID dedup, RemoveMessage handling), add_node(defer=True) (deferred-trigger node scheduling), and REMOVE_ALL_MESSAGES sentinel (wipe entire messages list in one update)." />
  <LinkCard title="Class deep-dives Vol. 30" href="/langgraph-guide/python/langgraph_class_deep_dives_v30/" description="Source-verified deep dives into 10 practical patterns with 3 runnable examples each (langgraph==1.2.7): Pregel.abatch()/abatch_as_completed() (concurrent ainvoke — completion-order iterator, return_exceptions=True), Pydantic BaseModel/@dataclass state schemas (constructor coercion, model_fields_set partial updates, @field_validator), ToolNode handle_tool_errors callable ((exc)->str error formatter, False re-raises, messages_key), @task(timeout=TimeoutPolicy) (async-only; per-attempt fresh clock; NodeTimeoutError.kind='run'/'idle'), BinaryOperatorAggregate dict-merge/set-union reducers + Overwrite bypass, get_stream_writer() (ContextVar StreamWriter — custom mode events from any node or @task), StateGraph.compile(durability=) (Durability 'sync'/'async'/'exit' checkpoint timing), ToolCallRequest.override() + wrap_tool_call middleware pattern (logging/injection/approval-gate), Pregel.get_state()/get_state_history()/StateSnapshot time-travel (newest-first iterator; filter/before/limit; checkpoint_id replay), and @entrypoint(checkpointer,store) + previous + runtime.store + entrypoint.final(value,save)." />
  <LinkCard title="Class deep-dives Vol. 31" href="/langgraph-guide/python/langgraph_class_deep_dives_v31/" description="Source-verified deep dives into 10 previously undocumented class groups (langgraph==1.2.7): _is_optional_type/_is_required_type/_is_readonly_type (TypedDict annotation predicates — PEP 604 union detection, Required/NotRequired/ReadOnly recursion through Annotated wrappers), get_update_as_tuples (Pydantic state-update projection — non-None values always pass through; None excluded only when default is also None and the field was not explicitly set), node Protocol variants + KWARGS_CONFIG_KEYS dispatch table (_RunnableWithConfigStore/_RunnableWithConfigWriter — six Protocol shapes; func_accepts dict auto-injects config/writer/store/runtime/previous/error into nodes), StrEnum/set_config_context/create_task_in_config_context (pre-3.11 string enum backport; copy_context() ContextVar propagation; asyncio task config inheritance), _create_root_model/_remap_field_definitions/create_model (RootModel factory with custom schema title; reserved-name escaping via private_ prefix + alias; LRU-cached model creation), is_supported_by_pydantic/get_fields (container type detection — dataclass/BaseModel/TypedDict; Pydantic v1/v2 field access shim), map_output_values/map_output_updates (output projection layer — full snapshot for stream_mode='values'; per-task deltas for 'updates'; TAG_HIDDEN suppression; RETURN sentinel), PregelNode.copy()/flat_writers/node (node container assembly — copy() clears cached properties; consecutive ChannelWrites merged; RunnableSeq pipeline built), _ensure_future/_convert_future_exc (Python-version-gated task factory: no-context/context/eager three paths; concurrent.futures→asyncio exception mapping), _copy_future_state/_set_concurrent_future_state (cross-executor result bridging — no-op guard on done destination; cancellation propagation between asyncio.Future and concurrent.futures.Future)." />
  <LinkCard title="Class deep-dives Vol. 34" href="/langgraph-guide/python/langgraph_class_deep_dives_v34/" description="Source-verified deep dives into 10 class groups (langgraph==1.2.7): channels_from_checkpoint/achannels_from_checkpoint/_needs_replay (DeltaChannel ancestor-walk replay, batch hydration), exit_delta_task_id/delta_channels_to_snapshot (synthetic UUID for exit-mode writes, superstep snapshot triggers, DELTA_MAX_SUPERSTEPS_SINCE_SNAPSHOT env var), prepare_push_task_functional/prepare_push_task_send/sanitize_untracked_values_in_send (@task call scheduling, Send() fan-out dispatch, UntrackedValue stripping), get_new_channel_versions/find_subgraph_pregel (checkpoint delta detection, recursive subgraph BFS with closure walk), ProtocolEvent/_ProtocolEventParams (v3 event envelope anatomy — seq/event_id/method/params fields), StreamTransformer class-variable flags/transformer_requires_async (before_builtins, required_stream_modes, requires_async, supports_sync), patch_checkpoint_map/_merge_metadata (parent-namespace checkpoint ID propagation, lc_versions deep merge, DEFAULT_RECURSION_LIMIT env var), _TasksLifecycleBase template-method hooks (_on_started/_on_terminal/_record_identity/_record_pending_tool_calls, dual tool-call input shape handling), PregelRunner.commit()/_should_route_to_error_handler/_should_stop_others (checkpoint commit path, error_handler routing, _handled_exception_ids guard), and draw_graph static topology inference (simulated Pregel loop, ChannelWrite.get_static_writes, mapper stripping, xray subgraph expansion)." />
  <LinkCard title="Class deep-dives Vol. 33" href="/langgraph-guide/python/langgraph_class_deep_dives_v33/" description="Source-verified deep dives into 10 class groups (langgraph==1.2.7): InMemoryCache/BaseCache namespace-TTL caching, EphemeralValue one-step channel, DeltaChannel (beta) replay-based accumulation, NamedBarrierValue/NamedBarrierValueAfterFinish fan-in barriers, RunControl/GraphDrained cooperative drain, entrypoint.final decoupled return/save, ValuesTransformer/UpdatesTransformer/CustomTransformer v3 native stream transformers, MessagesTransformer ChatModelStream lifecycle, AnyValue relaxed last-write channel, and ExecutionInfo/ServerInfo/get_runtime execution metadata." />
  <LinkCard title="Class deep-dives Vol. 32" href="/langgraph-guide/python/langgraph_class_deep_dives_v32/" description="Source-verified deep dives into 10 previously undocumented class groups (langgraph==1.2.7): NodeBuilder fluent node assembly (subscribe_to/subscribe_only, read_from, do, write_to, meta, add_retry_policies, add_cache_policy, set_timeout, build()), Pregel.bulk_update_state()/abulk_update_state() batch checkpoint injection (StateUpdate NamedTuple; multi-superstep seeding; reducer applied across concurrent same-superstep writes), GraphRunStream.interleave() multi-projection ordered iteration (monotonic push-stamp ordering; single-consumer lock; early-break cleanup), GraphRunStream.abort()/AsyncGraphRunStream.abort() cooperative cancellation (GeneratorExit propagation; async _anext_task cancellation; idempotent), ToolRuntime.emit_output_delta() per-tool streaming (ContextVar _tool_call_writer; silent no-op outside stream_mode='tools'; JSON-serializable delta payloads), ToolRuntime.execution_info+server_info execution metadata in tools (checkpoint_id/task_id/node_attempt/node_first_attempt_time; ServerInfo assistant_id/graph_id/user from LangSmith Deployments), ToolCallWithContext Send-based parallel tool dispatch (tool_call+__type+state fields; __type double-underscore prefix avoids collision; ToolRuntime.state injection), Runtime.merge()/override()/patch_execution_info() composition (immutable dataclass composition for middleware and unit-testing; sentinel comparison for stream_writer/heartbeat defaults), SubgraphRunStream path/graph_name/status navigation (parent-pump delegation; _seen_terminal lifecycle; status transitions started→completed/failed/interrupted/drained), and AgentStateWithStructuredResponse migration to response_format= (deprecated structured_response field; langchain.agents.create_agent replacement; 5-step migration checklist)." />
</CardGrid>

<CardGrid>
  <LinkCard title="Single-page comprehensive guide" href="/langgraph-guide/python/langgraph_comprehensive_guide/" description="All chapters in one long page — best for search and printing." />
  <LinkCard title="Recipes" href="/langgraph-guide/python/langgraph_recipes/" description="RAG, support routing, research agent, doc pipelines, long-term memory conversations." />
  <LinkCard title="Diagrams" href="/langgraph-guide/python/langgraph_diagrams/" description="20+ Mermaid architecture diagrams." />
  <LinkCard title="Production guide" href="/langgraph-guide/python/langgraph_production_guide/" description="Deployment, scaling, monitoring, disaster recovery." />
  <LinkCard title="Advanced error recovery" href="/langgraph-guide/python/langgraph_advanced_error_recovery/" />
  <LinkCard title="Observability" href="/langgraph-guide/python/langgraph_observability_python/" />
  <LinkCard title="Performance optimization" href="/langgraph-guide/python/langgraph_performance_optimization/" />
  <LinkCard title="Streaming server (FastAPI)" href="/langgraph-guide/python/langgraph_streaming_server_fastapi/" />
</CardGrid>

## What's new in v1.2.x (June/July 2026)

- **v1.2.7 (July 2026)** — [Class deep-dives Vol. 34](./langgraph_class_deep_dives_v34/) adds first-ever dedicated coverage (3 runnable examples each) of `channels_from_checkpoint`/`achannels_from_checkpoint`/`_needs_replay` (`langgraph.pregel._checkpoint` — how channels are hydrated from checkpoint blobs; `_needs_replay` returns `True` only when a `DeltaChannel` value is absent — triggering the `saver.get_delta_channel_history` batch ancestor walk; all absent delta channels are batched into a single saver call, not one per channel), `exit_delta_task_id`/`delta_channels_to_snapshot` (synthetic RFC-4122 UUID that embeds the superstep number in the first UUID group so `ORDER BY task_id, idx` in Postgres preserves chronological order; `delta_channels_to_snapshot` fires when either accumulated update count ≥ `snapshot_frequency` OR supersteps since last snapshot ≥ `DELTA_MAX_SUPERSTEPS_SINCE_SNAPSHOT` env-var global cap), `prepare_push_task_functional`/`prepare_push_task_send`/`sanitize_untracked_values_in_send` (how `@task` calls become `PUSH` tasks with `in_progress_task_path=(..., True)` so interrupts bubble to the parent; `Send()` fan-out resolves the `TASKS` channel by index; `sanitize_untracked_values_in_send` strips `UntrackedValue` keys from `Send.arg` before checkpointing to prevent bloat), `get_new_channel_versions`/`find_subgraph_pregel` (`get_new_channel_versions` returns the strict-version-increase subset used to select which channels to write in a checkpoint delta; `find_subgraph_pregel` BFS traverses `RunnableSequence`/`RunnableLambda`/`RunnableCallable` closures to find an embedded `PregelProtocol`, excluding `Pregel` instances with `checkpointer=False`), `ProtocolEvent`/`_ProtocolEventParams` (full TypedDict anatomy — `type="event"`, `seq` monotonic counter from root `StreamMux` for total ordering vs wall-clock `timestamp`, `method` = StreamMode value, `params.namespace`/`timestamp`/`data`/`interrupts`, `event_id` NotRequired wire field; `convert_to_protocol_event` bridges v2 `StreamPart` without stamping `seq` — that's done by the mux), `StreamTransformer` class-variable flags/`transformer_requires_async` (`before_builtins=True` runs the transformer before `MessagesTransformer`/`ToolCallTransformer` for content-mutating use cases; `required_stream_modes` unions across all registered transformers to auto-request the right modes; `requires_async=True` or any async-lane override → async-only; `supports_sync=True` + async overrides → sync/async dual lane; `transformer_requires_async()` predicate used by the mux at registration), `patch_checkpoint_map`/`_merge_metadata` (`patch_checkpoint_map` adds a `{parent_ns: checkpoint_id}` entry to `configurable["checkpoint_map"]` when transitioning into a child subgraph — enables `Command.PARENT` cross-graph reads; `_merge_metadata` deep-merges `lc_versions` one level while shallow-copying all other mapping values; `DEFAULT_RECURSION_LIMIT` defaults to 10007, overridable via `LANGGRAPH_DEFAULT_RECURSION_LIMIT`), `_TasksLifecycleBase` template-method hooks (shared bookkeeping for `LifecycleTransformer` and `SubgraphTransformer`; `_on_started(ns, graph_name, trigger_call_id)` fired once per discovered namespace; `_on_terminal(ns, status, error)` fired when parent `TaskResultPayload` arrives or via `finalize`/`fail` safety-net; `_record_pending_tool_calls` harvests `task_id → tool_call_id` from both current push-model and legacy batched input shapes; `process()` always returns `False` — tasks events suppressed from main log), `PregelRunner.commit()`/`_should_route_to_error_handler`/`_should_stop_others` (commit path branches on exception type: `CancelledError` persists `(ERROR, exc)` so the loop can finish; `GraphInterrupt` persists `INTERRUPT` + `RESUME` writes; `GraphBubbleUp` skips persistence; other errors route to error_handler if `_should_route_to_error_handler` is true, appending `ERROR_SOURCE_NODE` write and adding `id(exc)` to `_handled_exception_ids`; `_should_stop_others` skips futures in `_handled_exception_ids` to avoid double-cancel), and `draw_graph` static topology inference (runs a simulated Pregel loop with `empty_checkpoint()` and empty input `{}` without executing node callables; strips node mappers before simulation; probes `ChannelWrite.get_static_writes` for declared conditional edges; `END` writes become direct `Edge` entries rather than channel writes; `get_next_version` borrowed from the configured checkpointer or falls back to `increment`). Also in this release: [Class deep-dives Vol. 33](./langgraph_class_deep_dives_v33/) adds source-verified coverage (3 runnable examples each) of `InMemoryCache`/`BaseCache` (namespace-TTL cache, thread-safe `threading.RLock`, `(enc, bytes, expiry)` triple, serde round-trip, namespace-scoped `clear()`), `EphemeralValue` (one-superstep trigger channel — `guard=True` one-write-per-step, `update([])` auto-clears, pass-through semantics), `DeltaChannel` (β) (replay-based accumulation — `replay_writes()` batching-invariant, `snapshot_frequency` cadence, three-path `from_checkpoint()` handling), `NamedBarrierValue`/`NamedBarrierValueAfterFinish` (fan-in gate — `names`/`seen` tracking, `NamedBarrierValueAfterFinish` two-stage `finish()` gate), `RunControl`/`GraphDrained` (cooperative graceful drain — `drain_requested`, `drain_reason`, `GraphDrained` exception), `entrypoint.final` (decouple caller return from checkpoint `save`), `ValuesTransformer`/`UpdatesTransformer`/`CustomTransformer` (v3 native stream projections), `MessagesTransformer`/`ChatModelStream` (per-LLM-call handle lifecycle), `AnyValue` (last-write-wins with `update([])` clear), and `ExecutionInfo`/`ServerInfo`/`get_runtime()` (execution metadata injected into every node).
- **v1.2.7 (July 2026)** — [Class deep-dives Vol. 32](./langgraph_class_deep_dives_v32/) adds first-ever dedicated coverage (3 runnable examples each) of `NodeBuilder` (`langgraph.pregel.main` — fluent builder for `PregelNode`: `subscribe_to`/`subscribe_only`, `read_from` passive reads, `do` runnable chaining, `write_to` channel writes, `meta` tags/metadata, `add_retry_policies`, `add_cache_policy`, `set_timeout`, `build()`; plus `subscribe_to` vs `read_from` distinction for trigger-or-read vs read-only channels), `Pregel.bulk_update_state()`/`abulk_update_state()` (batch checkpoint injection without running any nodes — `StateUpdate(values, as_node, task_id)` NamedTuple; outer list = supersteps; inner list = concurrent task writes in same superstep; reducers applied across concurrent writes; async twin `abulk_update_state` for ASGI contexts; delegates to subgraph when `checkpoint_ns` set), `GraphRunStream.interleave()` (multi-projection ordered iteration — items yielded in strict monotonic push-stamp arrival order across named `StreamChannel` projections; single-consumer lock; early-break cleanup releases lock; mux pump called automatically when buffers empty), `GraphRunStream.abort()`/`AsyncGraphRunStream.abort()` (cooperative early cancellation — sync: `close()` propagates `GeneratorExit` into in-flight nodes + `mux.close()` drains projection channels; async: cancels `_anext_task`, `aclose()` graph iterator, `mux.aclose()`; both idempotent; `abort()` called automatically by `stream_events()` context manager on exit), `ToolRuntime.emit_output_delta()` (per-tool streaming via `_tool_call_writer` ContextVar installed by `StreamToolCallHandler.on_tool_start` — silent no-op when `stream_mode!="tools"` so no guarding needed; JSON-serializable `delta` payload appears as `tool-output-delta` event; surfaced on `ToolCallStream.output_deltas` when using `ToolCallTransformer`), `ToolRuntime.execution_info`+`server_info` (execution metadata injected into tools — `ExecutionInfo.checkpoint_id`/`task_id`/`node_attempt`/`node_first_attempt_time` for idempotency checks and retry discrimination; `ServerInfo.assistant_id`/`graph_id`/`user` from LangSmith Deployments for multi-tenant routing; `None` in local/open-source environments), `ToolCallWithContext` (internal `TypedDict` bundling `ToolCall`+`__type`+`state` — `__type: "tool_call_with_context"` double-underscore prefix avoids collision with user state `"type"` key; `ToolNode` detects it and injects `state` into `ToolRuntime.state` for read access; enables parallel HITL dispatch via Send API where each task carries a frozen state snapshot), `Runtime.merge()`/`override()`/`patch_execution_info()` (immutable `Runtime` composition — `merge` prefers other's non-null/non-default-sentinel values for clean middleware chaining; `override` wraps `dataclasses.replace` for targeted field swap in tests; `patch_execution_info` returns new runtime with patched `ExecutionInfo` fields; all three pure; original unchanged), `SubgraphRunStream.path`/`graph_name`/`status` (subgraph handle metadata in v3 streams — `path: tuple[str, ...]` is the checkpoint-namespace tuple; `graph_name: str | None` is the registered name; `status` transitions `"started"→"completed"/"failed"/"interrupted"/"drained"`; `error: BaseException | None` on failure; `trigger_call_id` for functional-API `@task`-spawned subgraphs; `_pump_next` delegates to parent pump so iterating subgraph projections drives the root graph), and `AgentStateWithStructuredResponse` migration (deprecated `LangGraphDeprecatedSinceV10` — `structured_response: StructuredResponse` field; replace `state_schema=AgentStateWithStructuredResponse` with `response_format=YourPydanticModel` on `langchain.agents.create_agent`; `result["structured_response"]` access path unchanged; 5-step migration checklist). Also in this release: [Class deep-dives Vol. 31](./langgraph_class_deep_dives_v31/) adds first-ever dedicated coverage (3 runnable examples each) of `_is_optional_type`/`_is_required_type`/`_is_readonly_type` (`langgraph._internal._fields` — TypedDict annotation predicates: PEP 604 `str | None` detection, `Required`/`NotRequired`/`ReadOnly` recursion through `Annotated` wrappers), `get_update_as_tuples` (Pydantic state-update projection — non-`None` field values always pass through; a `None` value is excluded only when the field default is also `None` and the field was not explicitly set; dataclass/non-Pydantic inputs pass all non-`MISSING` `getattr` values including `None`), node Protocol variants + `KWARGS_CONFIG_KEYS` dispatch table (`_RunnableWithConfigStore`, `_RunnableWithConfigWriter`, `_RunnableWithConfigWriterStore` — six Protocol shapes; `KWARGS_CONFIG_KEYS` tuple drives `func_accepts` dict built at `__init__`; `config`/`writer`/`store`/`runtime`/`previous`/`error` auto-injection), `StrEnum`/`set_config_context`/`create_task_in_config_context` (pre-3.11 string enum backport; `copy_context()`-based ContextVar propagation so nested calls see parent config; asyncio task config inheritance via `context.run`), `_create_root_model`/`_remap_field_definitions`/`create_model` (Pydantic `RootModel` factory with custom `schema()`/`model_json_schema()` title; reserved-name escaping via `private_` prefix + `alias`/`serialization_alias`; `@lru_cache(maxsize=256)` model creation), `is_supported_by_pydantic`/`get_fields` (container type detection — `is_dataclass`/`BaseModel`/`TypedDict`; Python ≥ 3.12 gate for `typing.TypedDict`; Pydantic v1/v2 shim), `map_output_values`/`map_output_updates` (output projection layer — `map_output_values` yields full channel snapshot; `map_output_updates` groups by task name, respects `TAG_HIDDEN`, handles `RETURN` sentinel, dedupes multi-write tasks), `PregelNode.copy()`/`flat_writers`/`node` (node container: `copy()` pops cached properties before `__init__`; `flat_writers` merges consecutive `ChannelWrite` instances; `node` assembles `RunnableSeq(bound, *writers)` or short-circuits to single writer), `_ensure_future`/`_convert_future_exc` (`CONTEXT_NOT_SUPPORTED` / `EAGER_NOT_SUPPORTED` version gates; three `create_task` paths; `concurrent.futures` exception mapping), and `_copy_future_state`/`_set_concurrent_future_state` (bidirectional future bridge — no-op guard on done destination; cancellation propagation across executor boundaries).
- **v1.2.7 (June 2026)** — Patch release; no new public API surface. [Class deep-dives Vol. 30](./langgraph_class_deep_dives_v30/) adds first-ever dedicated coverage (3 runnable examples each) of `Pregel.abatch()`+`abatch_as_completed()` (concurrent `ainvoke` — inherited from LangChain `Runnable`, `max_concurrency` in config, `return_exceptions=True` prevents first failure from cancelling others, `abatch_as_completed` yields `(index, result)` in completion order), Pydantic `BaseModel`/`@dataclass` as `state_schema` (constructor coercion via `schema(**dict)`, `model_fields_set` tracks which fields a node explicitly set so only those are written back, `@field_validator` runs on every merge), `ToolNode(handle_tool_errors=callable)` (callable signature `(exc: Exception) -> str` — returned string becomes `ToolMessage.content` with `status="error"`, `False` re-raises, `messages_key` for non-standard state keys), `@task(timeout=TimeoutPolicy)` (async-only — `TimeoutPolicy(run_timeout=, idle_timeout=, refresh_on=)` from `langgraph.types`, per-attempt fresh clock on each retry, `NodeTimeoutError.kind` is `"run"` or `"idle"`), `BinaryOperatorAggregate` dict-merge/set-union reducers with `Overwrite` bypass (`Overwrite(value)` skips the reducer entirely, `MISSING` bootstrap on first step), `get_stream_writer()` from `langgraph.config` (reads `StreamWriter` from the `Runtime` ContextVar — works inside any node or `@task`, emits to `stream_mode="custom"`), `StateGraph.compile(durability=)` with `Durability` literal (`"sync"` checkpoints after each step, `"async"` checkpoints in background while next step starts, `"exit"` checkpoints only on completion), `ToolCallRequest.override()` + `wrap_tool_call` middleware pattern (middleware-style: `(req, call_next) -> ToolMessage|Command`; `.override(tool_call=, tool=, state=)` returns new immutable instance; patterns: logging, tenant injection, approval gate), `Pregel.get_state()`/`get_state_history()`/`StateSnapshot` time-travel (`StateSnapshot` is a `NamedTuple` with `values/next/config/metadata/tasks/interrupts`, history iterator is newest-first with `filter=/before=/limit=`, replay by passing `snapshot.config` back to `invoke`), and `@entrypoint(checkpointer=, store=)` + `previous` + `runtime.store` + `entrypoint.final(value=, save=)` (functional API with persistence — `previous` is the last return value on the same thread, `runtime.store` is the injected `BaseStore`, `entrypoint.final` decouples the caller return from what is checkpointed as `previous`). [Class deep-dives Vol. 29](./langgraph_class_deep_dives_v29/) and [Vol. 28](./langgraph_class_deep_dives_v28/) are also verified against this release.
- **v1.2.6 (June 2026)** — [Class deep-dives Vol. 27](./langgraph_class_deep_dives_v27/) adds first-ever dedicated coverage of `BinaryOperatorAggregate` (reducer channel — `Overwrite` escape hatch that bypasses the operator, `MISSING` bootstrap for abstract collection types, lambda-equality rule in `__eq__`), `Topic` (PubSub channel — `accumulate=True` grows across steps while the default clears each step, scalar vs list writes both flatten correctly, backwards-compat tuple checkpoint), `UntrackedValue` (transient never-checkpointed channel — `guard=True` singleton enforcement, `checkpoint()` always returns `MISSING`, `guard=False` silently takes `values[-1]`), `BaseStore`+`Item`+`SearchItem` (cross-thread long-term memory base — `batch(ops)` two-method contract, namespace/key/value data model, `SearchItem.score` for ranked results, `InvalidNamespaceError` rules), `PutOp`+`GetOp`+`SearchOp`+`ListNamespacesOp` (store batch NamedTuples — TTL in minutes, `index` path syntax, filter operators `$eq`/`$ne`/`$gt`/`$gte`/`$lt`/`$lte`, `max_depth` truncation, `MatchCondition` wildcards), `InMemoryStore` (`_data`/`_vectors` `defaultdict` layout, `ThreadPoolExecutor` for sync embedding queries, `asyncio.gather` for async, numpy fast-path cosine similarity, max-pooling dedup for multi-field keys), `ManagedValue`+`ManagedValueSpec` (abstract scratchpad protocol — `@staticmethod get(scratchpad)`, read-only by design, custom managed value authoring pattern), `IsLastStepManager`+`RemainingStepsManager` (built-in managed values — `step`/`stop` scratchpad fields, `is_last = step == stop - 1`, `remaining = stop - step`, one-step warning window before `GraphRecursionError`), `ToolCallRequest`+`ToolCallWrapper`+`ToolInvocationError` (`ToolNode` interceptor API — immutable `.override()` dataclass method, sync/async wrapper callable types, retry-on-error patterns, `ToolInvocationError` filters injected-arg validation errors), and `NodeError`+`NodeCancelledError`+`NodeTimeoutError` (node-level failure context — `frozen=True` dataclass injection via `error_handler`, `asyncio.CancelledError` boxing, `kind="idle"/"run"` timeout discrimination with `elapsed`/`timeout` fields).
- **v1.2.6 (June 2026)** — Patch release; no new public API surface. [Class deep-dives Vol. 26](./langgraph_class_deep_dives_v26/) adds first-ever dedicated coverage of `GraphCallbackHandler`+`GraphInterruptEvent`+`GraphResumeEvent` (public lifecycle callback surface — `frozen=True` event dataclasses, `on_interrupt`/`on_resume` hooks, `get_sync/async_graph_callback_manager_for_config()` factories), `DeltaChannel` (beta reducer channel — `replay_writes()` batching-invariant contract, `snapshot_frequency` cadence, three-path `from_checkpoint()` handling MISSING/`_DeltaSnapshot`/plain-value migration, `Overwrite` reset), `InMemoryCache` deeper dive (`threading.RLock`, UTC timestamp TTL expiry, `(enc, bytes, expiry)` storage triple, `dumps_typed`/`loads_typed` round-trip, namespace-scoped `clear()`), `LastValue`+`LastValueAfterFinish` deeper dive (`INVALID_CONCURRENT_GRAPH_UPDATE` guard on >1 write/step, `finish()` gates `is_available()`, `consume()` resets, `(value, bool)` checkpoint tuple), `BranchSpec` (conditional edge `NamedTuple` — `from_path()` factory handling `dict`/`list` `path_map`, `_get_branch_path_input_schema()` type-hint introspection, open vs fixed-map routing), `JsonPlusSerializer` deeper dive (`ormsgpack` primary, `pickle_fallback`, `LANGGRAPH_STRICT_MSGPACK=true` → `allowed_msgpack_modules=None`, `_is_safe_json_type()` legacy-resume gate, `_warn_once()` circuit breaker, `with_msgpack_allowlist()` extension), `ToolCallStream`+`ToolCallTransformer` deeper dive (`output_deltas` `StreamChannel`, `_bind_pump`/`_bind_apump` wiring, `process()` event routing, `finalize()`/`fail()` teardown, `scope` namespace tuple for subgraph isolation), `AnyValue` deeper dive (`update([])` clears to `MISSING` unlike `LastValue`, `__eq__` matches all `AnyValue` instances), `NamedBarrierValue`+`NamedBarrierValueAfterFinish` deeper dive (`names`/`seen` set tracking, `consume()` resets `seen=set()`, explicit `finish()` two-stage gate, `(seen, bool)` checkpoint tuple), and `EphemeralValue` deeper dive (`guard=True` one-write-per-step enforcement, `update([])` auto-clear to `MISSING`, trigger-channel pass-through pattern).
- **v1.2.6 (June 2026)** — Actual 1.2.6 changes include nested subgraph checkpoint namespace fixes and v3 stream abort handling. [Class deep-dives Vol. 25](./langgraph_class_deep_dives_v25/) adds first-ever dedicated coverage of `BaseCache`+`FullKey`+`Namespace` (pluggable cache ABC — 6-method sync/async contract, `(Namespace, str)` tuple-addressed key space, custom serde injection), `StateNode` union + all 9 Protocol variants `_Node` through `_NodeWithRuntime` + `StateNodeSpec` (defer/ends/is_error_handler/error_handler_node), `_freeze`+`default_cache_key` (cache key canonicalization — depth-limited deep-freeze, dict key-sorting, numpy escape, protocol-5 pickle), `UUID` v6 + `uuid6()` (monotonic checkpoint ID generation — `_last_v6_timestamp` guard, DB index locality), `convert_to_protocol_event` (v2 StreamPart → v3 ProtocolEvent bridge — interrupts passthrough, seq vs timestamp ordering), `default_retry_on` (default RetryPolicy predicate — non-retryable builtin blocklist, httpx/requests 5xx detection), internal Pregel `sys.intern()` constants (`INPUT`/`INTERRUPT`/`RESUME`/`ERROR`/`TASKS`/`RETURN`, `CONFIG_KEY_SEND`/`READ`/`CALL`/`CHECKPOINTER`/`STREAM`), `map_debug_tasks`+`is_multiple_channel_write` (TAG_HIDDEN suppression, metadata filtering, `$writes` multi-write detection), `ensure_message_ids`+`_is_message_dict`+`_state_values` (pre-checkpoint in-place ID stamping, OpenAI/LangChain message detection, dict/Pydantic/dataclass extraction), and `coerce_timeout_policy`+`sync_timeout_unsupported` (float/timedelta/policy normalization, sync-cancellation hard limit, async-only timeout pattern). [Class deep-dives Vol. 24](./langgraph_class_deep_dives_v24/) adds first-ever dedicated coverage of `_DebugCheckpointPayload`+`_DebugTaskPayload`+`_DebugTaskResultPayload`+`DebugPayload` (stream_mode='debug' payload anatomy), `_InjectedArgs`+`_DirectlyInjectedToolArg` (ToolNode injection map internals), `_TaskIDFn`+`_uuid5_str`+`_xxhash_str`+`task_path_str` (deterministic task ID algorithms), `LangGraphDeprecationWarning`+`LangGraphDeprecatedSinceV05/V10/V11` (versioned warning hierarchy — V11 new in 1.2.6), `DeprecatedKwargs` (static-analysis deprecation TypedDict), `InvalidModuleError` (strict-msgpack allowlist-building), `_TasksLifecycleBase` (LifecycleTransformer+SubgraphTransformer shared base), `_DeltaSnapshot` (DeltaChannel ancestor-walk sentinel), `CacheKey` (cache entry identity NamedTuple), and `_ToolCallRequestOverrides` (ToolCallRequest override TypedDict). [Class deep-dives Vol. 23](./langgraph_class_deep_dives_v23/) adds first-ever coverage of `RunnableCallable`+`RunnableSeq` (node function wrappers), `_IdleProgressCallbackHandler` (idle-timeout heartbeat mechanics), `_GraphCallbackManager`+`_AsyncGraphCallbackManager` (lifecycle event dispatch), `DataclassLike`+`TypedDictLike*` (schema protocols), `AsyncQueue`+`SyncQueue`+`Semaphore` (concurrency primitives), `FunctionNonLocals`+`NonLocals` (AST closure analysis), `Edge`+`TriggerEdge` (graph draw primitives), `_RemoteGraphRunStream`+`_ChannelProjection` (remote streaming internals), `AgentState` migration guide (deprecated in 1.2.6), and `_SubgraphRunStreamMixin` (subgraph pump delegation). [Class deep-dives Vol. 22](./langgraph_class_deep_dives_v22/) covers v3 streaming internals (`StreamTransformer`, `StreamChannel`, `StreamMux`, `GraphRunStream`/`SubgraphRunStream`, `EncryptedSerializer`, `EmbeddingsLambda`). [Class deep-dives Vol. 21](./langgraph_class_deep_dives_v21/) adds expanded coverage (3 runnable examples each) for `BaseChannel` (custom channel ABC), `BinaryOperatorAggregate` (Overwrite sentinel mechanics), `UntrackedValue` (guard semantics + checkpoint=MISSING), `register_serde_event_listener`+`SerdeEvent` (first dedicated coverage), `SerializerProtocol`+`CipherProtocol`+`SerializerCompat`, `AsyncBatchedBaseStore` (_check_loop deadlock prevention), `UIMessage`+`RemoveUIMessage`+`ui_message_reducer` (merge semantics), `ManagedValue` custom values, `get_field_default`+`get_cached_annotated_keys` (_fields internals), and `PregelProtocol`+`StreamProtocol`. [Class deep-dives Vol. 20](./langgraph_class_deep_dives_v20/) adds source-verified documentation of existing execution engine internals (`StreamToolCallHandler`, `DeltaChannel`, `PregelScratchpad`, `LazyAtomicCounter`, and more) verified against this release.
- **v1.2.5 (June 2026)** — Patch release; `langgraph==1.2.5` with stability and dependency updates. No new public API surface. All core graph symbols verified with `-W error::DeprecationWarning`.
- **`TimeoutPolicy` dataclass** (`langgraph.types`) — fine-grained per-node/per-task timeout control with `run_timeout` (hard cap), `idle_timeout` (progress-based cap), and `refresh_on` (`"auto"` or `"heartbeat"`). Replaces the single `timeout=float` kwarg with a richer API.
- **`Runtime.heartbeat()`** — call from inside a long-running async node or task to reset an idle timeout. No-op outside an active idle-timed attempt.
- **`RunControl` + cooperative draining** — `runtime.control`, `runtime.drain_requested`, `runtime.drain_reason` let external code signal a graph run to exit gracefully without cancelling it abruptly.
- **`ExecutionInfo` extended** — new fields `checkpoint_ns` (subgraph namespace) and `task_id` (Pregel task ID) for richer per-node observability. `patch(**overrides)` helper for testing.
- **`RemainingSteps` managed value** (`langgraph.managed.is_last_step`) — companion to `IsLastStep`; gives the integer count of steps remaining before the recursion limit.
- **`task.clear_cache()` / `task.aclear_cache()`** — invalidate cached results for a specific `@task` without flushing the whole `BaseCache`.
- **`ToolRuntime` dataclass** (new in v1.2.0, `langgraph.prebuilt`) — passed to tools at execution time; exposes `state`, `context`, `config`, `stream_writer`, `tool_call_id`, `store`, `tools`, and `execution_info`. Allows tools to inspect the current graph state and write to the stream mid-execution.
- **`ToolCallTransformer` stream transformer** (new in v1.2.0, `langgraph.prebuilt`) — a `StreamTransformer` that projects raw `tools`-channel protocol events (`tool-started`, `tool-output-delta`, `tool-finished`, `tool-error`) into per-call **`ToolCallStream`** handles. Register via `builder.compile(transformers=[ToolCallTransformer])`; iterate `run.tool_calls` while streaming with `stream_mode="tools"` to receive one `ToolCallStream` per invocation. See the [Streaming modes reference](./reference-streaming-modes/#stream_modetools--per-tool-call-streaming) and [Prebuilt nodes reference](./reference-prebuilt-nodes/#toolcalltransformer--toolcallstream) for full examples.
- **`langgraph-checkpoint` 4.1.0** — minor checkpoint interface improvements; backward-compatible with all existing `MemorySaver`, `SqliteSaver`, `PostgresSaver` usage.
- **`langgraph-prebuilt` 1.1.0** — ships `ToolRuntime` and `ToolCallTransformer`; all prior prebuilt symbols (`create_react_agent`, `ToolNode`, `ValidationNode`, `InjectedState`, `InjectedStore`) unchanged.
- **v1.2.1 (May 2026)** — Patch release; stability and dependency updates. All core graph symbols (`StateGraph`, `END`, `START`, `CompiledStateGraph`, `MemorySaver`, `create_react_agent`, `ToolNode`, `InMemoryStore`, `Command`, `Send`, `Interrupt`, `interrupt`, `entrypoint`, `task`) verified with `-W error::DeprecationWarning` against installed `langgraph==1.2.2` (`.routine-envs/check-0522-langgraph`); all PASS.
- **v1.2.0 (May 12)** — All core graph symbols (`StateGraph`, `END`, `START`, `CompiledStateGraph`, `MemorySaver`, `create_react_agent`, `ToolNode`, `InMemoryStore`, `Command`, `Send`, `Interrupt`, `interrupt`, `entrypoint`, `task`) verified with `-W error::DeprecationWarning` against installed `langgraph==1.2.0` (`.routine-envs/check-0512-py`).
- **Prior (v1.1.x):** Type-safe streaming v2 API (`version="v2"` on `astream()`/`ainvoke()`); Pydantic/dataclass coercion; Python 3.10 – 3.14 support; time-travel fixes.

<Aside type="caution" title="Verified against installed library">
  Introspected against `langgraph==1.2.7`, `langgraph-checkpoint==4.1.1`,
  `langgraph-prebuilt==1.1.0`, `langgraph-checkpoint-sqlite==3.0.3`,
  `langgraph-checkpoint-postgres==3.0.5` in June 2026.

  Known corrections vs older drafts / third-party guides:

  - `langgraph.llm_hooks` and `langgraph.cache.cache_node` **do not exist**. The
    middleware system is `langchain.agents.create_agent(middleware=[...])`; see
    [Chapter 8](./chapter-08-middleware-hooks/).
  - `create_react_agent` in `langgraph.prebuilt` is **deprecated in v1.0** in
    favor of `langchain.agents.create_agent`. It still runs, with a warning.
  - `AgentState` / `AgentStatePydantic` moved from `langgraph.prebuilt` to
    `langchain.agents`.
  - `SqliteSaver.from_conn_string(...)` and `PostgresSaver.from_conn_string(...)`
    are **context managers** (`with ... as saver:`), not factories that return
    a saver directly. Some earlier examples omitted the `with`; see the
    [Checkpointers reference](./reference-checkpointers/).
  - `Interrupt.ns`, `Interrupt.when`, `Interrupt.resumable` were removed in v0.6;
    only `Interrupt.value` and `Interrupt.id` remain.
  - `checkpoint_during=False` on `invoke`/`stream` is deprecated — use
    `durability="exit"` (or `"sync"` / `"async"`).
  - `ShallowPostgresSaver` is deprecated as of `langgraph-checkpoint-postgres`
    2.0.20. Use `PostgresSaver` + `durability="exit"`.
</Aside>

## Install, minimal example

```python
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import InMemorySaver
from typing_extensions import TypedDict

class State(TypedDict):
    message: str
    response: str

def process(state: State) -> dict:
    return {"response": f"Processed: {state['message']}"}

builder = StateGraph(State)
builder.add_node("process", process)
builder.add_edge(START, "process")
builder.add_edge("process", END)

graph = builder.compile(checkpointer=InMemorySaver())

result = graph.invoke(
    {"message": "Hello"},
    config={"configurable": {"thread_id": "user-1"}},
)
print(result)
```

Ready for the full walk-through? **[Start Chapter 1 →](/langgraph-guide/python/chapter-01-setup-and-core-concepts/)**

---

## Revision history

| Date | Version | Changes |
|------|---------|---------|
| 2026-07-06 | 1.2.7 | Added Class deep-dives Vol. 34 — 10 source-verified deep dives into previously undocumented internals: channels_from_checkpoint/_needs_replay (DeltaChannel batch hydration, ancestor-walk batch opt), exit_delta_task_id/delta_channels_to_snapshot (synthetic UUID for Postgres ORDER BY, dual snapshot triggers, DELTA_MAX_SUPERSTEPS_SINCE_SNAPSHOT env var), prepare_push_task_functional/prepare_push_task_send/sanitize_untracked_values_in_send (@task and Send() scheduling, interrupt bubble path, UntrackedValue stripping), get_new_channel_versions/find_subgraph_pregel (checkpoint delta detection, closure-BFS subgraph discovery), ProtocolEvent/_ProtocolEventParams (seq/method/params anatomy, wall-clock vs monotonic ordering), StreamTransformer class-variable flags/transformer_requires_async (before_builtins, required_stream_modes, requires_async, supports_sync), patch_checkpoint_map/_merge_metadata (parent-ns checkpoint map propagation, lc_versions deep merge), _TasksLifecycleBase template-method hooks (_on_started/_on_terminal/_record_pending_tool_calls dual input shapes), PregelRunner.commit()/_should_route_to_error_handler/_should_stop_others (commit path branching, error routing, _handled_exception_ids guard), and draw_graph static topology inference (simulated Pregel loop, ChannelWrite.get_static_writes, mapper stripping). Added Class deep-dives Vol. 33 — 10 source-verified deep dives into InMemoryCache/BaseCache, EphemeralValue, DeltaChannel (β), NamedBarrierValue/NamedBarrierValueAfterFinish, RunControl/GraphDrained, entrypoint.final, ValuesTransformer/UpdatesTransformer/CustomTransformer, MessagesTransformer/ChatModelStream, AnyValue, and ExecutionInfo/ServerInfo/get_runtime(). Added Vol. 33 and Vol. 34 LinkCards to Jump-to-topic and Reference grids. Updated What's new section. |
| 2026-07-01 | 1.2.7 | Added Class deep-dives Vol. 31 — 10 source-verified deep dives into previously undocumented class groups: _is_optional_type/_is_required_type/_is_readonly_type (TypedDict annotation predicates — PEP 604 union detection, Required/NotRequired/ReadOnly recursion through Annotated wrappers, all three recurse transparently through Annotated), get_update_as_tuples (Pydantic state-update projection — non-None values always pass through; None excluded only when default is also None and field not explicitly set; dataclass/non-Pydantic: all non-MISSING getattr values pass through including None), node Protocol variants + KWARGS_CONFIG_KEYS dispatch table (_RunnableWithConfigStore/_RunnableWithConfigWriter/_RunnableWithConfigWriterStore — six Protocol shapes; KWARGS_CONFIG_KEYS tuple drives func_accepts dict; auto-injects config/writer/store/runtime/previous/error), StrEnum/set_config_context/create_task_in_config_context (pre-3.11 string enum backport; copy_context() ContextVar propagation; asyncio task config inheritance), _create_root_model/_remap_field_definitions/create_model (RootModel factory with custom title; reserved-name escaping via private_ prefix + alias; @lru_cache(maxsize=256) model creation), is_supported_by_pydantic/get_fields (container type detection; Python ≥ 3.12 gate for typing.TypedDict; Pydantic v1/v2 shim), map_output_values/map_output_updates (output projection layer — full snapshot vs per-task deltas; TAG_HIDDEN suppression; RETURN sentinel; multi-write dedup), PregelNode.copy()/flat_writers/node (node container assembly — copy() clears cached properties; consecutive ChannelWrites merged; RunnableSeq pipeline built), _ensure_future/_convert_future_exc (version-gated task factory: CONTEXT_NOT_SUPPORTED/EAGER_NOT_SUPPORTED guards; three create_task paths; concurrent.futures exception mapping), _copy_future_state/_set_concurrent_future_state (cross-executor result bridging; no-op guard on done destination; cancellation propagation). Added Vol. 31 LinkCards to Jump-to-topic and Reference grids. Added Vol. 31 What's new bullet. |
| 2026-06-26 | 1.2.6 | Added Class deep-dives Vol. 26 — 10 source-verified deep dives into channels, callbacks, cache & prebuilt streaming: GraphCallbackHandler+GraphInterruptEvent+GraphResumeEvent (frozen=True event dataclasses, on_interrupt/on_resume hooks, get_sync/async_graph_callback_manager_for_config() factories, config-based injection via config["callbacks"]), DeltaChannel (beta reducer channel — replay_writes() batching-invariant contract, snapshot_frequency cadence, three-path from_checkpoint() handling MISSING/DeltaSnapshot/plain-value migration, Overwrite reset semantics), InMemoryCache (threading.RLock, UTC timestamp TTL expiry, (enc, bytes, expiry) storage triple, serde round-trip via dumps_typed/loads_typed, namespace-scoped clear()), LastValue+LastValueAfterFinish (INVALID_CONCURRENT_GRAPH_UPDATE guard on >1 write/step, finish() gates is_available(), consume() resets, (value, bool) checkpoint tuple), BranchSpec (NamedTuple with path/ends/input_schema fields, from_path() factory for dict/list path_map, _get_branch_path_input_schema() type-hint introspection, open vs fixed-map routing), JsonPlusSerializer (ormsgpack primary format, pickle_fallback, LANGGRAPH_STRICT_MSGPACK=true sets allowed_msgpack_modules=None, _is_safe_json_type() legacy-resume gate, _warn_once() circuit breaker, with_msgpack_allowlist() extension), ToolCallStream+ToolCallTransformer (output_deltas StreamChannel, _bind_pump/_bind_apump wiring, process() event routing by event_type, finalize()/fail() teardown, scope namespace tuple for subgraph isolation), AnyValue (update([]) clears to MISSING unlike LastValue, __eq__ matches all AnyValue instances, last-write-wins), NamedBarrierValue+NamedBarrierValueAfterFinish (names/seen set tracking, raises on unknown value, consume() resets seen=set(), NamedBarrierValueAfterFinish adds explicit finish() gate, (seen, bool) checkpoint tuple), and EphemeralValue (guard=True one-write-per-step enforcement, update([]) auto-clear to MISSING, trigger-channel pass-through pattern). Added Vol. 26 LinkCards to Reference grid and Jump-to-topic grid. |
| 2026-06-25 | 1.2.6 | Added Class deep-dives Vol. 25 — 10 source-verified deep dives into previously undocumented class groups: BaseCache+FullKey+Namespace (pluggable cache ABC — 6-method sync/async contract, (Namespace, str) tuple key space, custom serde injection, DictCache implementation), StateNode union type + full Protocol family (all 9 add_node() Protocol variants — _Node through _NodeWithRuntime + StateNodeSpec defer/ends/is_error_handler/error_handler_node fields), _freeze+default_cache_key (cache key canonicalization — depth-limited deep-freeze, dict key-sorting, numpy .tobytes() escape, protocol-5 pickle), UUID v6+uuid6 (checkpoint ID generation — monotonic timestamp encoding for DB index locality, _last_v6_timestamp guard, subsec fractional field), convert_to_protocol_event (v2→v3 stream bridge — StreamPart→ProtocolEvent conversion, interrupts passthrough, wall-clock timestamp vs monotonic seq), default_retry_on (default RetryPolicy predicate — non-retryable builtin blocklist, httpx/requests 5xx detection, retry-everything-not-in-blocklist strategy), internal Pregel sys.intern() constants (INPUT/INTERRUPT/RESUME/ERROR/TASKS/RETURN, CONFIG_KEY_SEND/READ/CALL/CHECKPOINTER/STREAM — roles in write protocol and config.configurable routing), map_debug_tasks+is_multiple_channel_write (debug stream helpers — TAG_HIDDEN suppression, metadata key filtering, $writes multi-write detection), ensure_message_ids+_is_message_dict+_state_values (pre-checkpoint message coercion — in-place ID stamping before background thread, OpenAI-role/LangChain-type detection, dict/BaseModel/dataclass extraction), and coerce_timeout_policy+sync_timeout_unsupported (TimeoutPolicy normalization — float/timedelta/policy coercion, sync-cancellation hard limit, async-only timeout pattern). Added Vol. 25 LinkCards to Reference grid and Jump-to-topic grid. |
| 2026-06-24 | 1.2.6 | Added Class deep-dives Vol. 24 — 10 source-verified deep dives into previously undocumented internal class groups: _DebugCheckpointPayload+_DebugTaskPayload+_DebugTaskResultPayload+DebugPayload (stream_mode='debug' typed payload envelopes — field anatomy, combined-mode tracing, practical debug observer pattern), _InjectedArgs+_DirectlyInjectedToolArg (ToolNode injection map built at __init__ — InjectedState/InjectedStore/InjectedToolArg source discrimination, _DirectlyInjectedToolArg sentinel prevents double-injection, custom inject-your-own-value pattern), _TaskIDFn+_uuid5_str+_xxhash_str+task_path_str (deterministic task ID generation — UUID5/SHA-1 vs XXH3-128, LANGGRAPH_TASK_ID_HASH env var, path-tuple sortable string, reproducible replay IDs), LangGraphDeprecationWarning+LangGraphDeprecatedSinceV05/V10/V11 (versioned warning class hierarchy — machine-readable since/expected_removal, V11 new in 1.2.6, stacklevel=3 attribution, catch-and-filter patterns in tests), DeprecatedKwargs (empty TypedDict giving pyright/mypy static-analysis warnings on deprecated kwargs at zero runtime cost), InvalidModuleError (strict-msgpack unknown-type error raised when LANGGRAPH_STRICT_MSGPACK=true — allowlist-building workflow using SerdeEvent listeners), _TasksLifecycleBase (shared template-method base for LifecycleTransformer+SubgraphTransformer — _task_started/_task_finished/_task_errored hooks, custom lifecycle observer extending the base), _DeltaSnapshot (NamedTuple stored in checkpoint blobs as DeltaChannel ancestor-walk sentinel — channel/step/value fields, how it terminates history reconstruction), CacheKey (ns+key+ttl NamedTuple identifying cache entries — custom key_func producing CacheKey, TTL-isolated per-user namespaces, task.clear_cache() integration), and _ToolCallRequestOverrides (TypedDict driving ToolCallRequest.override() — tool_call/tool/state fields, pre-call interceptor chain pattern, testing with override() without live LLM). Added Vol. 24 LinkCard to Reference grid and Jump-to-topic grid. |
| 2026-06-23 | 1.2.6 | Added Class deep-dives Vol. 23 — 10 source-verified deep dives into previously undocumented class groups: RunnableCallable+RunnableSeq first dedicated coverage (node function wrapper — func_accepts injection detection, sync/async dual dispatch, trace/recurse/explode_args flags, RunnableSeq lightweight pipeline chaining), _IdleProgressCallbackHandler first dedicated coverage (idle-timeout heartbeat mechanics — every on_llm_new_token/on_tool_start/on_chain_start etc. calls scope.touch(), run_inline=True ordering guarantee, weakref scope-lifetime decoupling), _GraphCallbackManager+_AsyncGraphCallbackManager first dedicated coverage (graph lifecycle event dispatch — configure() from config["callbacks"], on_interrupt/on_resume fan-out to GraphCallbackHandler subclasses only, async twin with is_async=True), DataclassLike+TypedDictLikeV1+TypedDictLikeV2 first dedicated coverage (structural protocols for state schema discrimination — __dataclass_fields__ vs __required_keys__/__optional_keys__ ClassVar vs instance, how LangGraph detects schema type at runtime), AsyncQueue+SyncQueue+Semaphore first dedicated coverage (internal concurrency primitives — wait()-without-consuming non-destructive peek, SyncQueue built on deque+Semaphore, Pregel coordinator-worker signaling pattern), FunctionNonLocals+NonLocals first dedicated coverage (AST-based closure inspection — loads-stores outer-scope captures, visit_FunctionDef/AsyncFunctionDef/Lambda dispatch, practical @task factory capture analysis), Edge+TriggerEdge first dedicated coverage (graph topology NamedTuples from get_graph().edges — source/target/conditional/data fields, custom Mermaid rendering, topology diff for CI regression detection), _RemoteGraphRunStream+_ChannelProjection+_ProjectionRegistry first dedicated coverage (remote graph streaming adapter — context-manager lifecycle, _ChannelProjection sync/async lane DataDecoder, _ProjectionRegistry typed vs custom channel resolution), AgentState+AgentStatePydantic deprecation migration guide (deprecated in 1.2.6 — moved to langchain.agents, custom TypedDict migration with add_messages+RemainingSteps+response_format structured output, Pydantic model migration pattern), and _SubgraphRunStreamMixin first dedicated coverage (subgraph streaming pump-delegation internals — path/graph_name/trigger_call_id identity fields, status updated in-place by SubgraphTransformer, parent-pump delegation pattern, drilling into projections). Also added Vol. 22 LinkCard to the Jump-to-topic grid. |
| 2026-06-20 | 1.2.6 | Added Class deep-dives Vol. 21 — 10 source-verified deep dives with 3 runnable examples each, expanding coverage of previously brief topics: BaseChannel full lifecycle contract (update/get/checkpoint/from_checkpoint/consume/is_available ABC + custom channel implementation pattern), BinaryOperatorAggregate internals (Annotated reducer mechanics, Overwrite dual-detection via isinstance+OVERWRITE dict sentinel, _operators_equal lambda safety, _strip_extras ABC→concrete normalisation), UntrackedValue deeper dive (guard=True concurrent-write protection, checkpoint()→MISSING never persists a blob, from_checkpoint always returns empty, in-memory scratchpad use cases), register_serde_event_listener+SerdeEvent first dedicated coverage (thread-safe listener list with _listeners_lock, per-listener failure isolation, _MAX_WARNED_TYPES=1000 circuit breaker, use for audit logging and allowlist building), SerializerProtocol+CipherProtocol+SerializerCompat first dedicated coverage (runtime_checkable protocol, dumps_typed tuple type discriminator, SerializerCompat wraps old-style serde with type(obj).__name__ tag, maybe_add_typed_methods backwards compat, custom BYO cipher pattern), AsyncBatchedBaseStore deeper dive (_check_loop decorator raises asyncio.InvalidStateError from same loop, weakref drainer prevents store lifetime coupling, _ensure_task recreates cancelled background task), UIMessage+RemoveUIMessage+ui_message_reducer full reducer semantics (merge=True dict-merge vs replace, remove-ui ValueError on unknown IDs, state_key=None stream-only emission, metadata.merge flag), ManagedValue+IsLastStepManager+RemainingStepsManager deeper dive (get(scratchpad) staticmethod contract, PregelScratchpad.step/.stop arithmetic, custom managed value pattern, is_managed_value TypeGuard), get_field_default+get_cached_annotated_keys+get_enhanced_type_hints first dedicated coverage (Required/NotRequired precedence over total=False, WeakKeyDictionary MRO cache with reversed MRO traversal, Python 3.14 GetSetDescriptorType fallback, get_update_as_tuples model_fields_set filtering), and PregelProtocol+StreamProtocol first dedicated coverage (full abstract method inventory with v1/v2 stream overloads, StreamChunk=(namespace, mode, payload) tuple anatomy, StreamProtocol __slots__ design and modes filtering). |
| 2026-06-18 | 1.2.5 | Added Class deep-dives Vol. 19 — 10 source-verified deep dives into streaming internals, error taxonomy, HITL protocol & execution model: CheckpointsTransformer+TasksTransformer (v3 native stream projections — run.checkpoints and run.tasks attributes, scope filtering, combined audit pattern), StreamMessagesHandler+StreamMessagesHandlerV2 (stream_mode="messages" callback internals — run_inline=True ordering guarantee, dedup by message ID, TAG_NOSTREAM suppression, parent_ns subgraph forwarding), `_TimedAttemptScope`+`_AttemptContext`+`_AttemptEvent` (timeout/retry lifecycle observer protocol — immutable per-attempt context NamedTuple, lifecycle events start/progress/finish, progress observation via refresh_on="auto" vs "heartbeat", guarded write/stream/call lock after timeout), ChannelRead imperative state access (do_read() static method, str vs list channel selection, fresh=True cache bypass, mapper transform), ErrorCode enum+extended exception hierarchy (all 5 ErrorCode values, EmptyInputError, TaskNotFound, GraphBubbleUp/ParentCommand internal signals, NodeInterrupt deprecated alias), ValidationNode schema validation (pydantic/BaseTool/callable schemas, custom format_error, re-prompting loop pattern, migration to ToolNode with handle_tool_errors), MessageGraph legacy API migration (deprecated MessageGraph, StateGraph+add_messages equivalent, MessagesState shorthand), BackgroundExecutor+AsyncBackgroundExecutor+Submit protocol (`__cancel_on_exit__`/`__reraise_on_exit__`/`__next_tick__` flags, max_concurrency semaphore, GraphBubbleUp suppression timing-dependent: suppressed for tasks that complete before __exit__ snapshot, re-raised for tasks still in-flight at that point), HumanInterrupt full protocol from langchain.agents.interrupt (ActionRequest/HumanInterruptConfig/HumanResponse/accept+ignore+response+edit types, import path migration), PregelNode+StateNodeSpec node representation internals (channels/triggers/writers/bound, error_handler_node wiring, subgraph auto-detection, StateNodeSpec defer flag). |
| 2026-06-17 | 1.2.5 | Added Class deep-dives Vol. 18 — 10 source-verified deep dives with richer examples: DebugTransformer+stream_mode="debug" (first full coverage — event types, payload inspection, combined mode tracing), StateSnapshot history navigation (next/tasks/interrupts fields, get_state_history iteration, parent_config chain walking, time-travel rewind), Command full API (update+goto combining, Send fan-out in goto, Command.PARENT cross-graph state writes), add_messages advanced patterns (ID-based dedup replace-in-place, RemoveMessage targeting, REMOVE_ALL_MESSAGES clear-and-replace, format="langchain-openai" coercion), Topic channel accumulate modes (per-step clear vs. persistent log, multi-producer fanout with Send), NamedBarrierValue fan-in synchronization (parallel branch wait, Send-based fanout, reusable across loop iterations), entrypoint+task+previous stateful accumulation (running totals, conversation history, entrypoint.final to decouple return from save), push_message manual emission (streaming during execution, custom state_key, stream-only state_key=None), EphemeralValue trigger channels (guard=True single-producer signals, guard=False last-writer-wins, per-step config override), CachePolicy+InMemoryCache combined patterns (TTL caching on @task, custom key_func normalisation, per-user namespace isolation, task.clear_cache invalidation). |
| 2026-06-16 | 1.2.5 | Added Class deep-dives Vol. 17 — 10 source-verified practical patterns: RetryPolicy sequence chaining (ordered multi-policy matching, callable retry_on), TimeoutPolicy+Runtime.heartbeat() (idle_timeout with explicit heartbeats, combined with RetryPolicy), Overwrite (bypass BinaryOperatorAggregate reducer, summarisation pattern, error constraints), interrupt() multi-value and selective resume by ID (multi-step approval, id-targeted resume mapping), add_sequence() (ETL pipeline, named nodes, vs manual wiring), update_state()/bulk_update_state()/StateUpdate (state injection, multi-step replay, time-travel rewind), get_stream_writer()/StreamWriter (progress events, multi stream_mode, async warning), stream_mode="checkpoints"+CheckpointPayload/CheckpointTask (audit trails, checkpoint progression, task field inspection), stream_mode="tasks"+TaskPayload/TaskResultPayload (task lifecycle logging, interrupt detection, combined modes), set_node_defaults() (global retry/timeout/error_handler, per-node override, precedence table). Also added missing index links for Class deep-dives Vol. 13 (ToolRuntime, channels, policies), Vol. 14 (BranchSpec, LastValue, ManagedValue, @task, DeltaChannel, InMemoryCache, @entrypoint, CompiledStateGraph internals), and Vol. 15 (Runtime/ExecutionInfo/RunControl, BaseStore, store batch ops, UIMessage, StreamTransformer, RemoteGraph, error hierarchy, IsLastStep/RemainingSteps, HumanResponse). |
| 2026-06-15 | 1.2.5 | Added Class deep-dives Vol. 16 — 10 source-verified deep dives into the v3 streaming protocol (beta): GraphRunStream/AsyncGraphRunStream (caller-driven pumping, projections, context manager, abort, interleave, output/interrupted/interrupts properties), SubgraphRunStream/AsyncSubgraphRunStream (in-process subgraph navigation handles, path/status/error fields, recursive grandchild traversal), ValuesTransformer (full-state snapshots on run.values, scope filtering), UpdatesTransformer (per-node deltas on run.updates), CustomTransformer (get_stream_writer payloads on run.custom), MessagesTransformer/ChatModelStream (per-LLM-call handles with .text/.reasoning/.tool_calls/.output projections, token streaming), SubgraphTransformer (subgraph discovery via tasks events, direct-children-only scope, mini-mux child creation), LifecycleTransformer/LifecyclePayload (all-depth subgraph lifecycle events, error detection), and StreamChannel (single-consumer drainable queue, tee fan-out, named auto-forwarding). Updated version card to 1.2.5, "What's new" section, Reference Aside, and Jump-to-topic grid. |
| 2026-06-07 | 1.2.4 | Added Class deep-dives Vol. 12 — 10 source-verified deep dives into RemoteGraph/RemoteException (call deployed graphs as nodes, state management, distributed tracing), PostgresSaver/ShallowPostgresSaver (full vs constant-space persistence, pipeline mode, connection pooling, time-travel), AsyncPostgresSaver (async twin with asyncio.Lock and AsyncConnectionPool), PostgresStore/PoolConfig (TTL expiry, sweeper thread, InjectedStore in tools), AsyncPostgresStore (AsyncBatchedBaseStore coalescing, concurrent aput/aget), ANNIndexConfig/HNSWConfig/IVFFlatConfig/PostgresIndexConfig (pgvector index types, halfvec, distance metrics), GraphRunStream/SubgraphRunStream (v3 streaming projections, token streaming, abort(), interleave()), ToolCallWithContext/ToolInvocationError (tool dispatch internals, custom error handlers, categorised errors), LifecyclePayload/LifecycleTransformer (subgraph lifecycle monitoring, cause tracking, timing traces), and MessagesTransformer/CheckpointsTransformer/TasksTransformer (per-LLM-call streaming handles, checkpoint flush events, task execution tracing). Updated Jump-to-topic grid and Reference section with Vol. 10–12 links. |
| 2026-06-04 | 1.2.4 | Added Class deep-dives Vol. 9 — 10 source-verified deep dives into ToolCallRequest.override() (immutable interceptor mutation), Send+timeout (per-task fan-out timeouts), create_react_agent hooks (pre_model_hook, post_model_hook, version="v2", response_format), RetryPolicy chaining (ordered list of policies), CachePolicy custom key_func (per-user namespace isolation), InMemoryStore raw embedding callables (no LangChain dependency), context_schema+Runtime.context (typed run-scoped context in StateGraph and @entrypoint), Command.PARENT (cross-subgraph routing with error escalation), TimeoutPolicy.coerce() (run_timeout vs idle_timeout), and @entrypoint multi-policy retry. Updated reference files to v1.2.4. |
| 2026-06-03 | 1.2.4 | Added Class deep-dives Vol. 8 — 10 source-verified deep dives into ExecutionInfo/Runtime.heartbeat (advanced patterns: idempotency keys, node_attempt timing, patch() for testing), ServerInfo/BaseUser (platform auth and tenant isolation), ReplayState (time-travel subgraph checkpoint tracking), StreamMux (streaming event dispatcher, transformer factories, child muxes), Call (functional API invocation internals), ChannelWrite/ChannelWriteEntry (write subsystem, do_write() imperative writes, mapper transforms), PregelRunner/FuturesDict (concurrent task execution, error routing, early cancellation), WritesProtocol/PregelTaskWrites (write protocol, deterministic sorting via path), SyncPregelLoop/AsyncPregelLoop (execution state machine, status fields, cooperative drain), and DuplexStream (multi-sink fan-out streaming). Updated version card to 1.2.4, version references in Reference section and Aside block, langgraph-checkpoint to 4.1.1. |
| 2026-05-31 | 1.2.2 | Added Class deep-dives Vol. 5 — 10 source-verified deep dives into RedisCache, EncryptedSerializer, JsonPlusSerializer (strict mode), UntrackedValue, AnyValue, EmbeddingsLambda/ensure_embeddings (with path expressions), BaseCheckpointSaver (custom backend), typed StreamParts (ValuesStreamPart, UpdatesStreamPart, MessagesStreamPart, CheckpointStreamPart, TasksStreamPart), task.clear_cache/aclear_cache, and the HumanInterrupt structured HITL protocol with migration guidance to langchain.agents.interrupt. Updated index Jump-to-topic grid and Reference links. |
| 2026-05-30 | 1.2.2 | Added Class deep-dives Vol. 4 — 10 source-verified deep dives into set_node_defaults, add_sequence, input_schema/output_schema, context_schema/Runtime.context, get_stream_writer/StreamWriter, push_ui_message, entrypoint.final, REMOVE_ALL_MESSAGES, error_handler on add_node, and the full error taxonomy (GraphRecursionError, InvalidUpdateError, NodeTimeoutError, EmptyChannelError). Updated index Jump-to-topic grid and Reference links. Enhanced create_react_agent migration guidance in Vol. 1. |
| 2026-05-29 | 1.2.2 | Added Class deep-dives Vol. 3 — 10 source-verified deep dives into interrupt/Interrupt, DeltaChannel, EphemeralValue, NamedBarrierValue, RemoveMessage/push_message, Pregel, NodeBuilder, GraphOutput, PregelTask, IndexConfig/TTLConfig. Updated index Jump-to-topic grid and Reference links. |
| 2026-05-28 | 1.2.2 | Added Class deep-dives Vol. 2 — 10 source-verified deep dives into RetryPolicy, CachePolicy/InMemoryCache, TimeoutPolicy, add_messages/MessagesState, tools_condition, ToolCallTransformer/ToolCallStream, StateSnapshot, IsLastStep/RemainingSteps, ToolRuntime, and Runtime/RunControl (all verified against langgraph==1.2.2). Updated version card and index links. |
| 2026-05-25 | 1.2.1 | Review fixes: corrected `durability` placement (invoke/stream, not compile); fixed `context=` top-level param pattern; updated `ToolRuntime` to add missing `tools`, `execution_info`, `server_info` fields (source-verified); fixed `clear_history_node` P1 bug (preserve current message before REMOVE_ALL_MESSAGES); added None guards for `runtime.state`, `runtime.store`, `runtime.context`; removed `_DirectlyInjectedToolArg` private class from docs; fixed version history (RemainingSteps added in 1.2, not 1.0). | Claude review response |
| 2026-05-24 | 1.2.1 | Deep source dive: enhanced chapters 1, 4, 9 with `add_sequence`, `context_schema`/`Runtime`, `ToolRuntime`, `REMOVE_ALL_MESSAGES`, `push_message`, `Overwrite`, `GraphOutput v2`, `TimeoutPolicy`, `Send(timeout=)`. Updated streaming modes ref (all 7 modes + `StreamPart` v2 typed union). Updated store ref (vector search, filter operators, TTL, batch). New ref page: Runtime, ToolRuntime & Managed Values. Updated recipes to v1.2.1 with 3 new examples. All content verified against `langgraph==1.2.2` source. | Claude enhancement |
| 2026-05-22 | 1.2.1 | Patch release; version card updated 1.2.0 → 1.2.1; v1.2.1 bullet added to "What's new". All core symbols (`StateGraph`, `END`, `START`, `CompiledStateGraph`, `MemorySaver`, `create_react_agent`, `ToolNode`, `InMemoryStore`, `Command`, `Send`, `Interrupt`, `interrupt`, `entrypoint`, `task`) verified with `-W error::DeprecationWarning` against installed `langgraph==1.2.2` (`.routine-envs/check-0522-langgraph`); all PASS. | Claude routine |
| 2026-05-12 | 1.2.0 | Minor release; version card and all version references updated 1.1.10 → 1.2.0; `langgraph-checkpoint` updated 4.0.3 → 4.1.0; `langgraph-prebuilt` updated to 1.1.0. "What's new" section updated with `ToolRuntime` and `ToolCallTransformer` additions. All core symbols (`StateGraph`, `END`, `START`, `CompiledStateGraph`, `MemorySaver`, `create_react_agent`, `ToolNode`, `InMemoryStore`, `Command`, `Send`, `Interrupt`, `interrupt`, `entrypoint`, `task`) verified with `-W error::DeprecationWarning` against installed `langgraph==1.2.0` (`.routine-envs/check-0512-py`). | Claude routine |
| 2026-04-28 | 1.1.10 | Patch release; version card and reference pages updated 1.1.9 → 1.1.10; `langgraph-checkpoint` updated 4.0.2 → 4.0.3. All key symbols verified against installed 1.1.10 (`.routine-envs/main-py-0428`). | Claude routine |
| 2026-04-22 | 1.1.9 | Added six source-verified reference pages: [StateGraph](./reference-state-graph/), [Checkpointers](./reference-checkpointers/), [Store](./reference-store/), [Functional API](./reference-functional-api/), [Command / Send](./reference-command-and-send/), [Streaming modes](./reference-streaming-modes/). Corrected stale guidance: `create_react_agent` deprecated in v1.0; `from_conn_string` is a context manager; `ShallowPostgresSaver` deprecated. |
| 2026-04-21 | 1.1.8 | Index redesigned into Zero → Hero chaptered tutorial + Jump-to-topic grid; duplicated sections removed. |
| 2026-04-20 | 1.1.8 | Version pin updated to 1.1.8; frameworks.ts metadata aligned. |
| April 16, 2026 | 1.1.6 | Python 3.9 dropped; type-safe v2 streaming/invoke API; Pydantic/dataclass coercion; Python 3.14 support; time-travel fixes. |
| November 2025 | 1.0.3 | Node caching; deferred nodes; pre/post model hooks; cross-thread memory; Python 3.13 support. |
