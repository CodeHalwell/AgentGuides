# Open PR Conflict Analysis — 2026-06-05

Generated by Claude Code on 2026-06-05.

---

## 1. Open Pull Requests

| PR | Title | Author | Base Branch | Files | State |
|----|-------|--------|-------------|-------|-------|
| [#157](https://github.com/CodeHalwell/AgentGuides/pull/157) | feat(langgraph): Class deep-dives Vol. 10 — 10 source-verified types (1.2.4) | CodeHalwell | `main` | 1 | Open |
| [#158](https://github.com/CodeHalwell/AgentGuides/pull/158) | docs(google-adk): Class Deep Dives Vol. 13 — 10 source-verified class groups (2.2.0) | CodeHalwell | `main` | 2 | Open |
| [#159](https://github.com/CodeHalwell/AgentGuides/pull/159) | docs(pydantic-ai): Class Deep Dives Vol. 11 — 10 class groups, v1.106.0 | CodeHalwell | `main` | 2 | Open |
| [#160](https://github.com/CodeHalwell/AgentGuides/pull/160) | Docs refresh — 2026-06-05 | CodeHalwell | `main` | 12 | Open |
| [#161](https://github.com/CodeHalwell/AgentGuides/pull/161) | docs(microsoft-agent-framework): Class Deep Dives Vol. 8 — 10 source-verified class groups (1.8.0) | CodeHalwell | `claude/optimistic-hopper-cvgcZ` *(PR #160's branch)* | 2 | Open |

---

## 2. Merge Conflict Check

GitHub reports `mergeable_state: "clean"` for **all five PRs** — there are no hard, file-level merge conflicts at this moment.

---

## 3. Structural / Logical Conflict Risk

Although GitHub shows no file-level conflicts today, there is a **significant content-overlap risk** in the PR chain that will surface the moment any of these PRs are merged.

### 3.1 The overlap: PR #160 already contains the content of #157, #158, and #159

PR #160 ("Docs refresh — 2026-06-05") explicitly states in its description:

> *Three open PRs were found (#157 LangGraph Vol. 10, #158 Google ADK Vol. 13 + v2.2.0, #159 PydanticAI Vol. 11 + v1.106.0); **all three were merged into the working branch** before the refresh sweep.*

This means `claude/optimistic-hopper-cvgcZ` (the head of #160) already contains all commits from #157, #158, and #159, **plus** additional version-bump changes on top.

### 3.2 Merge order dependency

| Scenario | What happens |
|----------|--------------|
| **Merge #160 first, then any of #157/#158/#159** | GitHub will detect the commits as already present and likely show the PR as empty/already merged — low risk of a hard conflict, but the individual PRs become meaningless. |
| **Merge #157, #158, #159 first, then #160** | PR #160 contains those same file edits plus the version bumps on top. The version-bump hunks in #160 are applied *after* the deep-dive content, so merging to `main` after #157–#159 are already there is likely to cause **overlapping edits in `index.mdx` files** for LangGraph, Google ADK, and PydanticAI. These files are edited by both the individual PRs and #160, making a merge conflict very probable. |

### 3.3 PR #161 is stacked on PR #160

PR #161 targets `claude/optimistic-hopper-cvgcZ`, not `main`. It must not be merged to `main` directly. The intended flow is:

```
main ← #160 (docs refresh) ← #161 (MS Agent Framework Vol. 8)
```

If #160 is merged and its branch deleted before #161's base is retargeted to `main`, GitHub will be unable to compute the diff correctly for #161.

---

## 4. Files at Risk of Overlap

The following files are touched by more than one PR in the open set:

| File | PRs that touch it |
|------|------------------|
| `langgraph-guide/index.mdx` | #157 (new deep-dive Vol. 10 entry) · #160 (TS version bump 1.3.4 → 1.3.5) |
| `google-adk-guide/index.mdx` | #158 (new deep-dive Vol. 13 entry + v2.2.0 bump) · #160 (incorporates #158) |
| `pydantic-ai-guide/index.mdx` | #159 (new deep-dive Vol. 11 entry + v1.106.0 bump) · #160 (incorporates #159) |
| `anthropic-claude-agent-sdk-guide/index.mdx` | #160 only (0.2.89 → 0.2.91) |
| `anthropic-claude-agent-sdk-typescript-guide/index.mdx` | #160 only (0.3.162 → 0.3.165) |
| `langgraph-guide/typescript/index.mdx` | #160 only (1.3.4 → 1.3.5) |

---

## 5. Recommended Merge Order

To land all changes cleanly with zero conflicts:

1. **Close #157, #158, #159** (or mark them as superseded) — their content is already included in #160's branch. Merging them separately after #160 is in `main` is safe but redundant; merging them before #160 risks overlap conflicts.
2. **Merge #160** into `main` — this lands the deep-dive volumes (LangGraph Vol. 10, Google ADK Vol. 13, PydanticAI Vol. 11) plus the three version bumps in a single coherent batch.
3. **Retarget #161's base** from `claude/optimistic-hopper-cvgcZ` to `main` (use GitHub's "Edit" button on the PR, or `git rebase main`), then **merge #161** — this lands the Microsoft Agent Framework Vol. 8 deep-dive.

---

## 6. Summary

| | Count |
|--|--|
| Open PRs | 5 |
| PRs with hard merge conflicts | **0** |
| PRs with content-overlap risk | **4** (#157, #158, #159, #160) |
| PRs stacked on another PR's branch | **1** (#161 → #160) |

**Action required:** No emergency fixes needed, but merging in the wrong order will create avoidable index.mdx conflicts. Follow the merge order in §5 above.
