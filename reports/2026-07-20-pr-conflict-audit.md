# Open PR Conflict Audit — 2026-07-20

Generated by Claude Code on `claude/lucid-planck-b7n1nv`. This is a refresh of the prior audit in [`reports/2026-07-15-pr-conflict-audit.md`](./2026-07-15-pr-conflict-audit.md) (published via [#300](https://github.com/CodeHalwell/AgentGuides/pull/300), which is itself still open).

> **Scope note:** all PR counts and ranges below (41 PRs, `#264`–`#304`) reflect the open-PR list as it stood *at the time this audit was run*, before this report's own PR was opened to publish the findings. This audit's own PR is excluded from every count in this document, consistent with how `#300` excluded itself on 2026-07-15.

---

## 1. Open Pull Requests

The GitHub API returned **41 open PRs**, numbered contiguously **#264–#304** (plus `#300`, the prior audit report itself, which is not a documentation-chain PR). All 41 are authored by `CodeHalwell`, none are drafts. They form the same **four independent stacked chains** identified five days ago (Google ADK, LangGraph, Microsoft Agent Framework Python, PydanticAI), each adding one "class deep dives" volume per PR on top of the previous PR's branch — plus four brand-new PRs (`#301`–`#304`) that extend each chain by one more volume.

**Total open PRs: 41 — all by CodeHalwell.** (36 carried over from the 2026-07-15 audit, `#300` is that audit's own report PR, and `#301`–`#304` are new.)

---

## 2. Git Conflict Status

GitHub reports `mergeable_state: clean` (`mergeable: true`) for **all 41 PRs** as of this audit — no hard git text conflicts anywhere right now. Every PR's diff was checked against the actual base branch configured on the PR itself (which for most PRs is another open PR's branch, not `main`).

This matches the 2026-07-15 finding exactly: nothing has regressed, and the four new PRs (`#301`–`#304`) were all opened directly against the current tip of their respective chains, so they introduce no new duplication.

---

## 3. Stacked Chains

### 3.1 LangGraph — single clean chain (Vol. 33 → 42), no issues

```
#264 (main) → #268 → #272 → #276 → #280 → #284 → #288 → #292 → #296 → #301
```

Fully linear, no duplication, no orphans. `#301` (Vol. 42) extends the chain cleanly on top of `#296`. Can merge in numeric order.

### 3.2 Google ADK — single clean chain (Vol. 35 → 44), no issues

```
#265 (main) → #269 → #273 → #277 → #281 → #285 → #289 → #293 → #297 → #302
```

Fully linear, no duplication, no orphans. `#302` (Vol. 44) extends the chain cleanly on top of `#297`. Can merge in numeric order.

### 3.3 Microsoft Agent Framework — orphaned root still unresolved ⚠️

```
#267 (Vol. 31, main) ─┐  ← orphan: still no PR based on #267's branch
                       ├─ same branch history
#271 (Vol. 31+32, main) → #275 → #279 → #283 → #287 → #291 → #295 → #299 → #304
```

Unchanged from the 2026-07-15 finding: `#267` remains open with no downstream PR, still superseded by the `#271`-rooted chain (which now reaches Vol. 40 via the new `#304`). This was flagged as needing a maintainer decision five days ago and has not been acted on — `#267` is still sitting there.

**Consequence (unchanged):** not a hard text-conflict risk (`#271` was branched directly from `#267`'s commit), but whichever of `#267` / `#271` merges second will most likely hit a zero-diff / "nothing to merge" state. The longer this sits unresolved, the further the real chain (`#271`→`#304`) drifts from `main`, making eventual reconciliation carry more weight without adding risk.

### 3.4 PydanticAI — orphaned sub-chain still unresolved ⚠️

```
#266 (Vol. 30, main) → #270 → #274 → #278 (Vol. 33) ─┐  ← orphan: still no PR based on #278's branch
                                                       ├─ same branch history
                            #282 (Vol. 30–34, main) → #286 → #290 → #294 → #298 → #303
```

Unchanged from the 2026-07-15 finding: `#266`, `#270`, and `#274` are not orphaned themselves, but their sub-chain terminates at the still-orphaned `#278`. The real chain is rooted at `#282` and now reaches Vol. 38 via the new `#303`.

**Consequence (unchanged):** same as §3.3 — a merge-hygiene problem, not a text conflict, since `#282`'s branch was built directly on top of `#278`'s commit.

---

## 4. What changed since 2026-07-15

| Change | Detail |
|---|---|
| PRs added | `#301` (LangGraph Vol. 42), `#302` (Google ADK Vol. 44), `#303` (PydanticAI Vol. 38), `#304` (MS Agent Framework Vol. 40) — one per chain, all opened on the current tip of their chain |
| PRs merged/closed | None — `#264`–`#299` are all still open, including the two orphaned roots `#267` and `#278` |
| New conflicts | None |
| Prior recommendations acted on | None — the two orphan clean-ups recommended on 2026-07-15 are still outstanding |

---

## 5. Recommended Action

No urgent action is required — nothing is broken today, and no PR currently shows a real text conflict. The recommendation is unchanged from 2026-07-15:

1. **LangGraph (`#264`→`#301`) and Google ADK (`#265`→`#302`)**: merge in numeric order, no other action needed.
2. **Microsoft Agent Framework**: close `#267` as superseded by `#271` (or merge `#267` first — `#271` is already based on `main`, so GitHub will auto-shrink its diff to just Vol. 32 once `#267` lands), then continue `#271 → #275 → ... → #304` in order.
3. **PydanticAI**: close `#266`, `#270`, `#274`, `#278` as superseded by `#282` (or merge that sub-chain first — `#282` is already based on `main`, so its diff will auto-shrink to just Vol. 34), then continue `#282 → #286 → ... → #303` in order.
4. In all four series, merging **out of numeric order** within a chain will strand the skipped PR's base branch and likely turn everything above it "behind" or "dirty" against `main`, since each PR only carries a one-step diff from its immediate parent.
5. Each further volume added on top of an unresolved orphan chain increases the eventual clean-up surface — the two open items from 2026-07-15 are worth closing before the next round of volumes stacks on top.

Affected PRs needing a decision: **Microsoft Agent Framework** `#267`, `#271` (2 PRs); **PydanticAI** `#266`, `#270`, `#274`, `#278`, `#282` (5 PRs) — 7 total, same set as five days ago.

---

## 6. Summary

| Finding | Detail |
|---|---|
| Hard git conflicts | **None** — all 41 PRs are `mergeable_state: clean` right now |
| Clean single-root chains | LangGraph (`#264`→`#301`), Google ADK (`#265`→`#302`) |
| Orphaned-root / restart chains (redundant, not conflicting) | Microsoft Agent Framework: `#267` orphaned, superseded by `#271`-chain (now → `#304`); PydanticAI: `#278`-chain orphaned, superseded by `#282`-chain (now → `#303`) |
| PRs needing a maintainer decision | 7: `#266`, `#267`, `#270`, `#271`, `#274`, `#278`, `#282` — unchanged from 2026-07-15 |
| New since last audit | 4 new PRs (`#301`–`#304`), each a clean extension of an already-healthy chain tip; no new issues introduced |
| Recommended action | Same as 2026-07-15 — close (or merge-then-retarget) the two orphaned roots before merging their respective chains; LangGraph and Google ADK chains can merge straight through in numeric order |
